# C:road 작업 정리

> **이 문서의 용도** — 다음 작업을 시작할 때 이 파일 하나만 읽으면 프로젝트 전체 맥락이 잡히도록 정리한 인수인계 문서다.
> 이후로는 별도 메모리 저장 없이, "md 읽고 다음 작업 진행해줘" 라고 하면 이 문서를 기준으로 이어서 작업한다.
>
> - 작성 기준일: **2026-07-28**
> - 기준 브랜치: **`feature/jd-coach-news`** (`origin/main` 기준으로 분기. 옛 `feature/cas-quant-engine` 은 main 보다 8커밋 뒤처져 있어 새로 땄다)
> - 이번 작업분: 자소서 코치(2-5·9장) · 직무 트렌드(10장) · 이 회사 지금(11장) · 중견기업 판별(3-3) · B1(6-3-1)
>   · **Groq 전환(2-3-1)** · **B2 db.json 자동생성(6-5)** · **규칙 파서 근거 기반 교정(6-2)** — **전부 커밋·푸시 완료**
> - 테스트: `cas` **47** · `jd-competency` **22** · `job-trends` **12** · `news` **18** · `spec-parse` **37** — 합 **136** 전부 통과

---

## 1. 프로젝트 개요

**C:road (커리어 로드)** — 대학생 커리어 빌딩 플랫폼. (2026-08 이전 이름은 `careerly`) 선배들의 실제 스펙 데이터를 모아, 후배가 자기 스펙을 같은 기준으로 채점하고 목표 직무의 합격자 평균과 비교할 수 있게 하는 서비스.

| 항목 | 내용 |
|---|---|
| 위치 | `C:\Users\admin\Downloads\careerly\` |
| 프론트엔드 | 순수 HTML/CSS/JS. **`frontend/careerly.html` 단일 문서 SPA** (해시 라우팅), 빌드 없음 |
| 백엔드 | Node.js + Express (`backend/src/server.js`) |
| 저장소 | JSON 파일 DB (`backend/data/db.json`) — `store.js` 가 접근 레이어 |
| 인증 | bcryptjs + cookie-parser 세션 쿠키 |
| AI | **Groq**(`llama-3.3-70b-versatile`) **전용** — Ollama 경로는 2026-08 제거 (2-3-1) |

### 실행

```bash
cd backend
npm install
npm start          # node src/server.js  → http://localhost:3000
npm run dev        # nodemon
```

> ⚠️ **프론트엔드를 파일로 직접 열면 동작하지 않는다.** 데이터가 전부 서버 API에 있으므로 반드시 백엔드를 띄우고 `http://localhost:3000` 으로 접속해야 한다.
>
> **clone 한 사람도 위 3줄이면 끝난다** (2026-07-28, B2). `db.json` 은 없으면 자동 생성된다.
> `.env` 는 `cp .env.example .env` 로 만들되 키를 비워둬도 서버는 뜬다 — AI 기능만 503 이고 나머지는 동작한다.

### 폴더 구조 (실제)

```text
C:road
├─ frontend
│  ├─ careerly.html            # 단일 문서 SPA
│  ├─ css/  main.css · home.css · mentoring.css · jd-coach.css
│  └─ js/
│     ├─ app.js                # 라우터 + 인증
│     ├─ db.js                 # 데이터 레이어 (서버 API 호출 / 동기읽기·비동기쓰기 캐시)
│     ├─ ncs.js                # NCS 24개 직업 분류 카탈로그
│     ├─ career.js             # 커리어 로드맵
│     ├─ aggregation.js        # 스펙 집계 엔진
│     ├─ cas.js                # ★ CAS 점수 엔진 (배점 상수의 단일 출처)
│     ├─ cas-hero.js           # CAS 히어로 (점수·구성·비중·백분위·등급)
│     ├─ cas-compare.js        # 스펙 비교 막대
│     ├─ cas-radar.js          # CAS 레이더 차트
│     ├─ spec-form.js          # 스펙 입력 폼 (+ AI 한 번에 입력 패널)
│     ├─ jd-coach.js           # 자소서 코치 (직무기술서 → 역량 → 작성 가이드)
│     ├─ mentoring.js          # CAS 페이지 · 멘토 찾기 · 내 멘토링
│     ├─ backoffice.js
│     └─ home.js
├─ backend
│  ├─ src
│  │  ├─ server.js             # Express 진입점 + 인증/스펙/관리 API
│  │  ├─ store.js              # db.json 접근 레이어
│  │  ├─ db.js
│  │  ├─ company-classify.js   # ★ 회사명 → 기업규모 4분류
│  │  ├─ jd-competency.js      # ★ 역량 원형 14종 + 가이드 조립 (자소서 코치)
│  │  ├─ job-trends.js         # ★ 채용공고 → 직무별 역량 요구 빈도 집계·조회
│  │  ├─ news.js               # ★ 회사 뉴스 → 지원동기 소재 + 키워드 추출
│  │  ├─ ai-provider.js        # LLM 호출 한 겹 (Groq 전용) — 세 라우트가 공유
│  │  ├─ spec-parse.js
│  │  ├─ demo-seed.js
│  │  └─ routes/  careerData.js · recommendations.js · casAnalyze.js · jdCoach.js
│  ├─ data
│  │  ├─ db.json               # gitignore 대상 (users 1508 · userSpecs 1188)
│  │  ├─ career-data.json      # 학과/직무별 커리어 정적 데이터
│  │  ├─ ncs-taxonomy.json     # NCS 대분류 24개
│  │  ├─ public-orgs.json      # 공공기관 355건
│  │  ├─ local-public-orgs.json# 지방공기업·출자출연 1,312건
│  │  ├─ ftc-large-groups.json # 공정위 대규모기업집단 소속사 3,539건
│  │  ├─ work24-companies.json # 고용24 공채기업 2,102건 (중견 1,444) ← 중견기업 판별
│  │  └─ raw/                  # 지방공기업 CSV 원본 (EUC-KR)
│  └─ scripts/                 # 수집(fetch-*.js) · 집계(build-*.js)
├─ docs/                       # 설계 문서
│  ├─ 화면-구조-지도.md         # ★ "이 화면 HTML 어디 있지?" — 페이지·JS·CSS·API 지도
│  ├─ 배포.md                  # ★ Railway 배포 절차 (스테이징·2회차 배포 포함)
│  ├─ 팀-개발환경.md            # ★ 새 팀원이 clone 부터 첫 PR 까지 (15장)
│  └─ 2차-배포-수정목록.md      # ★ 14장을 "어느 파일·어느 콘솔" 로 푼 실행 목록
└─ test/  cas.test.js (47) · jd-competency.test.js (22) · jd-questions.test.js (23)
          job-trends.test.js (12) · news.test.js (32) · spec-parse.test.js (37)
          wage-jobs.test.js (16)
```

> **화면을 고치기 전에 `docs/화면-구조-지도.md` 를 볼 것.**
> 단일 문서 SPA 라 HTML 이 한 파일에 몰려 있고, 그중 스펙 입력·멘토 상세·백오피스는
> **HTML 이 빈 껍데기고 JS 가 내용을 만든다.** 모르면 한참 헤맨다.

---

## 2. 핵심 기능

### 2-1. 커리어 로드맵 (NCS 기반)

- **NCS 24개 직업 대분류 → 중분류 → 소분류(세부 직무)** 3단 탐색. 임의 분류가 아니라 공공데이터포털 NCS 공식 분류 데이터를 받아서 씀 (`scripts/fetch-ncs-taxonomy.js` → `ncs-taxonomy.json` → `gen-ncs-js.js` → `frontend/js/ncs.js`).
- 중분류별 **정량 스펙 집계** — 학점, 어학 성적, 자격증 보유율.
- 중분류별 **정성 스펙 집계** — 대외활동·인턴십·프로젝트 등 8개 항목 보유율.
- 옛 스키마(`dept`/`field`/`job`)는 `ncs.js` 의 `legacy` 매핑으로 NCS 중분류에 대응시켜 집계한다.

### 2-2. CAS 점수 (Career Asset Score) — 총 1000점

프로젝트의 핵심 엔진. **`frontend/js/cas.js` 가 모든 배점 상수의 단일 출처**이고, aggregation·spec-form·radar·백오피스·AI 프롬프트가 전부 여기서 값을 읽는다.

**채점 방식은 절대점수가 아니라 상대평가다.** 목표 직무 합격자(선배) 평균 원점수(`benchRaw`)를 기준으로 `relativeScore` 를 계산 — **평균이면 80%, 평균의 1.25배면 100%**. 화면의 비교 막대 폭도 같은 함수를 써서 "보이는 길이"와 "점수"가 같은 자를 쓴다.

#### 정량 (학점·어학·자격증)

`computeQuant()`. 가중치는 `docs/CAS-정량-가중치.md` 참조.

#### 정성 600점 — `computeQual()`

데이터 모델: `activities: [{ type, name, duration, role|stage, outcome }]` (구글 설문 구조와 1:1). 옛 boolean `qual` 은 `CAS.normalizeActivities()` 가 활동으로 환산해 하위호환.

**활동 유형 기본배점 (사용자 지시 서열):**

| 유형 | 배점 | | 유형 | 배점 |
|---|---:|---|---|---:|
| 인턴십 | 100 | | 프로젝트 | 62 |
| 공모전 | 78 | | 연구 | 58 |
| 대외활동·서포터즈 | 74 | | 동아리 | 46 |
| | | | 교환학생 | 40 |
| | | | 봉사 | 30 |
| | | | 기타 | 24 |

활동점수 = `기본배점 × 기간배수 × 역할(또는 연구단계)배수 × 성과배수`

#### 정량:정성 **동적** 비율

고정 4:6 이 아니다. `computeTotal()` 이 두 축의 성취도(`total/max`)를 비교해 **4:6 을 중심으로 양방향 이동**한다 (`resolveSplit`, `SPLIT_BASE .4 / SWING .1 / SENS .2`).

- 정성 우세 → **3:7**
- 정량 우세 → **5:5**
- 정량 비중 ∈ `[0.3, 0.5]`, 총점은 여전히 **1000 상한**
- 반환값에 `quantWeight` / `qualWeight` / `perf` 포함

#### CAS 페이지 배선

원래 `careerly.html` 의 CAS 히어로가 `87/100` **하드코딩**이라 누가 로그인하든 같은 점수가 떴다 — 엔진은 만들어만 두고 앱 어디서도 호출되지 않던 상태였다. `cas-hero.js` / `cas-compare.js` 신설로 연결 완료.

- **백분위**: 선배 각자를 같은 기준으로 채점해 줄 세운다. **표본 5명 미만이면 숨긴다** (3명 중 2등을 '상위 33%'로 보여주면 착시).
- **벤치마크 확장**: 직무 → 분야 → 학과 순으로 넓히되, 레이더와 달리 `MIN_PEERS` 를 채울 때까지 계속 넓힌다.
- `GET /api/specs` 는 익명화되어 `userId` 가 없으므로 백분위에서 자기 자신을 제외할 수 없다 (표준 정의상 포함이 맞으므로 문제 아님).

### 2-3. 스펙 입력 AI 분석 — `POST /api/cas/analyze`

`backend/src/routes/casAnalyze.js`. 학생이 반정형 텍스트로 경력을 쓰면 AI가 **활동을 폼 enum으로 정규화 + 빈칸 추론 + 정량값(gpa/toeic/opic/certs) 파싱**을 해준다.

- 프론트: `spec-form.js` 정성 섹션 상단 **'AI로 한 번에 입력'** 패널(textarea + 분석 버튼) → `DB.analyzeCas(text)` → 활동 편집기 `actState` 채우고 `applyQuant()` 로 정량 필드까지 채움. CSS는 `main.css` 의 `.sf-ai*` (녹색 테마).
- **프로바이더**: `CAS_AI_PROVIDER` (**기본 `groq`**, 대안 `ollama`). Ollama는 `POST {OLLAMA_HOST}/api/chat` 직접 호출 (`format:'json'`, temp .2, num_ctx 8192). `groq-sdk` 는 **지연 require** 라 ollama만 쓸 땐 로드되지 않는다.
- **루브릭 단일출처**: 프롬프트에 `cas.js` 상수(`ACTIVITY_TYPES`/`DURATION_MULT` 등)를 주입한다.
- **서버측 교정**: `coerceActivity()` 가 enum을 cas.js 유효목록 기준으로 고치고, `rescore()` 가 모델 점수를 버리고 `CAS.scoreActivity()` 로 재계산한다. 결정론 채점값도 교차검증용으로 함께 반환.
- `<think>` 를 내뱉는 모델 계열(qwen3·deepseek-r1·gpt-oss)은 JSON이 깨지므로 **모델명 정규식으로 `think:false` 를 주입**한다. 미지원 모델에 보내면 400 이므로 계열 판별이 반드시 필요.
- 연결거부·모델 미설치(404)는 502가 아니라 **503 + 설치 안내**로 분리. 타임아웃 240초(`CAS_AI_TIMEOUT_MS`).

### 2-3-1. Groq 전환 (2026-07-28)

기본 프로바이더를 **Groq(`llama-3.3-70b-versatile`)** 로 바꿨다. 로컬 Ollama 는 오프라인 대안으로 남는다.

| | 로컬 8B (Ollama) | Groq 70B |
|---|---|---|
| 스펙 AI 분석 | 약 60초 | **0.7초** |
| 자소서 코치 AI 보강 | 44초 | **0.69초** |

- **키 발급**: https://console.groq.com/keys (무료·카드 불필요) → `backend/.env` 의 `GROQ_API_KEY`
- **점검**: `node scripts/check-ai.js` — 서버를 띄우지 않고 프로바이더·모델·응답시간·JSON 파싱까지 확인한다 (`check-news-api.js` 와 같은 방식)
- **모델명은 `GROQ_MODEL` env**. Groq 는 모델을 주기적으로 폐기하므로 코드 수정 없이 `.env` 만 고치게 뺐다. 현행 목록: https://console.groq.com/docs/models

**오류 분기** — 원래 Groq 경로에는 오류 처리가 아예 없어서 키 오타·쿼터 초과·모델 폐기가 전부 `"AI 분석에 실패했습니다"`(500) 한 줄로 뭉개졌다. 사용자가 할 일이 각각 다르므로 갈랐다.

| 상황 | 상태코드 | 사용자가 할 일 |
|---|---|---|
| 401/403 | **503** | 키 확인 (설정 문제) |
| 429 | **429** | 기다렸다 재시도 |
| 404·모델 폐기 | **503** | `GROQ_MODEL` 교체 |
| 그 외 | 502 | — |

라우트(`casAnalyze`·`jdCoach`)도 **429 는 원문 문구를 그대로 내려보낸다** — 일반 실패로 보이면 사용자가 무엇을 해야 할지 알 수 없다.

> **A1(배포 대상)이 미결이어도 이제 이중 경로 부담이 줄었다.** 로컬·배포 모두 groq 가 기본이고 ollama 는 키 없이 쓰고 싶을 때의 선택지다. 다만 배포 확정은 여전히 필요하다.

### 2-4. 기업규모 자동분류 — `backend/src/company-classify.js`

회사명을 넣으면 **대기업 / 중견기업 / 중소기업 / 공기업** 4분류로 판정. `GET /api/company/classify`, `GET /api/company/stats`.

**이건 막힌 기능이 아니다** — 스펙 입력 폼에 기업유형 드롭다운이 이미 있고(`spec-form.js:102`), 자동판정은 그 값을 **미리 채워주는 편의**일 뿐이다. 학생이 직접 고칠 수 있다.

### 2-5. 자소서 코치 — `POST /api/jd/coach` (2026-07-27 신규)

채용공고(직무기술서)를 붙여넣으면 **요구 역량**을 뽑고, 역량마다 **"자소서에 이렇게 적어라"** 가이드를 준다. 화면은 `#page-jd` (`frontend/js/jd-coach.js`, `css/jd-coach.css`), 네비게이션 "자소서 코치".

**설계의 핵심 — AI 에게 문장을 쓰게 하지 않는다.**

| 단계 | 담당 | 이유 |
|---|---|---|
| ① 역량 추출 | 규칙(키워드) → 부족하면 AI | 대부분 규칙에서 끝난다(응답 **30ms**). AI 는 역량 **id 선택**만 하는 짧은 JSON |
| ② 가이드 문장 | **코드가 조립** (`jd-competency.js` 의 `ARCHETYPES`) | 산문 생성은 검증 수단이 없다. `rescore()` 와 같은 원칙 |

이 판단의 근거는 6-1·6-2 에 정리된 실측이다 — 8B 는 분류는 되지만 생성은 못 믿고, CPU 추론에서 출력 토큰이 곧 대기시간이다. 실제로 AI 보강이 붙은 요청은 **44초**(llama3.1), 규칙만이면 **0.03초**다.

**역량 원형 사전** — `backend/src/jd-competency.js` 의 `ARCHETYPES` **14종**. NCS 공식 데이터에는 직무별 역량 서술이 없어서(`ncs-taxonomy.json` 은 분류 이름뿐) 직접 정리했다. 각 원형이 갖는 것:

`keywords`(추출용) · `reads`(기업이 보는 것) · `frame`(①~⑤ 작성 순서) · `openers`(첫 문장 여는 방식) · `evidence`(근거가 되는 CAS 활동 유형) · `numbers`(숫자로 바꿔야 할 것) · `avoid`(감점 표현) · `followup`(이렇게 쓰면 면접에서 들어올 질문)

**신뢰를 지키는 장치 4개** (이 중 하나라도 빼면 기능의 성격이 바뀐다):

1. **공고 원문 근거 인용** — 역량마다 그 역량이 뽑힌 근거 문장을 그대로 보여준다. 자동 추출에는 오탐이 반드시 섞이므로, 근거가 보여야 사용자가 스스로 걸러낸다.
2. **대필 아님 명시** — 서버가 `disclaimer` 문구를 내려 화면에 띄운다. 완성 문장을 주면 대필이고 유사도·AI 검출에 걸린다.
3. **소재 배분(`spreadMaterials`)** — 역량별로 독립적으로 소재를 고르면 배점 1위(보통 인턴십)가 모든 카드를 차지한다(실측: 역량 7개가 전부 같은 인턴 추천). 중요한 역량부터 아직 안 쓴 활동을 배정하고, 남는 게 없으면 **겹침을 밝힌다**.
4. **약한 키워드 가중치(`WEAK_KEYWORDS`, 0.4점)** — "프로모션 기획 및 실행" 한 문장이 기획력·실행력·주도성을 동점으로 만들어, 우대사항의 글로벌·고객지향을 밀어냈다(실측). 약한 근거 하나(0.4)뿐인 역량은 버린다. 단 `기획`·`분석` 은 업무의 이름이라 **강한 쪽**에 둔다.

**AI 가 사전에 없는 역량을 들고 오면** (`buildCustom`) 그럴듯한 가이드를 지어내지 않고 "직접 확인이 필요한 직무 특수 요건"으로 정직하게 넘긴다.

**비로그인·스펙 미입력도 빈 화면이 되지 않는다** — 골격 가이드는 그대로 나오고, "스펙을 입력하면 소재까지 연결해 드립니다" 안내가 붙는다. 활동은 프론트가 자기 캐시에서 보낸다(`DB.coachJd`) — 본인 데이터를 본인 화면에 되돌려주는 용도라 서버 세션을 뒤지지 않는다.

**부수 리팩터링** — 프로바이더 호출을 `backend/src/ai-provider.js` 로 추출했다. `casAnalyze.js` 도 이제 이걸 쓴다. 모델 미설치 404 → 503 매핑, `<think>` 차단, 설치된 모델 자동 선택 같은 8B 특유의 처리가 두 곳으로 복사되면 반드시 한쪽만 고쳐지고 갈린다. (실제로 이 PC 에는 `qwen3:8b` 가 없어 `llama3.1:latest` 로 자동 대체돼 동작했다 — 이 폴백이 살아 있음을 확인한 셈이다.)

### 2-6. 그 외

- 회원가입 / 로그인 / 로그아웃 / 마이페이지 스펙 입력
- 멘토 찾기 — 목록에서 멘토 이름 가운데 글자 마스킹
- 내 멘토링
- 백오피스 — 집계 현황, 데모 시드 / 랜덤 시드(구조화 활동 생성) 데이터 생성, 회원 관리 (`devOnly` 가드)
- 좁은 화면(≤900px) 네비게이션 드로어

---

## 3. 외부 데이터 연동 현황

설계는 `docs/외부API-연동구조.md`. 수집기는 `backend/scripts/`.

| 대상 | 상태 | 소스 / 규격 | 캐시 |
|---|---|---|---|
| NCS 직업분류 | ✅ 완료 | data.go.kr 오픈API | `ncs-taxonomy.json` (24 대분류) |
| 공공기관 (중앙) | ✅ 완료 | 재정경제부 공공기관 지정현황 | `public-orgs.json` **355건** |
| 지방공기업·출자출연 | ✅ 완료 | 행안부 지방공기업평가원 설립현황 2종 (`15048282`, `15048281`) | `local-public-orgs.json` **1,312건** (공기업 422 · 출자출연 890) |
| 대기업 (공정위) | ✅ 완료 | 대규모기업집단 소속회사 | `ftc-large-groups.json` **3,539건** (202605 기준) |
| **중견기업 (고용24)** | ✅ **완료 (2026-07-28)** | 공채기업정보 `210L31` `coClcd=40` | `work24-companies.json` **2,102건** (중견 1,444) |

### 3-1. 공정위 대기업 API — 삽질 기록 (중요)

실패 원인은 승인/게이트웨이 문제가 **아니라 코드가 통째로 틀린 규격**이었다. `[승인]` 으로 표시되는데도 실패해서, 모르면 키가 틀린 줄 알고 헤맨다.

| | 틀린 옛 코드 | 실제 규격 |
|---|---|---|
| End Point | `openapi.egroup.go.kr` | `https://apis.data.go.kr/1130000/appnGroupAffiList/appnGroupAffiListApi` |
| `presentnYear` | 연도 4자리 `2026` → **HTTP 500 'Unexpected errors'** | 지정년월 **YYYYMM** (예: `202605`) |
| 응답 아이템 | — | `<appnGroupAffi>` |
| 회사명 필드 | `affiliateNm` / `cmpnyNm` (전부 오답) | **`entrprsNm`** |
| `unityGrupCode` | 필수로 오해 | 없이 전체 조회 가능 |

갱신: `cd backend && node scripts/fetch-ftc-groups.js` (최신 지정년월 자동 탐색). `classify('삼성전자') → 대기업` 확인됨.

### 3-2. 지방공기업 — CSV 예외 (중요)

기존 공공기관 명단은 **공공기관운영법 대상 = 중앙정부 산하만** 담아서, 지방공기업법 대상 지자체 산하 기관이 통째로 빠져 있었다 → 서울교통공사·서울시설공단·용인문화재단 등이 '중소기업'으로 오분류.

- 이 두 데이터셋은 **활용신청·오픈API 자동변환이 없고 CSV 다운로드만 된다.** 오픈API로 붙이려 하면 키가 멀쩡해도 **401**.
- 그래서 `scripts/fetch-local-public-orgs.js` 만 **네트워크를 타지 않고** `backend/data/raw/` 의 CSV를 읽는다 — 다른 `fetch-*` 와 유일하게 다른 점.
- CSV는 **EUC-KR** 이고 컬럼명이 파일마다 다르다 (`지방공기업명` vs `기관명`). 갱신은 CSV만 갈아끼우고 재실행.
- **분류 정책(사용자 결정)**: 출자출연기관(재단·진흥원)도 지방공기업과 함께 **전부 '공기업' 한 칸**. 취업 준비 관점에서 공공부문 채용으로 묶는 게 유용하다는 판단.

> **How to apply** — 지방 공공기관 오분류가 또 보이면 `company-classify.js` 를 의심하기 전에 **CSV 고시본이 최신인지부터** 볼 것.

### 3-3. 중견기업 — **해결됨 (2026-07-28)**

몇 주간 "요청 URL 미확보"로 막혀 있던 항목이다. 사용자가 고용24 명세서에서 **공채기업정보 API** 를 찾아오면서 풀렸다.

```text
https://www.work24.go.kr/cm/openApi/call/wk/callOpenApiSvcInfo210L31.do
  ?authKey=<키>&callTp=L&returnType=XML&startPage=1&display=100&coClcd=40
```

- `coClcd` : **10 대기업 · 20 공기업 · 30 공공기관 · 40 중견기업 · 50 외국계기업**
- 응답 : `<dhsOpenEmpHireInfoList>` > `total` / `<dhsOpenEmpHireInfo>` 반복
- 수집 : `node scripts/fetch-work24-companies.js` -> `data/work24-companies.json`
- 실측 건수 : **중견 1,444 · 공공기관 504 · 대기업 78 · 공기업 78 · 외국계 0** (중복 제거 후 2,102건)
- 검증 : `classify('아주산업') -> 중견기업 (source: 고용24:중견기업)`

> **명세서 표에 오타가 있다.** 출력항목에 `<coClcdNm>` 이 두 번 적혀 있고 하나를 '회사명'이라 설명하는데, **실제 응답에서 회사명은 `<coNm>`, 기업구분명이 `<coClcdNm>`** 이다. 명세서대로 읽으면 회사명 자리에 "중견기업"이 들어간다.

**전수 명단이 아니다** — 고용24에 **공채를 등록한 기업**만 담긴다. 중견기업이 국내에 1,444개일 리 없으므로 **여전히 '중소기업'으로 잘못 떨어지는 회사가 남는다.** 그래서 자동판정 실패를 화면에 알리는 일(B1)이 이 수집보다 여전히 중요하다.

**우선순위 규칙** — 공정위·공공기관 캐시가 이미 잡은 회사는 덮어쓰지 않는다(그쪽이 더 공식적). 실제로 베어링아트·일진베어링은 고용24가 '중견기업'이라 하지만 공정위 대규모기업집단 소속이라 **대기업**으로 분류된다. 이게 맞다.

**참고 — 폐기된 대체 소스 조사** (다시 조사하지 말 것): data.go.kr 에 중견기업 명단 데이터셋이 없고(집계 통계뿐), OpenDART 도 부적합하다(기업개황에 규모 필드가 없고 공시의무 법인만 담아 비상장 중견이 대거 누락). 이 조사 결과는 여전히 유효하며, 결론이 "Work24 가 유일한 소스"였던 것도 맞았다.

---

## 4. 환경변수 (`backend/.env`)

`.env.example` 을 복사해서 쓴다. `.env` 는 gitignore 대상.

| 키 | 용도 |
|---|---|
| `DATA_GO_KR_SERVICE_KEY` | 공공데이터포털 (NCS·공공기관·공정위) — 계정당 하나로 전부 호출 |
| `WORK24_API_KEY` | 고용24 별도 키. **비워두면 중견기업 자동판정만 빠지고 나머지는 정상 동작** |
| `CAREERNET_API_KEY` | 커리어넷 자체 키(data.go.kr 키와 다르다). 대학 목록 수집 → 스펙 입력의 '학교' 자동완성. **비워두면 자동완성만 빠지고 직접 입력은 그대로 된다.** 점검: `node scripts/check-careernet-api.js` |
| `ADMIN_USERNAMES` | 백오피스에 들어갈 수 있는 아이디(쉼표 구분). 서버 부팅 때 `users.is_admin` 을 켠다. **첫 관리자를 만드는 유일한 방법** — 비어 있으면 아무도 백오피스에 못 들어간다. 이름을 빼도 권한이 해제되지는 않는다(설정 실수로 전원이 잠기지 않게). |
| `NICE_SITE_CODE` / `NICE_SITE_PASSWORD` / `NICE_RETURN_URL` | 본인확인(CI) — '한 사람 = 한 계정'. **비워두면 개발 모드**(휴대폰 번호 해시로 모의 CI)로 돌아 중복 차단까지 그대로 확인된다. 단 `NODE_ENV=production` 에서는 개발 모드가 503 으로 막히므로 **배포 전 실제 키 필수**. 자세한 것은 `src/nice-auth.js` |
| ~~`CAS_AI_PROVIDER`~~ | 폐지 (2026-08, Groq 전용) |
| `GROQ_API_KEY` | **필수** (미설정 시 AI 기능만 503). 무료 발급: https://console.groq.com/keys |
| `GROQ_MODEL` | 기본 `llama-3.3-70b-versatile`. **모델 폐기 공지가 뜨면 여기만 바꾼다** |
| ~~`OLLAMA_HOST`~~ | 폐지 |
| ~~`OLLAMA_MODEL`~~ | 폐지 |
| `CAS_AI_TIMEOUT_MS` | `240000` |
| `NAVER_CLIENT_ID` / `NAVER_CLIENT_SECRET` | **뉴스 검색**. NCP NAVER API Hub 의 Application 키 — 13-4 의 표를 먼저 볼 것 |

**배포에서 추가로 필요한 것** (전부 **앱 서비스**에 넣는다. MySQL 서비스가 아니다 —
값을 읽는 코드가 전부 `backend/src/` 에서 돌기 때문이다):

| 키 | 용도 |
|---|---|
| `MYSQL_URL` | Railway 에서는 `${{MySQL.MYSQL_URL}}`. **같은 프로젝트 안에서만 참조가 풀린다** |
| `NODE_ENV`, `ALLOWED_ORIGINS`, `OAUTH_REDIRECT_BASE` | 뒤의 둘은 운영 도메인과 정확히 같아야 한다 |
| `NAVER_LOGIN_CLIENT_ID` / `NAVER_LOGIN_CLIENT_SECRET` | 네이버 **로그인** (뉴스 키와 다르다 — 13-4) |
| `KAKAO_REST_API_KEY` / `KAKAO_CLIENT_SECRET` | 카카오 로그인. client_secret 은 새 REST 키에서 **기본 활성**이라 사실상 필수 |
| `TOSS_CLIENT_KEY` / `TOSS_SECRET_KEY` | 결제. **secret 은 서버 전용 — 프론트로 내려보내지 않는다** |

> `NEWS_PROVIDER` 는 **설정하지 않는 게 기본**이다. 값이 있으면 키와 무관하게 그쪽을
> 강제하므로, `web` 이 남아 있으면 키가 멀쩡해도 웹 폴백으로 돈다.

설정이 실제로 동작하는지: `cd backend && node scripts/check-ai.js` (AI) · `node scripts/check-news-api.js` (뉴스).

---

## 5. 작업 이력 (커밋 기준)

```text
d27e254  fix(parse): 근거 없이 단정하던 곳 세 군데를 근거 기반으로 바꾼다 (6-2·6-2-1)
43148d3  fix(store): db.json 이 없으면 새로 만든다 — clone 직후 500 해소 (B2, 6-5)
bc4c0c7  feat(ai): AI 프로바이더를 Groq 로 전환하고 실패 원인을 갈라 보여준다 (2-3-1)
6876a76  docs: 작업 정리 문서 (CROAD-작업정리.md)
36d7200  fix(B1): 인턴 회사규모 판정 실패를 화면에 표시하고 직접 고치게 한다
068f335  feat(분류): 고용24 공채기업정보로 중견기업 판별 (몇 주째 막혀 있던 항목)
5afb6b1  feat(이 회사 지금): 뉴스 기반 지원동기 소재 + 자소서 키워드 추천
1a8f4cc  feat(직무 트렌드): 채용공고 집계 → 역량 카드에 "공고 N건 중 M% 요구" 배지
(자소서 코치) 직무기술서 → 요구역량 → 자소서 작성 가이드 + AI 프로바이더 분리
bfb9573  docs: CAS 정리 문서 추가
fe938b5  스펙 비교 8행을 실제 데이터로 배선
87d9899  CAS 히어로를 실제 점수 엔진에 배선
79294e4  AI 분석 결과에 두 점수의 차이를 설명하는 안내 추가
788cfa6  정성 총점 표시를 결정론 값으로 교체
c2c2d47  분류: 지방공기업 수집을 CSV 입력으로 변경 + 명단 1,312건 캐시
82712fe  분류: 지방공기업·지방출자출연기관을 공공기관 소스로 추가
624cd86  스펙 AI 분석: 반정형 텍스트 → 활동/정량 자동 입력
bb159f3  CAS 정성 600점 엔진 + 정량:정성 동적 비율
8a09408  fix(classify): 공정위 대기업 API 실제 규격으로 수정 + 명단 캐시
6d761e6  커리어 로드맵: 기업유형 4분류 + 회사명 기반 자동판정
5b494f9  멘토찾기 목록에서 멘토 이름 가운데 글자 마스킹
dc7aa68  커리어 로드맵 분류를 NCS 공식 데이터로 교체
4cfb533  백엔드: .env 지원 (dotenv) + .env.example
```

### 폐기된 결정 (되돌리지 말 것)

| 폐기 | 이유 |
|---|---|
| **localStorage 저장소** | 회원마다 자기 브라우저 데이터만 보여 "선배 n명 집계"가 성립하지 않았고, 비밀번호가 평문 저장됐다. → 서버 API 로 전환 |
| **Gemini** | 사용자 구글 계정이 무료티어 미지원(`free_tier limit 0`, 프로젝트를 새로 만들어도 재발 — 지역/계정 문제)이라 키 2개 다 429 |
| **`@anthropic-ai/sdk`, `@google/generative-ai`** | 제거함 |
| **Groq 를 기본 프로바이더로** | 키 발급 없이 쓰려고 로컬 Ollama 기본으로 전환. groq 경로는 **배포용으로 남겨둠** |
| **AI 총점(`aiTotal`)을 헤드라인에** | 아래 6-1 참조 |
| **다중 HTML 페이지** (`Main_web.html`, `login.html`, `signup.html`, `mypage.html`, `career.html`) | 단일 SPA `careerly.html` 로 통합 |

---

## 6. 부족한 점 / 실측 피드백

### 6-1. 로컬 8B 모델의 실패 패턴 (실측)

이 PC 사양: **RAM 28GB, AMD 780M iGPU = 사실상 CPU 추론**. `qwen3:8b`(5.2GB) 설치·검증, 요청 1건 ≈ **1분**.

| # | 증상 | 대응 |
|---|---|---|
| 1 | `rawScore` 에 곱셈 결과가 아니라 **기본배점만** 넣음 (reason엔 187.2, 필드엔 100) | **해결** — `rescore()` 추가, 모델 점수를 버리고 서버에서 `CAS.scoreActivity()` 재계산 |
| 2 | 정성 **총점**(`aiTotal`)이 상대평가 환산 산수를 못해 크게 흔들림 — 같은 입력에 **595 / 360** (결정론 값은 둘 다 600) | **해결** — 표시를 `deterministicTotal` 로 교체. AI 총점은 '참고(AI 추정)'으로 병기 + 두 숫자 차이 설명 문구 추가 |

두 번째 문제가 실제로 사용자를 헷갈리게 했다: `aiTotal` 은 **어디에도 저장되지 않는 표시용 숫자**인데 헤드라인을 차지해서, **입력할 때 본 점수와 저장 후 로드맵에서 본 점수가 어긋났다** (실측 AI 190 vs 결정론 367).

**결론: 활동 분류·정량 파싱은 믿을 만하지만, 총점은 결정론 값을 써야 한다.**

### 6-2. ~~입력 형식이 결과를 좌우한다~~ → **원인은 모델이 아니라 규칙 파서였다** (2026-07-28 정정·해결)

> **아래 옛 진단은 틀렸다. 되풀이하지 말 것.**
>
> | 입력 | 결과 |
> |---|---|
> | 줄글 한 문단 | 활동 3개 중 **1개만** 추출 |
> | 줄 단위 구조화 | 3개 다 정상 |
>
> 여기서 "8B 가 줄글을 못 읽는다 → 모델을 키우거나 placeholder 로 양식을 유도하자"고 결론지었다.

Groq 전환 후 재현 테스트에서 드러난 **진짜 원인**: 모델에 같은 문단을 직접 넣으면 **3개를 정확히 다 뽑는다.** 그런데 `/api/cas/analyze` 로는 여전히 1개였다. `ruleParse()` 가 입력을 `split('\n')` 로만 자르므로 문단은 통째로 한 줄이고, `parseLine()` 이 먼저 맞는 유형 하나로 활동 1건을 만들어 버린 것이다.

```
"카카오에서 6개월 인턴 … 공모전에서 대상 … 2년 동안 동아리 스터디장"
  → 인턴십 1건 / 기간 '1년이상'(동아리 것) / 성과 '수상'(공모전 것)
```

활동 3건이 1건으로 합쳐지고 **남의 기간·성과가 인턴 점수에 붙었다.** 게다가 `unparsedLines` 가 비어서 **AI 는 호출되지도 않았다** — 못 읽었는데 "다 읽었다"고 보고하는 구조라, Groq 로 바꿔도 이 입력은 결과가 그대로였다(응답 0.25초에 AI 미호출).

**고친 것** — 유형 신호가 2개 이상인 줄은 규칙이 단정하지 않고 AI 에게 넘긴다(`countTypeSignals`). 규칙 해석은 `fallbackActivities` 로 남겨 **AI 가 실패했을 때만** 쓴다. 이걸 안 남기면 키가 없거나 오프라인일 때 그 줄이 통째로 사라져 지금보다 나빠진다(가짜 키로 확인 — 오류가 아니라 예전과 같은 결과 + 안내 문구).

| | 이전 | 지금 |
|---|---|---|
| 활동 | 인턴십 1건 | 인턴십·공모전·동아리 3건 |
| 인턴 기간 | `1년이상` ← 동아리의 "2년" | `3개월~6개월` ← 자기 것 |
| 인턴 성과 | `수상` ← 공모전의 대상 | `결과물 없음` |
| 정성 총점 | 286 | 443 |

> **How to apply** — "AI 가 못 읽는다" 싶을 때 **모델에 직접 넣어보고 라우트 결과와 비교할 것.** 이번엔 규칙 파서가 조용히 가로채고 있었다. `unparsedLines` 가 비어 있으면 AI 는 아예 호출되지 않는다.

### 6-2-1. 근거 없이 단정하던 곳 (2026-07-28, 6-2 와 같은 커밋)

6-2 를 고치며 같은 성격의 문제 두 개를 더 찾았다. **셋 다 에러가 나지 않고 점수만 조용히 틀렸다.**

**① 기간 구간을 모델이 골랐다** — `3개월~6개월` 과 `6개월~1년` 이 둘 다 "6개월"을 포함해 같은 입력에도 답이 갈렸다(실측: "6개월간" → `6개월~1년`). 라벨을 바꾸면 `cas.js` 단일 출처와 저장된 스펙 1,188건을 건드려야 하므로, **모델에게는 원문 표현(`durationText`)만 받고 구간은 `parseDuration()` 이 정한다.** 규칙 경로와 AI 경로가 같은 자를 쓰게 됐다. `rescore()` 와 같은 원칙.

**② 없는 성과를 지어냈다** — 입력에 없는 `전환, 정규직 합격` 을 인턴에 붙였다. **5회 중 3회 재현.** 성과는 배수라 점수를 **120 → 168 로 40% 부풀린다.** `outcomeText` 로 원문 근거를 함께 받아, 근거가 없으면 성과를 인정하지 않는다. 근거는 있는데 규칙이 분류 못 하는 표현("특허 출원")이면 모델 판단을 살린다 — 막으려는 건 *없는 근거를 지어내는 것*이지 *드문 성과*가 아니다. 수정 후 5회 전부 `결과물 없음(120)` 으로 안정.

**③ 덤 — `ROLE_RULES` 의 `대표` 가 "대표활동1)" 머리말에 걸렸다.** 팀원이라고 적어도 **팀장으로 채점**됐다. 폼 placeholder 가 유도하는 표기 자체가 함정을 밟고 있었다. 머리말은 활동명에서 제거하고 `/` 도 구분자로 처리한다(`"카카오 인턴 / /"` → `"카카오 인턴"`).

테스트 `test/spec-parse.test.js` **37개** 추가.

### 6-3. 조용히 틀린 값을 보여주는 곳

**이 범주가 가장 위험하다 — 에러가 안 나서 눈에 띄지 않고 방치된다.**

- ~~**기업분류 자동판정 실패**가 조용히 '중소기업'으로 채워진다.~~ → ✅ 해결 (6-3-1)
- ~~줄글 입력에서 **활동 3건이 1건으로 합쳐지고 남의 기간·성과가 붙는다.**~~ → ✅ 해결 (6-2)
- ~~AI 가 **없는 성과("정규직 전환")를 지어내** 점수를 40% 부풀린다.~~ → ✅ 해결 (6-2-1)
- **GAP(부족한 항목) 이 고정 목업.** `mentoring.js:370` 의 `GAP_DATA` 하드코딩 배열이라 누가 로그인해도 정보처리기사·SQLD·AWS SAA 가 뜬다. CAS 페이지의 **마지막 남은 목업**. (B4)
- CAS 페이지 잔여 하드코딩 ("선배 24명" 등). (B5)

> **이 범주를 찾는 법 (실측으로 배운 것)** — 에러가 안 나므로 로그를 봐서는 못 찾는다. 대신 **같은 입력을 여러 번 넣어 결과가 흔들리는지 본다.** "정규직 전환" 환각은 5회 반복으로 3회 재현해 발견했고, 기간 구간이 갈리는 것도 같은 방식으로 잡았다. 한 번만 돌려봤으면 둘 다 지나쳤다.

### 6-3-1. B1 처리 결과 — 진짜 구멍은 다른 곳에 있었다 (2026-07-28)

B1 을 하러 들어가 보니 **회사명 입력칸의 판정 실패 안내는 이미 구현돼 있었다**(`spec-form.js` 의 `sf-company-hint` — "공식 명단에 없는 회사예요. 기업 유형을 직접 골라주세요"). 위 6-3 의 서술은 그 기능이 들어오기 전에 쓰인 것으로, **낡은 내용이었다.**

대신 훨씬 조용한 구멍이 **인턴십 활동의 회사 규모**에 있었다.

- 인턴십은 회사 규모가 CAS 점수에 **배수**로 들어간다 (`COMPANY_MULT` — 대기업·공공기관 ×1.2, 중견 ×1.1, 중소 ×1.0).
- 이 값은 입력칸이 아니라 **활동명에서 회사명을 추출해 자동 판정**한 결과다 (`casAnalyze.js` 의 `attachCompanyTier`).
- 그런데 `companyTierNote()` 가 **판정에 성공했고 배수가 1이 아닐 때만** 안내를 띄웠다. 즉 **회사를 못 찾으면 아무 말 없이 ×1.0** 이 적용됐다.
- 결과: "하계 인턴십" 처럼 활동명에 회사명이 없거나 명단에 없는 회사면, **대기업 인턴을 하고도 점수가 낮게 나오는데 학생은 이유를 알 수 없었다.** 에러가 안 나므로 발견되지도 않는다.

**고친 것** (`companyTierNote` → `companyTierRow`):

| 상태 | 예전 | 지금 |
|---|---|---|
| 대기업으로 확인 | "×1.2 적용됐어요" | 동일 + **직접 고치기** 드롭다운 |
| 중소기업으로 확인 | **표시 없음**(배수 1이라 숨김) | "중소기업으로 확인 (×1)" 명시 |
| **판정 실패** | **표시 없음** | ⚠️ "회사를 못 찾아 **중소기업 기준(×1.0)** 으로 계산됩니다. 대기업·공공기관 인턴이면 직접 골라주세요 — 점수가 최대 ×1.2 까지 달라져요" + 드롭다운 |

핵심은 **'조용히 틀리는 것'을 '보이고 고칠 수 있는 것'으로 바꾼 것**이다. 단순 경고가 아니라 그 자리에서 고를 수 있게 했고(`data-field="companyTier"`), 고른 값은 `collectActivities()` 가 저장하므로 점수에 즉시 반영된다. 판정에 성공한 경우에도 드롭다운을 주는데, 계열사·지사처럼 **학생이 아는 사실이 더 정확할 수 있기 때문**이다.

**서버도 같이 고쳤다** — `GET /api/company/classify` 가 `matched:false` 일 때도 `corpType:'small'` 을 내려보내고 있었다. 지금 화면은 `matched` 를 보고 걸러내지만, 그걸 잊은 다음 호출자가 생기면 **"회사를 못 찾음"과 "중소기업으로 확인됨"이 구분되지 않은 채 저장된다.** 이제 실패 시 `corpType:null` 을 주고, 점수 계산에 실제로 쓰이는 값은 `fallbackCorpType` 으로 분리했다 — 값 자체를 비워 실수할 수 없게 만든 것이다.

### 6-4. ~~배포하면 깨지는 것~~ → **해소됨** (2026-07-28, 2-3-1)

> 옛 내용: Ollama 는 localhost 전제라 배포하면 AI 분석이 전부 503. 8B 는 6~7GB RAM 이 필요해 무료티어 불가이고, 8GB VPS(월 2~4만원)를 써도 공유 vCPU 에서는 추론 3~5분이라 **호스팅 프록시 타임아웃(60~120초)에 걸려 돈을 쓰고도 동작하지 않는다.**

**기본 프로바이더를 Groq 로 바꿔 이 문제가 사라졌다.** 응답 0.7초라 프록시 타임아웃과 무관하고, 무료 티어라 인프라 증분비용이 0 이다. 8장 재정전문가·CEO 권고("배포 시 Groq 로 확정하라")를 그대로 따른 것이다.

남은 배포 이슈는 `NODE_ENV`·`ALLOWED_ORIGINS` 설정과 A1(배포 대상 확정)뿐이다.

### 6-5. ~~clone 만 한 사람은 서버가 떠도 화면이 안 된다~~ → **해결됨** (2026-07-28, B2)

> 옛 내용: `store.js` 의 `readDb()` 가 try/catch 없이 `readFileSync` 인데 `db.json` 은 gitignore 대상이라, clone 직후엔 API 마다 500 이 났다. **실제로 친구 PC 에서 겪은 문제.**

`readDb()` 가 파일이 없으면 시드로 새로 만든다. **파일 없음과 파일 손상은 반드시 구분한다** — "읽기 실패하면 만든다"로 뭉뚱그리면 JSON 이 한 글자 깨졌을 때 회원 1,508명을 빈 DB 로 덮어쓴다.

| 상황 | 동작 |
|---|---|
| 파일 없음 | 시드로 생성하고 진행 |
| JSON 손상 | **멈춘다** (덮어쓰지 않고 고치는 방법을 안내) |
| ENOENT 외 오류(권한 등) | 그대로 올림 |

- `data/db-seed.json` (커밋 대상) — `departments` 7건 · `careerSpecs` 3건의 **정적 참조 데이터만**. 회원 데이터 0건.
- `writeDb` 는 임시파일 + `rename`. 저장 도중 죽으면 반쯤 쓰인 파일이 남아 위의 "손상"이 되므로, **손상이 생기는 경로 자체**를 막았다.
- 검증: `db.json` 을 치우고 clone 상황 재현 → `career-data`/`departments`/`specs` 200, 회원가입 201, 로그인 200.

### 6-6. 문서가 코드와 불일치 — **부분 해결** (2026-07-28)

- ✅ 이 문서(`CROAD-작업정리.md`) — 갱신 완료
- ✅ `README.md` — 갱신 완료 (localStorage·다중 HTML·"HTML 직접 열어도 동작" 서술 제거)
- ✅ 루트 `C:\Users\admin\CLAUDE.md` — 갱신 완료

> **How to apply** — 코드를 고치면 이 세 파일도 같이 본다. 특히 **저장 방식·실행 방법·AI 프로바이더**는 세 곳에 중복 서술돼 있어 한 곳만 고치면 반드시 갈린다.

---

## 7. 앞으로 해야 할 일 (배포 전 기준 — **최신 목록은 14장**)

> 이 장은 1차 배포 이전에 정리한 것이다. 완료된 항목에 취소선을 그어 두었지만,
> **지금 무엇부터 할지는 14장을 본다.**

### A. 사용자 결정·조치가 필요 (내가 못 푸는 것)

| # | 항목 | 상세 |
|---|---|---|
| ~~A1~~ | ~~배포 대상 확정~~ | ✅ **2026-07-30 해결** — Railway + MySQL 로 1차 배포 완료 (**13장**) |
| ~~A2~~ | ~~고용24 중견기업 요청 URL~~ | ✅ **2026-07-28 해결** — 공채기업정보 `210L31` 로 수집 완료 (3-3) |
| **A3** | **고용24 채용정보(210L01) 는 기업·기관 회원만 호출 가능** | 개인회원 계정으로는 막힌다. 학교·사업단 명의 계정으로 재신청할지 결정 필요 (10-7) |
| ~~A4~~ | ~~네이버 뉴스 API 키~~ | ✅ **2026-07-28 해결** — NCP NAVER API Hub 키로 동작 (11-4) |

### B. 코드 작업 (바로 가능)

우선순위 순.

| # | 작업 | 비용 대비 효과 |
|---|---|---|
| ~~B1~~ | ~~기업분류 자동판정 실패 안내~~ | ✅ **2026-07-28 완료** (6-3-1 참조) |
| ~~B2~~ | ~~`store.js` db.json 자동생성~~ | ✅ **2026-07-28 완료** (6-5) |
| **B3** | **스펙 입력 placeholder** — 줄 단위 양식 예시를 깔아 오추출을 예방 | 근본 원인(규칙 파서)은 6-2 에서 고쳤으므로 **긴급도가 내려갔다.** 이제는 "정확도 보완"이 아니라 **입력을 쉽게 하는 UX** 목적이다 |
| **B4** | **GAP(부족한 항목) 배선** — `GAP_DATA` 하드코딩 제거 | ⭐ 자격증은 Aggregator 보유율로 바로 가능. **활동·수상은 "무엇을 부족으로 볼지" 기준 결정이 필요** (사용자 확인 사항). 8장 CEO 가 지정한 공개 선행조건 1번 |
| **B5** | CAS 페이지 잔여 하드코딩 제거 ("선배 24명" 등) | ⭐ 8장 CEO 공개 선행조건 1번 |
| ~~B6~~ | ~~`README.md` · 루트 `CLAUDE.md` 갱신~~ | ✅ **2026-07-28 완료** (6-6) |
| **B11** | **시드/실데이터 분리 표시** — 스펙 레코드에 출처 플래그, 집계에서 분리 | 8장 CEO 공개 선행조건 2번. `db.json` 의 userSpecs 1,188건은 데모 시드다 |
| **B12** | **CAS 를 '참고 지표'로 프레이밍하는 문구를 화면에 표시** | 8장 CEO 공개 선행조건 3번. 배점 서열이 데이터 도출이 아니라 설계 판단임을 밝힌다 |
| **B7** | **자소서 코치 후속** — 역량 원형 14종은 범용 공고 기준이다. 이공계·연구직 공고를 넣으면 `custom` 으로 빠지는 항목이 많다. 실제 공고 몇 개로 돌려보고 부족한 원형을 추가하는 것이 다음 단계 | 원형 1종 추가는 사전 항목 하나라 비용이 낮다 |
| **B8** | 자소서 코치 결과를 **저장·재열람** (지금은 새로고침하면 사라진다) | 공고별로 모아 보면 지원 관리 기능이 된다 |
| **B9** | **뉴스 키워드 × 역량 카드 교차** — 기사 키워드가 `ARCHETYPES` 와 겹치면 "이 회사가 실제로 이 역량을 말하고 있다"는 더 강한 근거가 된다 | 두 모듈이 이미 있어 연결만 하면 된다 |
| **B10** | 뉴스 카드의 회사명을 **내 스펙의 `company` 에서 자동 입력** | 지금은 매번 직접 타이핑 |

### C. 정리성

- 머지 완료 브랜치 삭제: `chore/spa-only-entrypoint`, `feature/ncs-career-roadmap`, `feature/spa-server-auth` (+ `feature/sqlite-database` 확인 필요)
- ~~`.env.example` 이 존재하지 않는 `scripts/fetch-work24-companies.js` 를 가리킨다~~ → ✅ 해결 (그 파일을 실제로 만들었다)

### C-1. 개발 중 함정 (실제로 겪음)

**새 라우트·새 파일을 추가하면 서버를 반드시 재시작해야 한다.** Node 는 파일이 바뀌어도 다시 읽지 않는다. 실행 중이던 옛 프로세스가 그대로 떠 있어서 `/api/jd/coach`·`/api/news/company` 가 **"요청한 경로를 찾을 수 없습니다"(404)** 로 응답했고, 코드 문제로 오해하기 쉬웠다.
→ 개발 중에는 `npm start` 대신 **`npm run dev`(nodemon)** 를 쓸 것. 브라우저도 캐시 때문에 옛 JS 를 들고 있을 수 있으니 **Ctrl+Shift+R**.

### D. 미결 판단

- **정부부처·행정기관 분류** — `공정거래위원회`·`문화체육관광부`·`경기도남부경찰청` 등은 구멍이 **아니다** (공공기관운영법상 공공기관이 아니라 명단에 없는 게 맞다). 다만 4분류에 담을 칸이 없고 alumni `category` 에 `국가고시/공직진출` 이 9건 있어, **공직 분류를 따로 둘지 미결.**
- **스펙 입력 폼의 NCS 이전** — `spec-form.js` 가 아직 학과 기반. NCS 분류로 옮기려면 기존 스펙 레코드 마이그레이션이 필요하다.

---

## 8. AI 페르소나 검증 — "아이디어 검증 위원회"

아래는 `AI프롬프트 엔지니어링 2일차 / 차시4 / persona_ex1.txt` 의 **6인 전문가 페르소나 프롬프트**에 이 서비스의 아이디어를 입력해 실행한 결과다.

> 인용문은 **실행 결과 그대로**라 당시 이름인 'Careerly' 가 남아 있다. 결과를 나중에 손대면 기록이 아니게 되므로 고치지 않았다.

**입력 프롬프트 요지** — 6명(기술전문가 Dr. Tech / 마케팅전문가 Ms. Market / 재정전문가 Mr. Finance / 긍정적 전문가 Ms. Optimist / 부정적 전문가 Mr. Critic / CEO Chairman)이 순서대로 1개씩 의견을 내고, CEO가 **긍정 / 중립 / 부정** 중 하나로 판정한다. 중립이면 쟁점을 지목해 다음 Round 개시, 최대 3 Round, 3 Round 후에도 중립이면 **조건부 판단을 반드시** 내린다.

**검증 대상 아이디어** — "대학생이 자기 스펙을 입력하면, 선배 합격자 데이터를 기준으로 CAS 1000점으로 채점하고 목표 직무 평균과 비교해 부족한 항목을 알려주는 플랫폼. 정성 스펙 입력은 로컬 LLM 이 반정형 텍스트를 파싱해 자동 완성한다."

---

### Round 1

**[기술전문가]**: 기술적 실현 가능성은 이미 **입증 단계를 지났다**. 채점 엔진(`cas.js`)은 외부 의존이 없는 순수 계산이고 테스트 47개가 통과한다. 외부 데이터 연동도 4종 중 3종(NCS 24분류, 공공기관 355건 + 지방공기업 1,312건, 공정위 3,539건)이 실제 캐시까지 완료됐다. 다만 **AI 파싱 레이어는 기술적 실현 가능성이 낮은 쪽**이다: 로컬 8B(qwen3:8b)는 이 하드웨어(RAM 28GB, 780M iGPU = CPU 추론)에서 요청당 1분이 걸리고, 실측에서 줄글 입력 시 활동 3개 중 1개만 추출했다. LLM을 **분류·추출에만** 쓰고 점수 계산은 결정론 코드로 뺀 현재 설계(`rescore()`)가 정확히 옳은 방향이며, 이 경계선을 넘기지 않는 한 구현 난이도는 관리 가능하다.

**[마케팅전문가]**: 시장에는 잡코리아·캐치·링커리어 등 "합격 스펙 공개" 서비스가 이미 있고, 이들은 **표본 규모에서 압도적**이다. Careerly의 차별점은 두 가지로 좁혀진다 — (1) 스펙을 **나열**하는 게 아니라 하나의 **CAS 1000점으로 환산**해 비교 가능한 척도를 만든 점, (2) 분류 축을 임의 카테고리가 아닌 **NCS 공식 24개 분류**에 맞춰 학교·정부 사업과 접점을 만든 점. 특히 (2)는 B2C 경쟁이 아니라 **대학 취업지원팀 B2B/B2G 판로**를 여는 우위다. 반대로 B2C 정면승부는 표본 열세로 승산이 낮다고 본다.

**[재정전문가]**: 비용 구조가 **AI 프로바이더 선택에 전적으로 좌우**된다. 정량 계산·집계·분류는 정적 JSON 캐시 조회라 사실상 0원이고, 서버는 소규모 VPS(월 1~2만원)로 충분하다. 문제는 AI 분석 경로다 — Ollama 자체 호스팅은 8B에 6~7GB RAM이 필요해 **무료티어가 불가능**하고 8GB VPS 월 2~4만원인데, 공유 vCPU에서는 추론 3~5분으로 프록시 타임아웃(60~120초)에 걸려 **돈을 쓰고도 동작하지 않는다.** Groq 무료 티어로 가면 인프라 증분비용이 0에 수렴한다. 즉 **월 2~4만원을 더 쓰는 선택지가 오히려 더 나쁜 결과를 낸다.** ROI 관점에서 Groq 경로가 유일한 합리적 선택이며, 현재 미결인 배포 대상 결정이 곧 비용 결정이다.

**[긍정적 전문가]**: 가장 큰 강점은 **데이터가 이미 있다**는 점이다. `db.json` 에 userSpecs 1,188건이 들어 있어 백분위·벤치마크가 "언젠가 사용자가 모이면"이 아니라 **지금 당장 의미 있는 숫자를 낸다.** 설계 곳곳에 통계적 정직성이 박혀 있는 것도 기회 요인이다 — 표본 5명 미만이면 백분위를 숨기고, 비교 막대 폭과 점수가 같은 함수(`relativeScore`)를 쓰고, AI 총점을 결정론 값으로 교체했다. 이건 "숫자를 믿을 수 있는 취업 서비스"라는 포지션으로 직결되며, 경쟁사가 표본 크기로는 이겨도 **신뢰성 서술로는 따라오기 어려운** 자산이다. NCS 축 정렬은 대학·공공사업 확장 여지까지 열어둔다.

**[부정적 전문가]**: 리스크는 **점수의 정당성**이다. 활동 유형 기본배점(인턴십 100 · 공모전 78 · 봉사 30)은 데이터로 도출한 게 아니라 **사용자가 지시한 서열**이다. 학생에게 "당신은 367점"이라고 단언하는 서비스에서 배점 근거를 요구받으면 답할 근거가 없고, 이건 취업이라는 **고이해관계 영역**이라 오판의 대가를 사용자가 치른다. 두 번째로, 6-3에 정리된 실패 양상 — 자동분류 실패가 조용히 '중소기업'으로 채워지고, GAP은 누구에게나 정보처리기사·SQLD·AWS SAA를 띄운다 — 은 단순 미완성이 아니라 **잘못된 정보를 확신 있게 제시하는** 형태의 결함이다. 에러가 나지 않아 발견되지 않고, 학생이 이 목업을 보고 실제로 자격증 준비 순서를 정하면 직접적 피해가 된다. 세 번째로 `db.json` 1,188건이 데모 시드인지 실데이터인지에 따라 위 '강점'이 통째로 뒤집힌다.

### CEO 종합 판단

- **판정: 중립**
- **근거**: 기술 실현성(Dr. Tech)과 데이터 준비도(Ms. Optimist)는 검증됐고, 재정 리스크도 "Groq 선택 시 0원 수렴"으로 답이 나와 있다. 그러나 Mr. Critic이 지적한 **점수 정당성**과 **목업이 확신 있게 틀린 값을 내보내는 문제**는 서비스의 핵심 가치(신뢰할 수 있는 숫자)를 직접 부정하는 사안이라 무게를 여기에 둔다. Ms. Optimist의 "통계적 정직성" 논거와 Mr. Critic의 "배점 근거 없음"이 상충하는데, **CEO는 Critic 쪽에 무게를 둔다** — 표본 처리는 정직해도 배점 자체가 임의라면 정직성이 상쇄된다.
- **다음 Round 쟁점**: (1) `db.json` 의 userSpecs 1,188건은 실제 사용자 데이터인가 데모 시드인가. 벤치마크 신뢰성의 전제다. (2) 임의 배점 서열을 정당화할 수 있는가 — 데이터 기반 보정이 가능한지, 아니면 "참고용" 프레이밍으로 리스크를 관리해야 하는지.

---

### Round 2

**[기술전문가]**: 쟁점 (1)은 코드로 답이 나온다 — `backend/src/demo-seed.js` 와 `POST /api/admin/seed-random` 이 존재하고, 랜덤 시드가 구조화 활동까지 생성한다. **1,188건은 실사용 데이터가 아니라 생성된 시드로 보는 것이 타당하다.** 다만 이건 기술적으로 복구 가능한 문제다: 스펙 레코드에 출처 플래그(`seeded: true`)를 넣고 집계에서 분리하면 실데이터가 쌓이는 대로 자연스럽게 전환된다. 쟁점 (2)의 데이터 기반 보정은 **현재 기술 스택으로 구현 가능**하다 — 합격자 스펙과 결과 라벨이 쌓이면 유형별 기여도를 회귀로 추정해 `ACTIVITY_TYPES` 를 갱신할 수 있고, 상수가 `cas.js` 한 곳에 모여 있어 교체 지점이 이미 하나다. 단, **지금은 그 라벨 데이터가 없다.**

**[마케팅전문가]**: 배점 임의성은 시장에서 **치명적 결함이 아니라 프레이밍 문제**다. 경쟁 서비스도 "합격자 평균"이라는 통계를 제시할 뿐 인과를 입증하지 않으며, 사용자가 실제로 원하는 건 정밀한 절대점수가 아니라 **"내가 어디쯤인지"** 다. 다만 B2B/B2G 판로에서는 얘기가 달라진다 — 대학 취업지원팀에 납품하려면 배점 근거 문서를 요구받을 가능성이 높다. 따라서 B2C는 '참고 지표' 프레이밍으로 즉시 가능, **B2B 확장 전에는 배점 근거 정리가 선행 요건**이라는 단계 구분을 권고한다. 시드 데이터 문제는 더 심각하다 — "선배 1,188명 데이터"를 마케팅 문구로 쓰는 순간 허위표시가 된다.

**[재정전문가]**: 두 쟁점 모두 **추가 비용이 거의 들지 않는다**는 점이 중요하다. 시드/실데이터 분리는 플래그 추가와 집계 필터 수준이고, 배점 프레이밍은 UI 문구다. 반대로 "데이터 기반 배점 보정"을 제대로 하려면 합격 결과 라벨 수집이 필요해 **사용자 확보가 선행 투자**가 되고, 이건 돈이 아니라 시간 비용이다. ROI 계산상 지금 단계에서 회귀 보정에 투자하는 것은 조기 최적화이며, **B1~B3(각 10분~1시간 규모)에 자원을 쓰는 편이 기대효과가 압도적으로 높다.**

**[긍정적 전문가]**: Round 1에서 제가 든 근거 중 "userSpecs 1,188건"은 철회한다. 다만 나머지 강점은 유지된다 — 오히려 시드 데이터라는 사실이 **기능이 데이터 없이도 완성돼 있다**는 뜻이므로, 실사용자 1명이 들어온 시점부터 즉시 작동한다. 그리고 이 프로젝트가 보여준 자기교정 이력 자체가 기회 요인이다: AI 총점이 흔들리자 결정론 값으로 교체했고, 하드코딩된 `87/100` 히어로를 실제 엔진에 배선했고, 3명 중 2등을 '상위 33%'로 표시하는 착시를 스스로 막았다. **틀린 숫자를 발견하면 걷어내는 습관이 이미 코드에 있다** — 남은 목업(GAP, 자동분류 실패 안내)도 같은 방식으로 정리될 것으로 본다.

**[부정적 전문가]**: 시드 데이터임이 확인된 것은 제 지적이 맞았다는 뜻이지만, **더 큰 리스크는 이걸 모르고 쓰는 경우**다. 화면에 "선배 24명"처럼 하드코딩된 숫자가 남아 있고 백분위가 시드 기반으로 계산되면, 학생은 자기 순위를 실제 경쟁자 대비 순위로 오해한다. 배점 임의성에 대해서는 Ms. Market의 '참고 지표' 프레이밍에 동의하지만 **조건을 붙인다**: 프레이밍은 화면에 실제로 표시되어야 하고, 현재 CAS 히어로는 점수를 등급·백분위와 함께 **단정적으로** 보여준다. 문구 하나 추가로 끝나는 일이지만 안 되어 있으면 프레이밍이 없는 것과 같다. 마지막으로 배포 대상 미결(A1)은 관리 리스크다 — 결정을 미루는 동안 groq/ollama 두 경로를 계속 유지해야 하고, 이는 테스트되지 않는 코드 경로를 하나 안고 가는 것이다.

### CEO 종합 판단

- **판정: 긍정 (조건 명시)**
- **근거**: Round 2에서 두 쟁점 모두 해소됐다. (1) userSpecs 1,188건은 **데모 시드**로 확인 — Ms. Optimist가 해당 근거를 스스로 철회했고, Dr. Tech가 플래그 분리라는 저비용 해법을 제시했다. (2) 배점 임의성은 Ms. Market·Mr. Finance·Mr. Critic이 **'참고 지표' 프레이밍으로 관리 가능**하다는 데 수렴했고, 회귀 보정은 라벨 데이터가 없는 현 단계에서 조기 최적화라는 Mr. Finance 판단을 채택한다.
  Ms. Optimist와 Mr. Critic이 남은 목업 처리 전망에서 상충하는데, **CEO는 Critic 쪽에 무게를 둔다** — 자기교정 이력이 있다는 것은 앞으로도 그럴 것이라는 보장이 아니고, 현재 화면에 틀린 값이 떠 있다는 사실은 그대로다. 다만 이는 아이디어의 결함이 아니라 **미완 작업**이므로 판정을 부정으로 돌릴 사유는 아니다.
- **최종 결정문**:

  **Careerly 아이디어는 진행 권고(긍정)한다.** 핵심 엔진이 검증됐고(테스트 47/47), 외부 데이터 연동 4종 중 3종이 완료됐으며, 데이터 없이도 기능이 완성돼 있어 실사용자 유입 시점부터 즉시 작동한다. 차별점은 CAS 단일 척도 환산과 NCS 공식 분류 정렬이며, 후자는 B2C 표본 열세를 우회하는 **대학·공공 판로**를 연다.

  단, 다음 3건을 **공개(외부 사용자 노출) 전 선행 조건**으로 지정한다.

  1. **틀린 값을 확신 있게 보여주는 곳을 전부 제거** — 기업분류 자동판정 실패 안내(B1), GAP 하드코딩 배선(B4), 잔여 하드코딩("선배 24명" 등, B5). 취업이라는 고이해관계 영역에서 이건 미완성이 아니라 결함이다.
  2. **시드 데이터와 실데이터 분리 표시** — 집계 표본이 데모 시드일 때 백분위·"선배 n명"을 실데이터로 오인하게 두어서는 안 된다. Dr. Tech 제안대로 스펙 레코드에 출처 플래그를 두고 집계에서 분리한다.
  3. **CAS 점수를 '참고 지표'로 프레이밍하는 문구를 화면에 실제로 표시** — 배점 서열이 데이터 도출이 아니라 설계 판단임을 밝힌다. Mr. Critic 지적대로 코드 밖에만 있는 프레이밍은 프레이밍이 아니다.

  **권고사항**: 자원 배분은 B1~B3(저비용·고효과)을 최우선으로 하고, 배점 회귀 보정은 합격 결과 라벨이 쌓인 뒤로 미룬다. **AI 프로바이더는 배포 시 Groq 로 확정할 것을 권고한다** — Mr. Finance 분석대로 Ollama 자체 호스팅은 월 2~4만원을 더 쓰고도 프록시 타임아웃으로 동작하지 않는다. 미결 상태인 배포 대상 결정(A1)은 Mr. Critic이 지적한 이중 경로 유지 부담과 직결되므로 **조속히 확정하라.** 중견기업 분류(A2)는 드롭다운 수동 입력으로 대체 가능한 편의 기능이므로 공개 차단 사유가 아니다.

---

## 9. 자소서 코치 — 설계 판단 기록 (2026-07-27)

기능 자체는 2-5 에 있다. 여기는 **왜 이 형태로 만들었고 무엇을 버렸는지**의 기록이다. 되돌리려는 사람이 같은 함정에 다시 빠지지 않게 남긴다.

### 9-1. 검토한 3가지 안

| 안 | 내용 | 판정 |
|---|---|---|
| **A. JD 붙여넣기 + 역량 카드** | 공고를 붙여넣으면 역량을 추출하고, 역량별로 작성 가이드 카드를 보여준다 | ✅ **채택** — 데이터 구축이 필요 없고 모든 직무를 커버하며, AI 를 잘하는 일(추출)에만 쓴다 |
| **B. 역량 사전 큐레이션 (AI 없음)** | NCS 소분류별 역량 사전을 미리 만들어, 직무를 고르면 가이드를 보여준다 | ❌ 즉시·무료·정확하지만 **NCS 소분류가 수백 개**라 커버리지가 노가다다. 게다가 실제 공고의 개별 요건(우대사항)을 반영할 수 없다 |
| **C. 내 스펙 ↔ JD 갭 리포트** | 공고와 내 활동을 대조해 "이 역량은 근거 있음 / 없음"만 보여준다 | ❌ 단독으로는 **스펙 입력이 선행 필수**라 비로그인·미입력 사용자에게 빈 화면이 된다 |

**결론 — A 를 골격으로 하고 B·C 를 흡수했다.** B 의 사전은 "직무별"이 아니라 **"역량 원형 14종"** 으로 축소해 조립 재료로 쓰고(커버리지 문제 소멸), C 의 갭 리포트는 **있으면 붙는 보강**으로 넣었다(`mine` / `gap` 필드). 세 안의 장점만 남고 각각의 치명적 약점은 사라진다.

### 9-2. 버린 것 — "AI 가 자소서 문장을 써 준다"

요청은 "문장 형태로 적어주는 AI 프롬프트"였고, 실제로 만든 것은 **작성 지침을 문장으로 주는 기능**이다. 완성된 자소서 문장을 생성하지 않기로 한 이유:

1. **검증 수단이 없다.** 지금까지 AI 를 쓴 곳(CAS 스펙 분석)은 전부 짧은 JSON 추출이라 서버에서 enum 교정(`coerceActivity`)·재채점(`rescore`)으로 틀린 것을 걸러냈다. 산문에는 그런 안전망을 만들 수 없다.
2. **8B 의 실측 성능이 그것을 못 한다.** 6-1·6-2 참조 — 줄글 파싱조차 활동 3개 중 1개만 잡았다. 생성 품질은 분류보다 낮다.
3. **속도.** CPU 추론에서 출력 토큰이 곧 대기시간이다. 분류는 `num_predict 512` 로 끝나지만 역량 6개 × 문장은 그 열 배다. 실측으로 AI 보강 한 번에 이미 **44초**가 걸린다.
4. **대필 리스크.** 완성 문장을 주면 학생이 그대로 낸다. 유사도 검사·AI 검출에 걸리는 것은 학생이 감당한다. 그래서 화면에 대필이 아님을 명시한다(`disclaimer`).

되살리고 싶다면 **배포를 Groq(70B)로 확정한 뒤**에 논의해야 한다. 로컬 8B 로는 안 된다.

### 9-3. 뉴스 트렌드 → 자소서 키워드 (제안 검토)

**요청받은 데이터셋은 이 용도로 쓸 수 없다.** `data.go.kr/data/15157639` 를 확인한 결과:

> **"전북개발공사_키워드 기반 뉴스 자동 알림 시스템 소스코드_20260317"** — 제공기관 전북개발공사, **DOCX 파일 1건**, 1회성.

뉴스 데이터가 아니라 **남이 만든 프로그램의 소스코드 문서**다. 기사도 키워드도 들어 있지 않아 분석 대상이 없다. (오픈API 없음, 파일 다운로드만 — 지방공기업 CSV 와 같은 `fileData` 유형이다 → 3-2 참조)

**아이디어 자체에 대한 판단** — 방향은 맞지만 **연결 지점을 정확히 잡아야 값이 있다.**

- ❌ **뉴스 → 역량 키워드**: 연결이 약하다. 자소서의 역량 키워드는 뉴스가 아니라 **공고(JD)에서 나온다**. 뉴스에서 "협업"·"문제해결"을 뽑아봐도 지금 규칙 추출이 하는 일과 겹치고 정확도만 떨어진다.
- ✅ **뉴스 → 지원동기·산업이해 문단 소재**: 여기가 진짜 빈칸이다. 자소서에서 학생들이 가장 못 쓰는 문항이 "왜 우리 회사인가"이고, 그건 역량 사전으로는 채울 수 없다(회사·산업마다 달라서). 회사명·산업 키워드로 최근 기사를 모아 **"이 회사가 지금 무엇을 하고 있는지"** 를 보여주면, 지원동기 문단의 근거가 된다.

**데이터 소스는 다시 골라야 한다** (위 데이터셋은 탈락):

| 후보 | 평가 |
|---|---|
| **빅카인즈(BIGKINDS) 오픈API** | 언론사 기사 원문·키워드 분석까지 제공. 이 용도에 가장 정확하지만 **API 신청·승인 필요** |
| **네이버 검색 API (뉴스)** | 무료·즉시 발급, 일 25,000건. 제목/요약만 오지만 지원동기 소재로는 충분 |
| **DuckDuckGo HTML 스크래핑** | **이미 이 프로젝트에 있다** (`casAnalyze.js` 의 `searchSnippets` — 활동 기간 추정용). 키 없이 되므로 시제품에는 이게 가장 빠르다 |

**권고**: 시제품은 이미 있는 `searchSnippets` 를 재사용해 **"지원동기 소재" 카드**를 자소서 코치 페이지에 덧붙이는 형태로 만들고(회사명 입력 → 최근 기사 3~5건 → "이 중 하나를 지원동기의 근거로 삼으세요"), 실제로 쓸모가 확인되면 네이버 API 로 승격한다. **아직 만들지 않았다** — 이 문서를 읽고 진행할 때 B7·B8 보다 뒤에 둘지 결정하면 된다.

---

## 10. 직무 트렌드 (채용공고 집계) — 2026-07-27 신규, **인증키 승인 대기**

자소서 코치의 역량 카드에 **"이 직무 공고 200건 중 65%가 요구"** 배지를 붙이는 기능. 코드는 완성됐고, **워크넷 인증키 승인만 되면 스크립트 2개를 돌려 바로 동작**한다.

### 10-1. 왜 뉴스가 아니라 채용공고인가

"이 직무가 무엇을 요구하나"의 답은 뉴스가 아니라 공고에 있다. 결정적으로 **공고 한 장을 읽는 코드(`jd-competency.js`)가 이미 있어서**, 수천 장에 돌려 세기만 하면 된다 — 새 파서가 필요 없다.

그리고 이게 **"일반 LLM과 뭐가 다르냐"에 대한 답**이다. ChatGPT는 "요즘 데이터 분석이 중요합니다"까지만 말할 수 있고, **"이 직무 공고 512건 중 68%"** 는 말할 수 없다.

### 10-2. 확인된 API 규격

| | |
|---|---|
| 요청 URL | `https://www.work24.go.kr/cm/openApi/call/wk/callOpenApiSvcInfo210L01.do` (신 게이트웨이) |
| ~~구 주소~~ | ~~`openapi.work.go.kr/opi/opi/opia/wantedApi.do`~~ — 2026년 발급 키를 모른다. 쓸 수 없다 |
| 필수 파라미터 | `authKey`, `callTp=L`(목록), `returnType=XML`, `startPage`, `display` |
| 선택 | `keyword`, `occupation`, `region`, `education`, `career`, `empTp`, `coTp` … |
| 응답 | `<wantedRoot>` > `total`/`startPage`/`display` > `<wanted>` 반복 |
| `<wanted>` 필드 | `wantedAuthNo`, `company`, `title`, `salTpNm`, `sal`, `region`, `holidayTpNm`, `minEdubg`, `maxEdubg`, `career`, `regDt`, `closeDt`, `wantedInfoUrl` |
| 신청 | [data.go.kr 3038225](https://www.data.go.kr/data/3038225/openapi.do) 또는 openapi.work.go.kr — **활용신청 필수** |

### 10-3. 만든 것

```bash
node scripts/fetch-worknet-jobs.js          # 공고 수집 → data/worknet-jobs.json
node scripts/build-job-trends.js            # 역량 빈도 집계 → data/job-trends.json
```

- **`scripts/fetch-worknet-jobs.js`** — 수집. **직종코드(KECO) 대신 NCS 중분류 이름을 키워드로 훑는다.** 매핑표를 만들 필요가 없고, 수집 단위와 C:road 화면 단위가 처음부터 일치한다. `--keyword=` / `--limit=` / `--probe` / `--probe-detail=` 옵션.
  - **응답 스키마를 박아두지 않았다.** 210L01 응답을 아직 못 봤기 때문이다(권한 차단). 반복되는 항목 태그를 **자동 인식**하고 제목·회사명도 후보 목록(`TITLE_FIELDS`/`COMPANY_FIELDS`)에서 고른다. 권한이 열리면 **`--probe` 부터 돌려 실제 응답을 눈으로 확인**할 것.
- **`src/job-trends.js`** — 집계·조회. 같은 `ruleExtract` 를 쓰므로 **화면 카드와 집계 숫자가 어긋날 수 없다**.
- **`scripts/build-job-trends.js`** — 집계만 재실행(네트워크 안 탐). 추출 규칙은 앞으로 자주 손볼 부분이라 수집과 분리했다.
- **`test/job-trends.test.js`** — 12 케이스. 캐시 파일 없이 함수에 직접 넣어 검증한다.

### 10-4. 지킨 원칙 (되돌리지 말 것)

1. **`data/` 에 가짜 데이터를 만들지 않았다.** 검증은 임시 캐시로 하고 지웠다. 시드와 실데이터가 섞이면 "선배 1,188명"이 시드였던 일이 반복된다(8장 CEO 조건 2번).
2. **표본 30건 미만이면 비율을 숨긴다**(`MIN_SAMPLE`). 12건 중 8건을 '67%'로 보여주면 실제보다 단단한 숫자로 읽힌다 — CAS 백분위 5명 미만 숨김과 같은 원칙.
3. **직무를 못 고르면 배지를 안 보여준다.** 엉뚱한 직무의 비율보다 없는 게 낫다.
4. **제목 기준이라는 한계를 화면에 적는다.** 목록 API 응답에 자격요건 본문이 없어 **채용제목만으로 집계**한다 → 실제보다 낮게 나온다. 화면에 이 설명이 함께 나간다.
5. **상세 API 필드를 추정해서 박지 않았다.** 공정위 API 때 명세를 추정했다가 통째로 틀렸다(3-1). `--probe-detail=<구인인증번호>` 로 실제 응답을 덤프해 눈으로 확인한 뒤 `TEXT_FIELDS` 에 추가할 것.

### 10-5. 직무 매칭 — 별칭이 필요했던 이유

버킷 이름(NCS 중분류, 예: `기획사무`)으로 공고를 매칭하면 **거의 항상 실패한다.** 마케터 공고에 "기획사무"라고 적혀 있을 리 없기 때문이다. 그래서 **소분류 이름까지 별칭**으로 쓴다(`마케팅`·`홍보·광고`는 공고에 실제로 등장한다). 여러 개가 걸리면 더 구체적인(긴) 이름이 이긴다.

### 10-6. 남은 일

### 10-7. 인증키 진단 결과 (2026-07-27 실측) — **키가 이 서비스에 안 붙어 있다**

`.env` 의 `WORK24_API_KEY`(36자)는 들어 있지만 채용정보 API 가 거부한다. 실제 응답으로 원인을 좁혔다:

| 시도 | 응답 | 뜻 |
|---|---|---|
| `authKey=<키>` (http/https 둘 다) | **"유효하지 않은 인증키 입니다"** | 키는 전달됐는데 **이 서비스에 붙어 있지 않다** |
| `authkey` / `auth_key` | "인증키값이 없습니다" | 파라미터 이름은 **`authKey`** 가 맞다 (대소문자 구분) |
| 신 게이트웨이 `work24.go.kr/cm/openApi/call/wk/...` | **"개인회원은 사용할 수 없는 OPEN-API입니다"** | 새 고용24 게이트웨이는 **기업·기관 회원**만 |

**원인 확정 (2차 진단)** — 포털 확인 결과 인증키는 **채용정보 서비스로 2026-07-17 승인**된 UUID 가 맞았다. 그런데도 막힌다. 신 게이트웨이 오퍼레이션을 훑어 원인을 특정했다:

| 오퍼레이션 | 응답 |
|---|---|
| `callOpenApiSvcInfo210L01` (채용정보 목록) | **"개인회원은 사용할 수 없는 OPEN-API입니다"** |
| `callOpenApiSvcInfo210D01` (채용정보 상세) | 같음 |
| `211L01` / `220L01` | "신청하신 OpenApi 서비스가 존재하지 않습니다" |

호스트(`www` / `m`)·`infoSvc` 파라미터를 바꿔도 메시지가 같다. 구 게이트웨이(openapi.work.go.kr)는 이 키를 아예 모른다("유효하지 않은 인증키") — **2026 발급 키는 신 고용24 게이트웨이 전용**이다.

> **결론: 키도 규격도 맞다. 막는 것은 계정의 회원유형이다.**
> 고용24 채용정보 OPEN-API 는 **기업·기관 회원**만 호출할 수 있고, 지금 계정은 개인회원이다.
> 신청이 '승인' 으로 뜨는 것은 접수가 승인된 것이고, 실제 호출은 회원유형에서 따로 걸린다.
> 코드를 고쳐서 풀 수 있는 문제가 아니다.

**해결 경로 3가지**

| | 방법 | 평가 |
|---|---|---|
| **A** | 고용24 **기업·기관 회원**으로 전환하거나 그 유형 계정(학교·사업단 명의 등)으로 재신청 | 정공법. 되면 지금 코드가 **그대로** 동작한다(`ai-provider` 처럼 게이트웨이만 신 주소로 바꾸면 됨) |
| **B** | [재정경제부_공공기관 채용정보 조회서비스](https://www.data.go.kr/data/15125273/openapi.do) (잡알리오, data.go.kr 15125273) 로 대체 | **이미 승인된 `DATA_GO_KR_SERVICE_KEY` 계열**이고 개발단계 **자동승인**(일 1,000건). 단 **공공기관 채용공고만** 담아 커버리지가 좁다 → 화면 문구를 "공공기관 채용공고 N건 중"으로 정직하게 바꿔야 한다. (엔드포인트 경로는 활용신청 후 Swagger 로 확정할 것 — 추정 경로로 호출하면 403 이 난다. 실측함) |
| **C** | 시장 배지 없이 간다 | 자소서 코치는 **트렌드 데이터가 없어도 완전히 동작하도록** 이미 설계돼 있다(`marketFor` 가 null 이면 배지만 빠짐) |

**신 게이트웨이 주소** (A 가 풀리면 `fetch-worknet-jobs.js` 의 `API` 만 교체):
`https://www.work24.go.kr/cm/openApi/call/wk/callOpenApiSvcInfo210L01.do?authKey=…&callTp=L&returnType=XML&startPage=1&display=100`

| # | 할 일 | 상태 |
|---|---|---|
| **10-A** | **워크넷 채용정보 서비스용 인증키 확보** | **사용자가 해야 함** (위 확인 순서) |
| 10-B | 승인 후 `--probe-detail` 로 상세 API 응답 확인 → 본문 기준 집계로 승격 | 코드 자리 준비됨(`TEXT_FIELDS`) |
| 10-C | 시장 빈도 + **선배 보유율** 나란히 배치 → "시장은 요구하는데 선배도 없다 = 확실한 차별점" | `aggregation.js` 에 보유율이 이미 있음. B4(GAP)와 계산이 같다 |
| 10-D | 뉴스 → "이 회사, 지금" 카드 (지원동기 문항용) | 3단계. 네이버 검색 API(무료·즉시) 또는 기존 `searchSnippets` 재사용 |

**뉴스와 채용정보는 섞지 않는다** — 하나의 "트렌드 점수"로 버무리면 단위도 의미도 다른 데이터라 가중치를 정당화할 수 없고, 결국 `aiTotal` 처럼 아무도 검증 못 하는 숫자가 된다. 채용정보는 **직무역량 문항**, 뉴스는 **지원동기 문항**으로 화면에서 나눠 붙인다.

---

## 11. 이 회사, 지금 — 뉴스 기반 지원동기 소재 (2026-07-28 신규)

자소서 코치 페이지 하단의 별도 카드. 회사명을 넣으면 **최근 기사 5건 + 기사에서 반복된 키워드 + 지원동기 작성 지침**을 준다. `GET /api/news/company?name=…` (`backend/src/news.js`, `routes/news.js`)

### 11-1. 왜 별도 카드인가 — 섞지 않는다

| | 근거 데이터 | 답하는 자소서 문항 |
|---|---|---|
| 역량 카드 (2-5) | 채용공고 | 직무역량 문항 |
| **이 회사, 지금** | 회사 뉴스 | **지원동기 문항** |

둘을 하나의 "트렌드 점수"로 버무리지 않는다. 단위도 의미도 다른 데이터라 가중치를 정당화할 수 없고, 결국 `aiTotal` 처럼 아무도 검증 못 하는 숫자가 된다. 화면에서 **문항별로 나눠 붙이고**, 각 숫자는 자기 출처를 달고 다닌다.

역량 사전으로는 "왜 우리 회사인가"를 채울 수 없다 — 회사마다 답이 다르기 때문이다. 그 빈칸을 메우는 기능이다.

### 11-2. AI 를 쓰지 않는다

기사를 **요약시키지 않는다.** 8B 는 요약에서 없는 사실을 지어내는데, 지어낸 사실을 자소서에 쓰면 면접에서 그대로 무너진다. 그래서:

- **기사 제목·링크를 그대로** 보여주고 원문을 읽게 한다
- 키워드는 **기사 원문에 실제로 있는 단어만** 센다
- "이렇게 쓰라"는 작성 지침만 코드가 조립한다 (`MOTIVE_GUIDE` — 5단계 골격 · 주의사항 · 감점 표현 · 면접 예상질문)

### 11-3. 키워드 추출 — 형태소 분석기 없이

기사 2건 이상에서 나온 말만 남긴다(1건짜리는 그 기사의 문장 부스러기일 뿐이다). 걸러내는 것들:

| 거르는 대상 | 이유 |
|---|---|
| 주소 조각 (`www`·`view`·`about`) | 웹 폴백이 제목 끝에 표시용 URL 을 붙여 보낸다 — 안 걸러내면 키워드를 통째로 망친다(실측) |
| 기사 주소의 영문 사명 (`Aju`·`autoever`) | 한글 회사명만 걸러내면 영문 표기가 살아남는다(실측) |
| 숫자로 시작하는 말 (`77만원`·`2분기`) | 그 기사의 수치일 뿐 회사의 화두가 아니다 |
| 서술어 (`밝혔다`·`낮췄다`) | 3글자 이상이면서 '다'로 끝나는 말 |
| 뉴스 관용어 (`보도자료`·`기자`·`목표가`) | `NEWS_STOP` 목록 |
| 실체참조 `&#x27;` | 안 풀면 `x27` 이 키워드로 뜬다(실측) |

**키워드마다 몇 번째 기사에서 나왔는지 남긴다.** 근거를 못 짚는 키워드는 학생이 검증할 수 없고, 검증 못 한 표현을 자소서에 쓰면 위험하다(공고 근거 문장을 그대로 인용하는 것과 같은 원칙).

실측 결과 (아주산업): `에스와이삼양 · 특수콘크리트 · 기술협력 · 파트너십 · MOU · 체결` — 지원동기에 바로 쓸 수 있는 신호가 잡힌다.

### 11-4. 프로바이더 — 발급처가 둘인데 **어느 쪽이든 된다** (2026-07-28 확정)

뉴스 검색 키는 두 콘솔에서 발급되고, **응답 형식이 같다.**

| id | 엔드포인트 / 헤더 | 비고 |
|---|---|---|
| `ncp` | `https://naverapihub.apigw.ntruss.com/search/v1/news`<br>`X-NCP-APIGW-API-KEY-ID` / `X-NCP-APIGW-API-KEY` | NCP 콘솔 → **NAVER API Hub → 검색**. Secret 40자. **현재 이 프로젝트가 쓰는 경로** |
| `naver` | `https://openapi.naver.com/v1/search/news.json`<br>`X-Naver-Client-Id` / `X-Naver-Client-Secret` | developers.naver.com. Secret 10자 안팎 |
| `web` | DuckDuckGo HTML | 키 없이 동작하는 폴백 |

> ⚠️ **한때 "NCP 에는 검색 API 가 없다"고 판단했는데 틀렸다.** NAVER API Hub 에 검색(뉴스·블로그·이미지 등)과 검색어 트렌드·쇼핑인사이트가 있다. 그 오판 때문에 사용자가 개발자센터에서 키를 다시 발급받는 헛수고를 했다. **문서(api.ncloud-docs.com/docs/naver-api-hub-overview)를 먼저 확인할 것.**

`src/news.js` 는 **키 하나로 양쪽을 차례로 시도하고 통한 쪽을 기억한다**(Ollama 모델 자동 선택과 같은 방식). `NEWS_PROVIDER` 로 고정할 수 있지만, 지정값이 키와 안 맞으면 나머지도 시도하고 경고를 남긴다 — 실제로 `.env` 에 `NEWS_PROVIDER=naver` 를 적어둔 채 NCP 키를 넣어 전부 실패한 적이 있다.

**점검**: `node scripts/check-news-api.js` — 양쪽에 실제로 던져보고 어느 콘솔의 키인지 알려준다.

### 11-4-1. 검색 품질에서 배운 것 (실측)

| 문제 | 원인 | 조치 |
|---|---|---|
| `sort=date` 로 뽑으니 '현대오토에버' 1위가 **LG이노텍 MSCI 편입** 기사 | date 는 '최신 언급순'이라 스치듯 언급된 기사가 앞에 온다 | **`sort=sim`(관련도순)** 으로 변경. 지원동기에 필요한 건 '이 회사를 다룬 기사'다 |
| '아주산업' 검색 결과가 **우리銀·산업부** 기사 (total 39만 건) | 네이버가 검색어를 '아주'+'산업' 으로 쪼갠다. 기사가 적은 회사일수록 심하다 | `relevant()` 가 전부 걸러내 0건 → **웹 검색으로 자동 재시도**(`web-fallback`). 실제로 '특수콘크리트 기술협력' 기사를 찾아낸다 |

중견·중소기업 지원자에게는 웹 폴백이 사실상 유일한 소재원이라 그냥 비워두지 않는다. 대신 화면 안내 문구가 경로별로 달라진다(`web-fallback` 이면 "네이버에서 못 찾아 웹 검색으로 대신했다"고 밝힌다).

**실측 결과**:
- 현대오토에버 `[ncp]` → `GPU · 기아 · 서버 · 현대자동차` (3,044억 GPU 서버 공급 기사)
- 삼성전자 `[ncp]` → `SK하이닉스 · 폴더블폰 · 반도체 · 갤럭시`
- 아주산업 `[web-fallback]` → `에스와이삼양 · 특수콘크리트 · 기술협력 · MOU`

### 11-5. 남은 일

- 뉴스 키워드를 **역량 카드와 교차**시키기 — 기사 키워드가 `ARCHETYPES` 의 역량과 겹치면 "이 회사가 실제로 이 역량을 말하고 있다"는 더 강한 근거가 된다
- 회사명을 **스펙의 `company` 에서 자동으로** 채워 넣기 (지금은 직접 입력)
- 언론사·날짜 필터 (네이버 키가 들어온 뒤에 의미가 있다)

---

## 12. 다음 세션 빠른 시작

```bash
cd /c/Users/admin/Downloads/careerly
git branch --show-current                      # main (2026-07-30 이후)
for f in test/*.test.js; do node "$f" | tail -1; done   # 합 227 통과 확인
docker start careerly-mysql                    # DB 가 떠 있어야 서버가 뜬다
cd backend && node scripts/check-ai.js         # Groq 키·모델 동작 확인
npm run dev                                    # http://localhost:3000 (nodemon)
```

> **DB 가 없으면 서버는 일부러 죽는다.** 살아있는 척하면 원인을 못 찾기 때문이다.
> 부팅에 성공하면 `[mysql] 연결됨` 과 `[기능] ...` 두 줄이 뜬다 — 후자로 어떤
> 선택 기능이 켜졌는지 바로 알 수 있다.

> 자소서 코치는 `http://localhost:3000/#jd`. AI 없이도 동작하므로 키가 없어도
> 'AI 보강 사용' 체크를 끄면 즉시 결과가 나온다.
>
> **테스트는 반드시 저장소 루트에서 돌린다.** `backend/` 안에서 `node test/...` 를 치면
> 경로가 어긋나 `Cannot find module` 이 난다 (옛 `out.json`·`out3.txt` 가 바로 그 에러 덤프였다).

1. 이 문서 **13장(1차 배포)** 과 **14장(다음에 할 일)** 을 먼저 읽는다 — 7장보다 최신이다.
2. 6장(부족한 점)은 여전히 유효하다. 12-1 · 13-3 의 함정 표는 성격이 같으니 같이 본다.
3. 코드 작업은 **B16(멘토 '받은 요청' 서버 연동)** 부터다. 배포된 상태에서 티가 나는
   유일한 미완성 지점이다 — 멘티 신청은 DB 에 들어가는데 멘토 화면이 못 읽는다.
4. 메모는 이 문서를 갱신하는 방식으로 남긴다 (별도 메모리 저장 없음).

### 12-1. 이 프로젝트가 반복해서 밟은 함정 (성격이 같다)

세 번 다 **"에러가 안 나는데 값이 틀린"** 형태였고, 세 번 다 **모델이나 외부 API 를 의심하다 시간을 버렸다.**

| 겉보기 | 실제 원인 |
|---|---|
| 공정위 API 키가 잘못됐나? | 코드가 통째로 틀린 규격이었다 (3-1) |
| 8B 가 줄글을 못 읽나? | 규칙 파서가 가로채고 AI 를 호출조차 안 했다 (6-2) |
| Groq 로 바꾸면 정확해지나? | 프로바이더와 무관한 파서 문제였다 (6-2) |

**How to apply** — "AI/외부 API 가 이상하다" 싶으면 **그 계층을 건너뛰고 직접 호출해 본 뒤 앱 결과와 비교한다.** 두 결과가 다르면 범인은 그 사이의 우리 코드다.

---

## 13. 1차 배포 — Railway + MySQL (2026-07-29 ~ 30) ✅ 완료

운영 주소: `https://careerly-production-5d6b.up.railway.app`

### 13-1. 구성

| | |
|---|---|
| 호스팅 | Railway — 앱 서비스 + MySQL 서비스 **한 프로젝트 안에** |
| Root Directory | **비워 둔다(저장소 루트)**. `backend` 로 두면 안 된다 — 13-3 참조 |
| Start Command | `npm start` (루트 `package.json` 이 workspaces 로 `backend` 를 물고 있다) |
| DB | MySQL 8, DB 이름은 Railway 기본값 `railway` |
| 스키마 투입 | 로컬에서 `MYSQL_PUBLIC_URL` 로 — Railway 컨테이너엔 셸이 없다 |

```powershell
# 스키마·카탈로그는 내 PC 에서 넣는다 (PowerShell)
$env:MYSQL_URL = "<MySQL 서비스의 MYSQL_PUBLIC_URL>"
cd backend
node scripts/load-schema.js                    # 테이블 13개
node scripts/migrate-to-mysql.js --catalog     # 자격증643·학과193·기업6,634·직업461
Remove-Item Env:MYSQL_URL                      # 안 지우면 로컬 작업이 운영 DB 를 건드린다
```

> `mysql` CLI 를 쓰지 않는다. MySQL 을 Docker 로만 돌려서 클라이언트가 PATH 에 없다.
> 그래서 `scripts/load-schema.js` 를 만들었다 — 어느 DB 에 넣는지 먼저 찍고 시작한다.

### 13-2. 배포 점검 (밖에서 확인할 수 있는 것)

```bash
D=https://careerly-production-5d6b.up.railway.app
curl -s $D/api/payments/config          # {"enabled":true,"clientKey":"test_ck_..."}
curl -s -D - -o /dev/null $D/api/auth/naver | grep -i location   # nid.naver.com 으로 가야 정상
curl -s "$D/api/news/company?name=삼성전자"                       # provider:"ncp", weekly 5건
curl -s "$D/api/certs/suggest?q=정보&limit=1"
curl -s $D/api/jobs | head -c 200                                # counts: 10/35/461
```

부팅 로그 한 줄로도 볼 수 있다(2026-07-30 추가):

```
[기능] 결제 켜짐 · 네이버로그인 켜짐 · 카카오로그인 켜짐 · AI 켜짐 · 뉴스 ncp
```

`뉴스 web` 이면 검색 키가 안 읽힌 것이다(웹 폴백). 웹 폴백은 **기사 날짜가 없어
주간 대표기사가 통째로 0건**이 되고 키워드에 사이트 이름 조각이 섞인다
(`뉴스룸`, `Newsroom`, `Korea`). 기사 자체는 나오므로 **'되는데 이상한' 모습**이다.

### 13-3. 배포에서 밟은 함정 (전부 같은 성격이다)

이 장은 다음에 배포할 사람을 위한 것이다. **다섯 개 중 넷이 "서버는 떴는데 값이 틀린"
형태였고, 로그가 원인을 말해 주지 않아서 시간을 버렸다.** 그래서 고칠 때마다
진단을 같이 넣었다.

| 증상 | 실제 원인 | 남긴 진단 |
|---|---|---|
| `Cannot find module '../../../frontend/js/cas.js'` | Root Directory 를 `backend` 로 둬서 `frontend/` 가 이미지에 안 들어감. **require 를 고쳐도 화면이 통째로 빈다** — `express.static(frontend/)` 도 같이 깨지기 때문 | 루트 `package.json`(workspaces) + 배포 문서 수정 |
| `[mysql] 연결 실패 — root@127.0.0.1:3306` | `${{MySQL.MYSQL_URL}}` 이 빈 문자열이 되어 **로컬 기본값으로 조용히 폴백**. 앱과 MySQL 이 **다른 프로젝트**에 있어서 참조가 안 풀렸다 | 실패 로그에 "접속 정보 출처" 를 찍는다 |
| `DB 에 연결하지 못해...` 한 줄만 뜨고 원인 없음 | 참조가 안 풀린 `${{...}}` 가 글자 그대로 값이 됨 → `new URL()` 이 `config()` 안에서 던짐 → `assertConnection` 의 try 바깥이라 연결 로그도 안 남고, `server.js` 의 `.catch(() => ...)` 가 **에러를 통째로 버렸다** | URL 파싱 실패를 잡아 사유를 말한다. `.catch` 가 메시지를 버리지 않는다 |
| 결제가 `enabled:true` 인데 결제창이 안 뜸 | Raw Editor 에서 `KEY=값` 줄을 통째로 **값 칸**에 붙여넣어 `TOSS_CLIENT_KEY=test_ck_...` 가 됨. 비어 있지 않으니 '켜짐'으로 보인다 | 부팅 때 `값이 자기 이름으로 시작하면` 경고 |
| 뉴스 키를 고쳐도 계속 웹 폴백 | **`Redeploy` 는 그 배포의 옛 커밋을 다시 올린다.** 코드를 고쳐 푸시해도 반영되지 않았다. NCP 키는 처음부터 맞았다 | 발급처별 상태코드·응답본문을 보여준다(401/403/404 구분) |

> **`Redeploy` 는 변수만 바꿀 때 쓴다. 코드를 바꿨으면 새 배포를 만든다**
> (푸시 이벤트 또는 Deploy latest commit). 오늘 가장 오래 잡아먹은 함정이다.

### 13-4. 네이버 키는 두 쌍이다 (제일 헷갈린 지점)

| 용도 | 변수 | 발급처 |
|---|---|---|
| 로그인 | `NAVER_LOGIN_CLIENT_ID` / `NAVER_LOGIN_CLIENT_SECRET` | 개발자센터 — 네이버 로그인 앱 |
| 뉴스 검색 | `NAVER_CLIENT_ID` / `NAVER_CLIENT_SECRET` | **NCP NAVER API Hub** 의 Application → API Key ID / API Key |

`LOGIN_` 이 붙고 안 붙고가 전부다. 서로 바꿔 넣으면 **둘 다 조용히 실패**한다.
NCP 는 계정 전체의 `Access Key ID / Secret Key`(마이페이지 → 인증키 관리)와도
헷갈리기 쉽다 — 필요한 것은 **Application 화면 안의 값**이다.

코드(`news.js`)는 NCP·개발자센터 **양쪽을 차례로 시도해서 통한 쪽을 기억**한다.
어디서 발급받았든 변수 이름은 `NAVER_CLIENT_ID`/`SECRET` 하나로 쓴다.

### 13-5. 같이 고친 화면 버그 5건 (배포 후 실사용에서 나옴)

| 증상 | 원인 |
|---|---|
| 가입 때 적은 닉네임이 사라짐 | `db.js` 의 `createUser` 가 `nickname` 을 **인자에서 흘려서** 서버로 안 보냈다. 화면·서버 양쪽 다 준비돼 있는데 중간에서만 빠져 **에러도 안 났다** |
| 희망 진출분야·세부직무가 빔 | 학과를 고르면 코드로 `select.value` 를 바꾸는데 **JS 로 값을 바꾸면 `change` 가 나지 않는다.** 거기 걸어 둔 `fillFieldJob` 이 영영 안 돌았다 |
| 자격증 첫 칸 자동완성이 뜨자마자 닫힘 | 배지 갱신에 `paintCerts()`(=`innerHTML` 통째 교체)를 불러 **방금 연 드롭다운과 포커스를 스스로 날렸다.** 첫 글자에서 배지가 반드시 바뀌므로 매번 걸렸다 |
| 저장 안내가 안 보임 | 저장 버튼은 폼 아래인데 안내는 페이지 맨 위 초록 상자였다 → 토스트로 통일 |
| '내가 신청'이 비어 있음 | 신청은 DB 로 가는데 **화면은 `localStorage` 만 봤다.** 기기를 바꾸면 아예 사라졌다. `GET /api/mentoring/requests` 를 붙이고 취소도 서버로 보낸다 |

**공통점**: 다섯 중 넷이 *에러 없이 값만 틀린* 부류다. 12-1 의 함정과 성격이 같다.

### 13-6. 배포 전 삭제한 것

`sqlite3` 경로를 걷어냈다 (`src/db.js`, `routes/careerData.js`, `database/init-db.js`).
MySQL 전환 뒤 아무도 안 쓰는데 부팅할 때 로드만 되고 있었고, **네이티브 모듈이라
빌드에서 컴파일이 필요**했다. 읽는 `alumni` 테이블은 어디에도 없고, `careerly.db` 는
저장소에 없고, 프론트도 테스트도 이 API 를 부르지 않았다.

---

## 14. 다음에 할 일 (2026-07-30 기준)

### A. 사용자 조치가 필요

| # | 항목 | 상세 |
|---|---|---|
| **A5** | **브라우저에서 결제 전 과정 확인** | `TOSS_SECRET_KEY` 는 **승인 단계**에서만 쓰인다. 밖에서는 검증할 수 없다 — 멘토 신청 → 결제창 → 테스트 카드 승인까지 한 번 통과시켜야 그 키가 맞는지 알 수 있다 |
| **A6** | **운영 전환 시 토스 라이브 키** | 지금은 `test_ck_`. 실제 결제를 받으려면 라이브 키로 바꾸고 **사업자 심사**가 필요하다 |
| **A7** | **DB 비밀번호 교체** | 셋업 중 대화에 노출됐다. `${{MySQL.MYSQL_URL}}` 참조를 쓰므로 앱 변수는 자동으로 따라간다 |
| A3 | 고용24 채용정보(210L01) 기업회원 계정 | 기존 미결 (10-7) |
| A8 | 커리어넷 학과정보 API 승인 대기 | 오면 `major-catalog.js` 의 MAJORS 를 `fetch-majors.js` 결과로 교체. **RULES·`deptOf` 는 그대로 둔다** |

### B. 코드 작업

| # | 작업 | 메모 |
|---|---|---|
| **B13** | **CAS 점수표 재설계** | 회의 메모의 숫자는 "(가중치 예시)" 라고 적혀 있었다. 메모 순서대로 **배포 후 데이터를 모은 뒤** 손대는 게 맞다 |
| **B14** | 희망직무 chip 다중선택 | 지금은 단일 select |
| **B15** | `job-trends` 를 NCS → KECO 로 이전 | 로드맵은 옮겼는데 트렌드만 NCS 분류로 남아 있다 |
| **B16** | 멘토 쪽 '받은 요청'도 서버 연동 | 13-5 에서 **멘티('내가 신청')만** 서버로 옮겼다. 멘토 화면은 아직 `localStorage` 다 — 같은 증상이 그대로 남아 있다 |
| B3 | 스펙 입력 placeholder | 기존 미결 |

> **B16 이 가장 급하다.** 멘티가 보낸 신청이 DB 에 있는데 멘토 화면은 로컬만 보므로,
> **실제로는 멘토가 신청을 영영 못 받는다.** 배포된 상태에서 티가 나는 문제다.

---

## 15. 팀 협업 체제 (2026-07-30~, 2차 배포부터)

2차 작업부터 **2~3명이 같이 고친다.** 1차까지는 혼자 `main` 에 바로 푸시했지만,
그대로 두면 **누구든 푸시하는 순간 검증 안 된 코드가 운영에 나간다.**
13-3 의 함정들은 전부 "서버는 떴는데 값이 틀린" 부류라 로컬에서 잘 안 보인다 —
그래서 운영 앞에 **실제로 배포된 환경에서 확인하는 단계**를 하나 넣었다.

### 15-1. 브랜치와 환경

```
feature/*  각자 작업 ──PR──▶ dev ──PR──▶ main
                             │            │
                             ▼            ▼
                        Railway       Railway
                        staging      production
                     (staging MySQL) (운영 MySQL)
```

| | main | dev |
|---|---|---|
| 역할 | 운영 | 통합 (**기본 브랜치**) |
| 직접 푸시 | 금지 (PR + 승인 1명) | PR 권장 |
| 배포 | Railway production 자동 | Railway staging 자동 |

**기본 브랜치를 `dev` 로 둔 이유**: 새로 clone 한 사람이 아무 생각 없이 시작한
자리가 운영 브랜치면 안 된다. 규칙을 기억에 의존하지 않게 하는 가장 싼 방법이다.

### 15-2. 정해 둔 것과 이유

| 결정 | 이유 |
|---|---|
| 스테이징 환경을 따로 만든다 | 운영을 계속 살려둔 채 고쳐야 한다. 13-3 의 다섯 함정 중 넷은 배포된 뒤에야 보였다 |
| 스테이징 DB 는 **별도 MySQL** | 같은 DB 를 보면 테스트 가입·결제가 운영에 섞이고 스키마 변경이 운영을 깬다 |
| 개발 DB 는 **각자 로컬 Docker** | 공유 DB 는 한 명의 스키마 변경이 나머지를 멈춘다 |
| API 키는 **팀 전원 공유** | 발급처가 8곳이라 각자 받게 하면 온보딩이 반나절이다. 대신 Groq rate limit 은 공유되고(429), 운영 DB 접속 정보는 공유하지 않는다 |
| 스키마 변경은 **코드보다 먼저** 운영에 넣는다 | 순서를 뒤집으면 새 코드가 없는 컬럼을 읽는 구간이 생긴다 |

### 15-3. 네이버 로그인은 '개발 중' 상태였다 (2026-07-31)

팀원이 운영에서 네이버 로그인이 안 된다고 했다. 서버는 정상이었다 — 올바른
`client_id`·`redirect_uri` 로 `nid.naver.com` 에 리다이렉트하고 있었고, 화면에 뜬 것은
`현재 careerly는 개발 중 상태입니다. 개발 중 상태에서는 등록된 아이디만 로그인할 수 있습니다.`

**네이버 로그인 앱은 만들면 '개발 중' 으로 시작하고 등록된 테스터만 로그인된다.**
코드에 원인이 없는데 화면에는 실패로 보이는 부류라 12-1·13-3 과 성격이 같다.
조치는 멤버관리에 아이디 추가(즉시) 또는 검수 요청(공개). `docs/2차-배포-수정목록.md` K1.

> **How to apply** — 소셜 로그인이 "안 된다" 는 신고를 받으면 **에러 문구를 먼저
> 그대로 받아 적는다.** 콘솔 상태 때문인지 우리 코드 때문인지가 문구 한 줄에 갈린다.

### 15-4. 절차 문서

- 무엇을 고칠 차례인지: **`docs/2차-배포-수정목록.md`** — 14장을 "어느 파일·어느
  콘솔에서 무엇을" 로 푼 실행 목록. **새 수정사항은 거기에 먼저 적는다**
- 새 팀원 온보딩: **`docs/팀-개발환경.md`** (clone → Docker MySQL → `.env` → 첫 PR)
- 스테이징 구성: `docs/배포.md` **8장**
- 2회차 이후 배포: `docs/배포.md` **9장**

> 사용자가 웹 콘솔에서 해야 하는 것: GitHub 브랜치 보호(main/dev)와 기본 브랜치 변경,
> Railway `staging` 환경 생성, 각 OAuth 콘솔에 스테이징 콜백 추가.

### 15-5. 브랜치를 많이 만들지 않는다 (2026-08-16, 사용자 지시)

한때 원격 브랜치가 **13개**까지 늘었다. 그중 11개가 이미 머지된 채 몇 주씩 남아
있었고(가장 오래된 것은 132커밋 뒤처진 `feature/sqlite-database`), 어느 것이 살아
있는 작업인지 목록만 봐서는 알 수 없었다.

**규칙**

| | |
|---|---|
| 기본 | **작업 하나에 브랜치 하나.** 관련된 변경은 나눠 따지 말고 한 브랜치에 커밋으로 쌓는다 |
| 이름 | `feature/<한 덩어리>` — 화면 하나가 아니라 **끝나는 단위**로 짓는다 |
| 수명 | **머지되면 그 자리에서 지운다.** "나중에 정리" 는 오지 않는다 |
| 급한 수정 | 새 브랜치를 따기 전에 **지금 브랜치에 넣을 수 있는지** 먼저 본다 |

로드맵 흐름·잡알리오 연동·B20 세 작업을 `feature/croad-rename` **한 브랜치에
커밋 3개**로 넣은 것이 기준이다. 서로 이어지는 작업이라 나눴으면 PR 세 개가
서로를 기다렸을 것이다.

**정리 명령** (머지 여부를 먼저 확인하고 지운다):

```bash
# dev 에 완전히 들어간 원격 브랜치 목록
git fetch --prune
git branch -r --merged origin/dev | grep -v 'origin/\(dev\|main\|HEAD\)'
git push origin --delete <위 목록>
```

### 15-6. `main` 을 웹에서 직접 고치면 역류한다 (2026-08-16 실제 사고)

GitHub 웹에서 `CROAD-작업정리.md` 를 지웠다. "옛 내용만 있는 문서" 로 보였기
때문인데 **실제로는 13~18장이 전부 최신이었다.** 그 자체도 문제였지만, 더 나쁜 것은
**경로**였다.

```
main 에서 직접 삭제 → dev 가 main 을 받아오며 dev 에서도 사라짐
                    → 그 문서를 고치던 feature 브랜치와 modify/delete 충돌
```

`main` 에 직접 올라간 커밋 두 개 때문에 `main` 이 `dev` 보다 6커밋 앞서는,
**흐름이 거꾸로 된 상태**가 됐다. 15-1 의 "`main` 에 직접 푸시하지 않는다" 는
규칙이 이미 있었지만, **웹에서 파일을 지우는 것도 직접 푸시라는 점**이 빠져 있었다.

> **How to apply** — 웹 UI 의 'Delete file' · 'Edit' 도 커밋이다. `main` 에서는
> 쓰지 않는다. 그리고 **문서가 오래돼 보이면 지우기 전에 마지막 장의 날짜를 본다** —
> 이 문서는 아래에서 위로 자라서, 앞부분만 보면 항상 낡아 보인다.
>
> 근본 대책은 **브랜치 보호**다(Settings → Branches → `main`·`dev`).
> 걸어 두면 웹에서도 PR 을 거치게 된다.

---

## 16. 기업분석 + 자소서 작성 기준 (2026-08-09 신규)

자소서 코치에 **"이 회사를 분석해서, 내 스펙 중 쓸 것을 골라, 이렇게 적어라"** 를 채운 작업.
출처는 링커리어 커뮤니티 글 두 편(기업분석 방법 2026 · AI 자소서 프롬프트 정리)이고,
거기서 C:road 에 **없던 것만** 골라 넣었다. 이미 있던 것(문항별 기준·소재 배분·역량 추출)은 그대로 뒀다.

### 16-1. 만든 것

| 파일 | 하는 일 |
|---|---|
| `src/cover-guide.js` | STAR 4단계 · 상투표현 15종 · AI 흔적 7종 · 면접질문 3종 · **첫 문장 틀** |
| `src/dart.js` | 기업개황(업종) · 재무 3년 · 같은 업종 회사. 키 없으면 조용히 null |
| `routes/companyAnalysis.js` | `GET /api/company/analysis?name=` — 5단계를 한 번에 |
| `scripts/fetch-dart-corps.js` | 고유번호 색인(corpCode.xml). **ZIP 을 의존성 없이 직접 푼다** |
| `scripts/build-dart-industry.js` | 업종코드 채우기. 중단해도 이어서 실행된다 |
| `test/cover-guide.test.js` (34) · `test/dart.test.js` (16) | 합 **314** 전부 통과 |

### 16-2. 첫 문장을 "예시"로 주되 완성문은 주지 않는다

9-2 에서 "AI 가 자소서 문장을 써 준다"를 버렸는데, 그 유보 조건("Groq 70B 로 확정한 뒤")이
지금 충족됐다(기본 Groq · AI 보강 실측 0.69초). 그래서 **한 칸만 내려갔다.**

- 주는 것: 내 활동 이름이 채워진 **문장 틀** — `커머스 데이터 인턴에서 [무엇이 문제였는지 한 문장]이었습니다…`
- 안 주는 것: 완성된 문장

**대괄호가 남은 문장은 그대로 낼 수 없다.** 이게 대필 방지 장치다. 채웠는지 여부는 작성칸이 세어 준다.
그리고 **AI 를 부르지 않는다** — 아는 것(활동명·유형·기간)만 끼우는 조립이라 같은 입력이면 같은 결과가 나오고,
지어낸 사실이 섞일 자리가 없다. 9-2 의 "검증 수단이 없다"는 지적이 문장 생성에는 여전히 유효하다.

### 16-3. 초안 검사는 브라우저에서만 돈다

상투표현·AI 흔적 검사는 **서버로 초안을 보내지 않는다.** 서버는 '무엇을 찾을지'(사전)만 내려보내고
대조는 화면에서 한다. 자소서 초안은 남의 서버에 올릴 이유가 없는 글이고, 검사 자체가 문자열 대조라
거기서 끝난다. `JdCoach.checkDraft()`.

> 실측 함정 — 사전에 `'~을 통해'` 로 적었더니 **`를 통해` 를 통째로 놓쳤다**(을/를).
> 조사를 떼고 `'통해'` 만 센다. 테스트가 잡았다.

### 16-4. DART — 없는 것을 있는 척하지 않는다

- **키가 없으면 재무·경쟁사 칸만 빠진다.** 서버는 죽지 않는다(job-trends 와 같은 정책)
- **비상장사는 개황까지 된다.** 재무(사업보고서)는 상장사만 있지만 기업개황(업종·설립일·대표·본사·홈페이지)은
  공시대상 법인이면 다 열려 있다. 없는 것은 없다고 화면이 그대로 밝힌다 → 16-8
- **경쟁사라고 단정하지 않는다.** "업종코드가 같은 회사"라고 적는다 — 코드가 같아도 사업은 다를 수 있다
- `toNumber('')` 가 **0 이 되는 함정**이 있었다. 신규 상장사의 빈 전전기가 "0원"으로 찍혀
  "매출이 0이었다"로 읽힌다. 자료 없음(null)과 실제 0 을 구분한다 — 테스트가 잡았다

### 16-5. 5단계를 다 못 채워도 칸을 지우지 않는다

기업분석 5단계(사업구조·재무·경쟁사·최근이슈·인재상JD) 중 C:road 가 못 채우는 칸은
**숨기지 않고 "직접 확인하세요"로 남긴다.** 안 보여주면 학생은 그 칸이 필요 없다고 오해한다.
뉴스와 재무를 하나의 '기업 매력도' 점수로 버무리지 않는 것도 그대로다(11-1 원칙).

### 16-6. 발견 — `routes/news.js` 가 dev 에서 사라져 있었다

`e628b86 chore(자소서): 안 쓰는 회사뉴스 코드를 정리한다` 가 dev 에서 지웠다.
**당시엔 옳은 정리였다** — 자소서 코치를 데모로 바꾸며 UI 가 빠져 실제로 죽은 코드였다.
다만 `main` 에는 남아 있어서 **dev→main 병합이 main 의 기능을 되돌리는 상태**였다.
이번 작업이 그 자리를 `routes/companyAnalysis.js` 로 대체하므로 문제는 해소됐다.

> **How to apply** — "안 쓰는 코드"를 지울 때 **다른 브랜치에서 쓰고 있는지** 본다.
> 한쪽에서만 죽은 코드는 병합 방향에 따라 기능 삭제가 된다.

### 16-7. 남은 것

| | |
|---|---|
| **키 발급** | `DART_API_KEY` — https://opendart.fss.or.kr (무료·즉시). `.env` 에 넣고 스크립트 2개 실행 |
| **미반영** | 입사 후 포부 1년차/3년차 분리(요청에서 빠짐) · 사업부별 매출 비중(DART 단일 API 로는 안 나온다) |

### 16-8. 기업 색인을 상장사 → 공시대상 전체로 넓혔다 (2026-08-11)

**증상.** 캐논코리아 리포트가 개요·재무·경쟁사 모두 "공시 자료 없음" 이었다.

**원인은 DART 가 아니라 우리 캐시였다.** `fetch-dart-corps.js` 가 상장사(3,981건)만
남기고 있었는데, 캐논코리아는 DART 에 **등록돼 있다**(고유번호 `00120580`, 비상장).
캐시에 없으니 이름 조회에서 떨어지고, 그다음 단계는 아예 부르지 않았다.

**실측 — 비상장사도 개황은 전부 나온다.**

| API | 캐논코리아 |
|---|---|
| `company.json` 기업개황 | **나온다** — 업종 29180 · 1985.05.10 설립 · 대표 박정우 · 본사 삼성동 · kr.canon |
| `fnlttSinglAcnt.json` 재무 | 없다 (사업보고서 제출 의무가 없다) |
| `empSttus.json` 직원현황 | 없다 (같은 이유) |

없는 것은 재무 하나인데 그것 때문에 나머지를 통째로 버리고 있었다.
→ 캐시를 **공시대상 전체 118,692건**(약 6MB)으로 바꿨다. 쿠팡·우아한형제들·컬리·
무신사·당근마켓 같은 비상장사가 같이 딸려 들어왔다.

**대신 생긴 문제 — 동명이인.** 이름이 겹치는 회사가 7,957쌍이고, 그중 **419건은
상장사와 같은 이름의 비상장 법인**이다(신한·하나은행·쇼박스…). 색인이 먼저 온 것을
쓰면 학생이 아는 회사 대신 이름만 같은 소규모 법인의 개황이 뜬다 — **남의 회사 재무를
자소서에 쓰게 된다.** 두 군데서 막았다: 수집 때 상장사를 앞으로 정렬하고,
`dart.js` 색인이 겹칠 때 상장사로 덮어쓴다. 테스트가 지킨다(dart.test.js 6장).

**업종코드는 상장사만 채운다.** 11만 건을 다 채우려면 DART 일일 한도(20,000)로
엿새가 걸린다. 업종코드를 쓰는 곳은 경쟁사 목록 하나뿐이고 후보는 어차피 상장사다
(비상장은 매출 비교가 불가능하다). 다시 받을 때 이전 파일의 업종코드를 물려받는다 —
그 값에 API 호출 3,981번이 들어 있다.

**브랜드 이름 ≠ 법인명.** 캐시를 넓혀도 '토스'는 안 잡힌다(법인명 비바리퍼블리카).
`ALIASES` 에 12개 추가: 토스·배달의민족·당근·한국타이어·마켓컬리·오늘의집·
삼성SDS·올리브영·스타벅스·현대오일뱅크.

**색인 파일은 깃에 넣지 않는다.** 6MB 를 커밋하면 **다시 받을 때마다** 그 크기가
히스토리에 통째로 쌓인다(내용이 조금 달라도 JSON 한 줄이라 델타가 안 먹는다).
우리가 만든 자료가 아니라 DART 가 매일 주는 원본이니 받아 오면 그만이다.

| | |
|---|---|
| 배포 | Railway **Build Command `npm run build`** — 빌드 산출물이 이미지에 그대로 들어간다 |
| 로컬 | `npm run build` 한 번. 클론 직후엔 없다 |
| 키가 없거나 DART 가 죽었을 때 | **건너뛰고 배포는 계속된다**(`--if-possible`). 기업분석 칸만 빈다 |
| 지금 어느 쪽인지 | 부팅 로그 `[기능] … 기업분석 켜짐 / 색인 없음 / 꺼짐(키 없음)` |

> **How to apply** — 외부 API 가 통째로 주는 큰 원본은 커밋하지 말고 빌드에서 받는다.
> 대신 **없을 때 어떻게 되는지**를 부팅 로그 한 줄로 남긴다. 안 그러면 화면만 보고
> "DART 가 자료를 안 준다"로 오해한다 — 실제로 이 작업의 출발점이 그 오해였다.

### 16-9. 그 .gitignore 가 업종코드를 같이 데려갔다 (2026-08-11, 운영에서 발견)

색인을 깃에서 빼자 **배포에서만** 계열별 둘러보기가 0곳이 되고 경쟁사가 빈 채로 나갔다.
로컬은 멀쩡했다 — 로컬 파일에는 업종코드가 그대로 남아 있었으니까. 부팅 로그도
`기업분석 켜짐` 이라 정상으로 보였다(색인 파일은 실제로 있었다).

**업종코드가 색인 파일 안에 살고 있었다.** 두 값의 성격이 완전히 다른데 한 파일에 있었다.

| | 이름 색인 | 업종코드 |
|---|---|---|
| 무엇 | DART 가 통째로 주는 원본 | 회사마다 개황 API 를 한 번씩 불러 만든 값 |
| 다시 만드는 비용 | 다운로드 1회 | **호출 3,981번 · 약 8분** |
| 크기 | 6MB | 72KB |
| 그래서 | 빌드에서 받는다 | **깃에 넣는다** (`data/dart-industry.json`) |

`dart.js` 가 둘을 읽어 합친다. `company-sectors.js` 는 예전에 `dart-corps.json` 을
**직접** 읽고 있어서 이 사실을 혼자 몰랐다 — `DART.allCorps()` 를 쓰게 바꿨다.
빌드 시나리오(새로 받은 색인의 업종코드 0건 → 덧씌워 3,981건)를 테스트가 지킨다.

> **How to apply** — 파일을 .gitignore 에 넣기 전에 **그 파일 안에 비싸게 만든 값이
> 같이 들어 있지 않은지** 본다. 크기 때문에 빼는 파일과 계산 비용 때문에 넣는 값은
> 같은 파일에 두면 안 된다. 그리고 이번처럼 **로컬에서는 절대 안 잡히는 종류의 결함**이
> 있다 — 배포 후 화면을 한 번 열어 보는 것 말고는 잡을 방법이 없었다.

---

## 17. 커리어 로드맵 = 네 화면을 잇는 흐름 (2026-08-15)

브랜드 개편(`ed558f7`)에서 **이름만** 바꿔 뒀던 것을 실제 동작으로 만든 작업이다.
그 커밋의 커밋메시지에 "커리어 로드맵 = 흐름 전체의 이름"이라고 적혀 있었지만,
**코드에서는 네 화면이 서로를 모르고 있었다.**

```
① 직무 찾기(#career) → ② 지금 내 위치(#dashboard·CAS)
    → ③ 지원할 회사(#company) → ④ 자소서 코치(#jd)
                ↑
        ②에서 부족하면 스펙 채우기(#mypage/spec) 로 갔다가 ②로 돌아온다
```

### 17-1. 무엇이 문제였나 — 같은 사람이 목표 직무를 두 개 갖고 있었다

| 화면 | 직무를 무엇으로 세고 있었나 |
|---|---|
| 직무 찾기 | KECO 직업 **461개** (고른 값은 화면 안 변수에만 있었다) |
| 선배 스펙 통계 | KECO **2차 분류 35개** |
| CAS 점수 | 완전히 다른 축 — 옛 학과 스키마(`dept`/`field`/`job`) |

로드맵에서 '백엔드 개발자'를 고르고 CAS 로 넘어가면 CAS 는 그 사실을 **모르고**
자기 나름의 '비교 직무' 셀렉트로 다른 직무를 골라 채점했다. 두 화면의 숫자가 왜
다른지 설명할 방법이 없었다. 회사 찾기·자소서 코치는 직무를 아예 몰랐다.

### 17-2. 만든 것

| 파일 | 하는 일 |
|---|---|
| `frontend/js/roadmap.js` | **흐름 상태의 단일 출처.** 고른 직무·회사, 집계 기준(`bench()`), 네 화면 공통 스텝바 |
| `company-sectors.js` 의 `SECTORS_BY_MIDDLE` | 직무(KECO 2차 35개) → 계열 15개. 3단계가 쓴다 |
| `GET /api/company/sectors?middle=` | 목록은 그대로 두고 `focus` 만 덧붙인다 |
| `mentoring.js` 의 GAP 재작성 | 하드코딩 목업 → 선배 데이터 실계산 (**B4 완료**) |
| `test/roadmap.test.js` (55) · `company-sectors.test.js` (15 → 26) | 합 **481** 전부 통과 |

### 17-3. 저장 위치를 둘로 나눈 이유

| 무엇 | 어디에 | 왜 |
|---|---|---|
| 1차·2차 분류 (목표 직무) | **서버** `spec.jobMajor` / `jobMiddles` | 기기를 바꿔도 목표는 남아야 한다. **스펙 입력 폼과 같은 칸이라 새 컬럼이 필요 없었다** |
| 직업 코드·이름, 고른 회사 | 브라우저 localStorage | 화면 이동 중의 문맥이다. 집계에 안 쓰이므로 서버에 둘 이유가 없다 |

> 스키마를 안 건드려도 됐던 것은 운이 아니라, 스펙 폼이 이미 KECO 로 옮겨져 있었기
> 때문이다(`spec-form.js` initJobPicker). **새 기능을 붙이기 전에 기존 칸부터 볼 것.**

**남이 고른 것을 말없이 지우지 않는다** — 스펙 폼의 '세부직무'는 다중선택이다.
로드맵에서 고른 하나로 덮어쓰면 거기서 고른 나머지가 사라지는데 에러가 안 난다
(6-3 '조용히 틀리는 곳'과 같은 부류). 그래서 같은 1차 분류면 **더하고**, 1차가
바뀌면 교체하되 **토스트로 알리고 되돌리는 곳까지 말한다.**

### 17-4. 해상도를 속이지 않는다 — 이 작업의 핵심 판단

요구는 "고른 직무의 CAS 점수"였지만 **직업 461개 단위로는 낼 수 없다.** 그 단위로
쪼개면 선배 표본이 한 자릿수가 되어 평균이 뜻을 잃는다.

그래서 `Roadmap.bench()` 는 **2차 분류로 집계하고, 화면은 "○○ 직무군 기준"이라고
그대로 적는다.** career.js STEP 03 과 **같은 matcher·같은 certKey** 를 쓰므로
로드맵에서 본 "선배 n명"과 CAS 의 n 이 어긋날 수 없다.

표본이 0이면 **넓히지 않고 옛 기준으로 물러선 뒤 그렇게 밝힌다.** 조용히 학과 전체로
넓히면 화면은 '이 직무 점수'라고 적힌 채 다른 모집단의 점수를 보여주게 된다.

### 17-5. GAP 목업을 걷어냈다 (B4 · 8장 CEO 공개 선행조건 1번)

누가 로그인해도 **정보처리기사·SQLD·AWS SAA** 가 뜨던 하드코딩 배열을 지웠다.
판정 기준은 전부 **선배 데이터**다 — 우리가 중요하다고 정한 것이 아니라
"같은 직무로 간 선배들이 실제로 갖고 있는데 내게 없는 것"이다.

| 탭 | 부족 판정 |
|---|---|
| 자격증 | 선배 보유율 ≥ **40%** 인데 내 목록에 없다 |
| 활동·경험 | 선배 보유율 ≥ **40%** 인 활동 유형인데 한 건도 없다 |
| 수상 경력 | 선배 보유율 ≥ **25%** 인 성과 종류인데 어디에도 없다 |

- **표본 5명 미만이면 판정을 접는다.** 3명 중 2명을 '67% 필수'로 적으면 실제보다
  단단한 숫자로 읽힌다 (CAS 백분위 5명 · 직무 트렌드 30건과 같은 원칙).
- **수상만 문턱이 25%** 다. 40% 를 걸면 어느 직무에서도 아무것도 안 뜨는데,
  그러면 '부족 없음'으로 잘못 읽힌다.
- **'부족한 게 없다'와 '판정할 수 없다'를 화면에서 가른다.** 빈 목록 하나로 두 가지
  다른 뜻을 쓰면 로그인 안 한 사용자가 "나는 완벽하다"로 읽는다.

### 17-6. 3단계 '지원할 회사' — 있는 데이터로 만들었다

직무→회사를 이을 데이터가 셋 다 막혀 있었다.

| 후보 | 상태 |
|---|---|
| 사람인 공고 검색 | `SARAMIN_ACCESS_KEY` **비어 있음** (신청·승인 필요) |
| 워크넷(고용24) | 개인회원이라 목록 API 차단 (A3, 여전히 미해결) |
| 선배 스펙의 회사명 | 시드 데이터에 회사가 없어 **표본 0** |

그래서 **업종 계열 매핑**으로 만들었다(사용자 결정). 2차 분류 35개 → 계열 15개
매핑표를 `company-sectors.js` 에 뒀다 — 계열 이름의 단일 출처가 거기라, 프론트에
복사하면 계열 하나를 고칠 때 둘이 갈리고 갈린 것은 '빈 목록'으로만 보인다.

**세 가지를 정직하게 적는다:**

1. **"업종 기준"이라고 화면에 쓴다.** "이 회사가 지금 그 직무를 뽑는다"가 아니다.
   실제 채용 여부는 회사 리포트의 공고 칸에서 확인하게 한다.
2. **경영·사무직과 영업·판매직은 좁히지 않는다**(`universal`). 전 업종이 뽑는
   직무에 계열 몇 개를 억지로 적으면 나머지 업종을 후보에서 지운다.
3. **군인·경찰은 민간 계열이 없다고 말한다.** 칸을 지우지 않는다 (16-5 와 같은 원칙).

`universal`(전 업종) · `sectors: []`(민간 경로 없음) · `matched:false`(모르는 직무)를
**응답에서 구분**한다. 셋을 빈 배열 하나로 뭉치면 화면이 같은 값을 세 가지 다른
뜻으로 쓰게 된다.

> 사람인 키가 들어오면 `saramin-jobs.js` 에 키워드 검색을 더해 **"채용 중" 배지**로
> 승격하면 된다. 지금 코드는 그 자리를 비워 두고 있다(job-trends 와 같은 구조).

### 17-7. 스텝바 — 막지 않는다

네 화면 맨 위에 같은 줄이 뜬다(`Roadmap.stepBar`). 옛 '회사 검색 / 자소서 코치'
2칸 서브네비는 지웠다 — 그 둘이 로드맵의 3·4단계 그 자체라, 같은 이동을 두 줄로
두면 어느 쪽이 흐름인지 흐려진다.

**앞 단계를 잠그지 않는다.** 상단 네비에 CAS·회사 찾기가 그대로 있어서, 스텝바만
막으면 "네비로는 되는데 여기서는 안 되는" 화면이 된다. 대신 흐릿하게 두고, 각
화면이 "직무를 고르면 이 직무 기준으로 계산해 드려요"라고 스스로 안내한다.

**2단계의 갈림길도 한쪽을 막지 않는다.** 부족한 항목이 있으면 '스펙 채우기'를 주
버튼으로 두지만 '지원할 회사'도 같이 보여준다. 공고에는 마감일이 있고, 스펙이
완벽해질 때까지 지원하지 말라는 말이 되면 안 된다.

### 17-8. 남은 것

| | |
|---|---|
| **B17** | 사람인 키 발급 후 3단계에 '채용 중' 배지 — 코드 자리는 준비돼 있다 |
| **B18** | 선배 스펙에 회사명이 쌓이면 "이 직무 선배가 실제로 간 회사"를 계열 위에 얹는다. 지금은 시드에 회사가 없어 표본 0 |
| **B19** | 4단계 완료(자소서 저장) 뒤 스텝바에 완료 표시 — 지금은 3단계까지만 체크가 붙는다 |
| B5 | CAS 페이지 잔여 하드코딩 — GAP(B4)은 이번에 끝났고 "선배 24명" 류가 남아 있는지 재확인 필요 |

### 17-9. 되돌아보기에서 잡은 것 두 개 (실측 아님 — 코드 재검토)

둘 다 **에러가 안 나고 화면만 조용히 어긋나는** 부류라 12-1 과 성격이 같다.

**① CAS 에서 직무군을 바꿔도 직무 찾기 화면은 옛 직무를 보여줬다.**
로드맵 직무를 화면 상태로 되돌리는 코드를 처음에 "한 번만" 으로 썼는데, 직무는
CAS 의 비교 직무 셀렉트에서도 바뀐다(`Roadmap.switchMiddle`). 같은 흐름 안에서 두
화면이 다른 직무를 말하게 되는 — **이 작업이 없애려던 바로 그 상태**였다.
'한 번만' 이 아니라 **'마지막으로 반영한 값과 달라졌을 때만'** 으로 바꿨다
(화면 안에서의 둘러보기는 로드맵 값을 바꾸지 않으므로 방해받지 않는다).

**② 스텝바 체크를 추론으로 붙이고 있었다.** "회사를 골랐으니 2단계도 지났겠지"
로 계산했는데, 상단 네비로 3단계에 바로 들어온 사람에게 **하지도 않은 일이 완료로
찍힌다.** 칸마다 남은 흔적으로만 판정하게 고쳤다 — 1단계는 고른 직무, 2단계는 내
스펙, 3단계는 고른 회사. 4단계는 근거가 없어서 아직 체크가 안 붙는다(B19).

> **How to apply** — 흐름 상태를 여러 화면이 **쓰기까지** 하면, 각 화면은
> "내가 마지막으로 본 값" 과 지금 값을 비교해야 한다. 읽기만 한다고 가정하고
> 한 번만 동기화하면 반대 방향 변경을 놓친다.

### 17-10. 조사를 데이터로 만든다

`'을(를)'` 로 적어 뒀더니 화면에 **`응용소프트웨어개발자을(를) 주로 뽑는 계열`** 이
그대로 나갔다. 직무·회사 이름을 문장에 끼우는 자리가 여럿이라(제목·버튼·안내문)
받침에 따라 조사를 고르는 `Roadmap.josa()` 를 뒀다.

- 한글 음절은 `0xAC00` 부터 28개씩 묶여 있어 `(코드 - 0xAC00) % 28` 이 종성이다
- `'로'` 만 **ㄹ 받침을 예외**로 둔다 — `서울으로` 가 아니라 `서울로`
- **한글로 안 끝나면 둘 다 적는다**(`SK를(을)`). 영문 사명·숫자는 받침을 알 수 없고,
  찍어서 틀리느니 괄호를 남기는 편이 낫다

> 미리보기(17-11)에서 눈으로 보고서야 찾았다. 문자열 조립은 테스트를 통과해도
> **읽어 보기 전에는 어색한 줄 모른다.**

### 17-11. DB 없이 화면을 확인하는 길

작업 당일 Docker Desktop 이 **새 약관 동의 대기**로 엔진을 못 올려(`license
acceptance: false` → `engine state: stopped`) MySQL 을 띄울 수 없었다. 그래도 확인이
필요해서, 스크래치패드에 **DB 없이 뜨는 미리보기 서버**를 만들어 흐름을 눈으로 봤다.

| 엔드포인트 | 출처 |
|---|---|
| `/api/jobs` | **진짜** — `wage-jobs.js` 가 캐시 파일을 읽는다(10·35·461). DB 불필요 |
| `/api/company/sectors` | **진짜** — `company-sectors.js` 는 원래 DB 를 안 쓴다(778곳) |
| `/api/specs` · `/api/auth/me` | **가짜** — `demo-seed.js` 의 `generateRandom()` + 로그인한 척 |

즉 **직무 분류와 계열 매핑은 실제 데이터로** 확인했고, CAS 점수의 숫자 자체는
확인 대상이 아니었다(테스트 481개가 본다).

> **How to apply** — 이 프로젝트는 DB 가 없으면 서버가 일부러 죽는다(그 판단은 옳다).
> 하지만 **화면을 보려고 그 규칙을 무르지 말고**, 캐시 파일만 읽는 모듈이 무엇인지
> 확인해 별도 하네스를 세우면 된다. 여기서는 로드맵이 쓰는 두 API 가 마침 둘 다
> DB 를 안 쓰는 모듈이었다.

---

## 18. 공공기관 채용공고 (잡알리오) — 2026-08-15

회사 리포트의 '채용공고' 칸이 대부분 비어 있던 것을 채웠다. 사람인은 승인이 심사라
나지 않았고, 워크넷 채용정보(고용24 210L01)는 **개인회원이 호출할 수 없다**(10-7,
이번에 재확인 — 응답이 그대로다). 잡알리오는 공공데이터포털 **자동승인**이라
이미 가진 `DATA_GO_KR_SERVICE_KEY` 로 열린다.

### 18-1. 확인된 규격 (2026-08-15 실호출)

| | |
|---|---|
| 요청주소 | `https://apis.data.go.kr/1051000/recruitment/list` |
| 인증 | `serviceKey` (소문자 s) · **URL 인코딩한 값** |
| 형식 | `resultType=json` — **`type=json` 이 아니다** |
| 봉투 | `{ resultCode, resultMsg, totalCount, result: [] }` |
| 핵심 필터 | **`ongoingYn=Y`** — 접수 중만 |
| 트래픽 | 개발계정 1,000건/일. 응답 헤더 `x-ratelimit-remaining` 로 남은 수를 준다 |

**`ongoingYn=Y` 가 이 연동의 전부라고 해도 된다.** 필터 없이 부르면 서비스 개시
이후 전량 **112,920건**이 잡힌다 — 100건씩 1,130회로 일일 한도를 넘고 저장하면
120MB 다. `ongoingYn=Y` 하나로 **532건 · 6회 호출 · 0.9MB** 가 된다.

실측(2026-08-15): 접수 중 532건 · 기관 126곳 · 신입 지원 가능 503건.

### 18-2. 하루를 날릴 뻔한 함정 — Swagger 예제 URL

`.env` 에 넣은 주소가 요청주소가 아니라 **포털의 Swagger 예제 URL** 이었다.
예제용 필터가 통째로 붙어 있다:

```
…/recruitment/list?serviceKey=…&recrutPbancTtl=보훈&pblntInstCd=C0203
  &pbancBgngYmd=2023-07-01&pbancEndYmd=2023-12-10&ongoingYn=Y&…   (16개)
```

거기에 우리 파라미터를 덧붙이니 같은 이름이 두 번 실려 **서버가 앞의 것(예제 값)을
썼다.** 증상이 고약했다:

| 겉보기 | 실제 |
|---|---|
| 첫 프로브가 "2023년 보훈 공고 **1건**" → *"상세 조회 엔드포인트구나"* | `recrutPbancTtl=보훈` + `pblntInstCd=C0203` 이 걸린 **목록** 조회였다 |
| 그 뒤 계속 0건 → *"트래픽 제한인가, 장애인가"* | `2023-07-01~12-10` + `ongoingYn=Y` 라 당연히 0건 |
| 간헐적으로 보였다 | 내가 주소를 직접 넘긴 한 번만 필터가 없었다 |

`x-ratelimit-remaining: 983` 을 보고서야 한도가 아님을 알았고, 그때 `.env` 원문을
`cat -A` 로 열어 보고 찾았다. **HTTP 200 · resultCode 200 인데 0건** 이라 어디에도
오류가 없었다 — 12-1 의 함정과 성격이 같다.

**고친 것**: 수집기가 주소에서 `origin+pathname` 만 남기고 쿼리스트링을 떼어낸다.
그리고 **떼어낸 파라미터 이름을 화면에 찍는다** — 조용히 고치면 다음 사람이 또 겪는다.

> **How to apply** — 공공데이터포털 주소를 받으면 **쿼리스트링이 붙어 있는지 먼저 본다.**
> 붙어 있으면 그건 요청주소가 아니라 예제다. 그리고 "결과가 0건인데 오류가 없다" 면
> 키·장애를 의심하기 전에 **내가 보낸 URL 전문**을 먼저 찍어 본다.

### 18-3. 붙인 자리 — 배지가 아니라 회사 리포트

`fetchJobs()` 가 **사람인 → 잡알리오 → 워크넷** 순으로 부르고 처음 잡히는 것을 쓴다.
셋 다 `companyJobs(회사명)` 한 가지 모양이라 화면은 구분하지 않는다.

**3단계 회사 목록에 '채용 중' 배지를 붙이지 않았다** (사용자와 함께 내린 결정).
이 자료에는 공공기관만 있어서, 배지를 붙이면 **삼성전자에 배지가 없는 것이 "지금
채용을 안 한다"로 읽힌다.** 에러가 안 나는 오해라 6-3 부류다. 커버리지가 한쪽만
있는 데이터로 '있음/없음' 배지를 만들면 안 된다.

회사 리포트 안에서는 이 문제가 없다 — 이미 그 회사를 열어 본 상태이고, 0건일 때
**왜 0건인지 말하는 자리**가 이미 있다.

### 18-4. 0건의 이유를 셋으로 가른다

같은 "0건" 이지만 학생이 할 일이 다르다.

| 상황 | 내려보내는 말 |
|---|---|
| 민간 기업 | "이 자료에는 공공기관 공고만 들어 있어, 민간 기업은 조회되지 않습니다" |
| 있었지만 마감 | "공고 N건이 있지만 모두 접수가 마감됐습니다" |
| 신입 필터에 걸림 | "진행 중 공고 N건이 있지만 신입 지원 가능 공고는 없습니다" |

`recrutSeNm` 이 비어 있으면 **거르지 않는다** — 모르는 것을 '경력직'으로 단정하면
실제 기회를 지운다.

### 18-5. 캐시를 지키는 장치 둘

- **빈 응답을 '수집 완료'로 넘기지 않는다.** 1페이지가 비면 오류로 멈춘다.
  그냥 넘기면 그게 캐시를 비우는 경로가 된다.
- **멀쩡한 캐시를 빈약한 결과로 덮지 않는다.** 이전의 30% 미만이면 거부하고
  `--force` 를 요구한다. 6-5 에서 `db.json` 을 다룰 때와 같은 원칙이다.

원본에는 `disqlfcRsn`·`scrnprcdrMthdExpln` 같은 수천 자짜리 본문이 붙어 온다.
**쓰는 필드만 남기고 긴 텍스트는 1,200자에서 자른다** — 상세가 필요하면 `srcUrl` 로
원문에 간다. 캐시는 `.gitignore` 대상이다(16-9 확인: 이 파일에는 비싸게 만든 값이 없다).

### 18-6. 남은 것

| | |
|---|---|
| **B20** | `aplyQlfcCn`(지원자격)·`prefCn`(우대사항)이 구조화돼 오므로, **공고 본문을 붙여넣지 않아도** 자소서 코치가 역량을 뽑게 한다. 공공기관 지원자는 복붙 단계를 건너뛴다 |
| **B17** | 민간 공고 소스가 생기면 그때 '채용 중' 배지. 그전에는 붙이지 않는다 |
| 갱신 | `node scripts/fetch-alio-jobs.js` — 하루 한 번. 배포에서는 빌드나 크론에 얹는다(미결) |

### 18-7. B20 — 전제가 틀렸다는 것을 실측으로 확인했다 (2026-08-15)

**하려던 것**: 잡알리오가 `aplyQlfcCn`(지원자격)·`prefCn`(우대사항)을 구조화해서 주니,
학생이 **공고 본문을 복사·붙여넣지 않아도** 자소서 코치가 역량을 뽑게 한다.

**532건에 돌려 보고 접었다.** 공공기관 공고의 저 필드는 **응시 요건**이지 직무 역량
서술이 아니다. 민간 공고의 [자격요건]/[우대사항]과 성격이 다르다.

| | 역량 평균 | 역량 0개 |
|---|---|---|
| 제목만 | 0.08개 | — |
| 본문 포함(필터 전) | 1.54개 | 110건 |
| 본문 포함(필터 후) | **0.90개** | **265건 (50%)** |

필터 전 1.54개가 좋아 보이지만 **근거 문장이 가짜였다.** 실측으로 뽑힌 근거:

```
"「대한적십자사운영규정」 제20조(결격사유)에 해당되지 않는 자"     → process 역량
"우리 기관은 정부의 블라인드 채용 가이드라인에 따라…"              → process 역량
```

역량 카드에 **직무와 무관한 인용문**이 붙는다. 에러가 안 나고 그럴듯해서 더 나쁘다.
532건에서 문장 빈도를 세니 상위가 전부 정형문구였다 — 취업지원대상자 33회 ·
첨부파일 참조 31회 · 전형 가산 27회 · 장애인 가점 19회.

**필터를 세게 걸었다가 되돌린 기록** — 처음엔 `우대` 가 든 줄을 통째로 버렸더니
"관련 자격증 소지자 우대" 같은 진짜 우대사항까지 날아가 역량 0개가 110 → 222건으로
**늘었다.** 지금은 법정 가점·전형 안내만 겨냥한다. **애매하면 남긴다** — 남은
노이즈는 근거 문장으로 한 번 보이고 말지만, 지운 신호는 되찾을 방법이 없다.

**그래서 무엇을 했나** — 기능은 남기고 **말을 바꿨다.**

- 공고 칸은 그대로 채운다. 연령·학력·전공 제한, 자격증 요건은 학생이 **지원 가능한
  공고인지 판단**하는 데 실제로 쓴다. 값어치가 없는 게 아니라 **다른 값어치**다
- 안내 문구는 "바로 역량 분석이 됩니다" → **"응시 요건 위주라 역량은 잘 드러나지
  않아요. 역량까지 뽑으려면 본문을 이어붙이는 편이 정확합니다"**
- 배지도 '자격요건 포함' → **'지원자격 포함'**

> **How to apply** — "데이터가 구조화돼 있다"와 "그 데이터가 내가 원하는 것을
> 담고 있다"는 다른 말이다. 필드 이름만 보고 설계하지 말고 **전량에 돌려서 결과
> 분포를 먼저 본다.** 이번엔 필드 이름(`aplyQlfcCn` = 지원자격)이 맞았는데 내용이
> 기대와 달랐다. 한 건만 열어 봤으면 그럴듯해 보여서 그대로 갔을 것이다.

---

## 19. 스펙 채우기(#specup) · 로드맵 잔손질 (2026-08-16)

작업 요청 10건을 한 브랜치(`feature/specup-and-roadmap-fixes`)에 커밋으로 쌓았다
(15-5 브랜치 규칙).

### 19-1. 이름 정리 — careerly → C:road

화면·로고는 이미 `feature/croad-rename` 에서 바뀌어 있었다. 이번에는 **문서 쪽**이다.

- `CAREERLY-작업정리.md` → **`CROAD-작업정리.md`** (참조하는 문서 6개도 같이 고쳤다)
- README·docs·루트 `CLAUDE.md` 의 제품 이름
- `CAS (Careerly Ability Score)` → **`Career Asset Score`** — 화면(`careerly.html`)이
  이미 그렇게 적고 있었는데 문서만 다른 말을 쓰고 있었다

**일부러 안 바꾼 것** (바꾸면 사용자 데이터·주소가 끊긴다):

| 남긴 것 | 바꾸면 |
|---|---|
| `frontend/careerly.html` | 배포 주소·북마크가 깨진다 |
| `careerly_session` 쿠키 · `careerly_*` localStorage 키 | **접속 중인 사용자가 전부 로그아웃되고** 저장된 로드맵·회사 선택이 사라진다 |
| Docker `careerly-mysql` · DB 이름 · Railway 주소 · 저장소 URL | 팀원 환경과 배포가 함께 깨진다 |

8장 페르소나 인용문의 'Careerly' 도 남겼다 — **실행 결과 그대로**라 손대면 기록이
아니게 된다.

### 19-2. 로드맵 화면 잔손질

| 무엇 | 왜 |
|---|---|
| STEP 03 의 **연봉 카드 제거** | 직업 카드(STEP 02)에 평균연봉이 이미 붙어 있다. 고른 직후 같은 숫자를 크게 다시 띄우면 이 화면의 주제인 '선배 스펙'보다 연봉이 먼저 읽힌다 |
| **'같은 갈래의 다른 직업' 카드 제거** | 방금 고른 직업의 형제를 다시 늘어놓아 결정을 되돌리게 만든다. 둘러보기는 STEP 02 가 하는 일이다 |
| 목표 칩의 **'바꾸기' 를 직무 찾기에서만 숨김** | 그 링크가 `navigate('career')` 라, 직무 찾기에서 누르면 지금 화면으로 다시 온다. 아무 일도 안 일어나는 버튼이다. 나머지 세 화면에서는 유일한 되돌아가는 길이라 그대로 둔다 |

### 19-3. 직업 카드를 누르면 화면이 맨 위로 튀던 문제

`career.js` 의 `render()` 가 `#career-main` 의 `innerHTML` 을 통째로 갈아 끼우는데,
브라우저는 그때 **스크롤 위치를 0 으로 되돌린다.** 카드를 눌러도 방금 고른 카드도,
아래에 새로 열린 로드맵도 안 보여서 **누른 게 먹히지 않은 것처럼** 보였다.

`paint(main, html, toTop)` 한 곳으로 모으고 다시 그리기 전 위치를 되돌린다.
`render()` 에는 중간에 빠져나가는 갈래가 넷이라, 각자 `innerHTML` 을 쓰면 한두 곳을
반드시 빠뜨린다. **분야를 통째로 바꾸는 이동만** 맨 위로 올린다(사이드바 · 01 단계로
되돌아가기) — 그때는 화면 내용이 전부 갈리므로 위가 맞다.

### 19-4. CAS '비교 직무' 셀렉트가 비어 있던 이유 — 하나가 아니라 셋

| 상황 | 예전 결과 |
|---|---|
| 비로그인 · 스펙 없음 | `showEmpty()` 로 먼저 빠져나가 셀렉트를 **아예 안 칠했다** |
| 로드맵 직무 없음 | 옛 학과 기반 `SpecForm.JOB_OPTIONS[spec.field]` 인데 그 표가 비면 옵션 0개 |
| 같은 1차 분류에 형제가 없음 | `Roadmap.siblings()` 결과가 하나 → 고를 수 없는 셀렉트 |

**무엇을 보여줄 것인가** — 비교의 단위는 **KECO 2차 분류(직무군) 35개**다. CAS
벤치마크·로드맵 STEP 03·GAP 이 전부 이 단위로 집계하므로 셀렉트도 같은 단위여야
화면끼리 말이 맞는다. 1차 분류로 묶어 `optgroup` 에 담으면 35개도 한눈에 들어온다.

**어떻게 고르게 할 것인가** — 옵션마다 **그 직무군의 선배 표본 수**를 같이 적는다
(`정보통신 연구개발직 · 선배 12명`). 선배가 0명인 직무군은 점수를 내도 비교가
성립하지 않는데, 고르기 전에 보이면 "왜 계산이 안 되지" 가 "표본이 없구나" 로 바뀐다.
개수만 필요하므로 `Aggregator.compute()`(선배마다 정성 원점수 재채점) 대신 matcher 로
세기만 한다 — 35번 부르면 1,000여 명 × 35 회 채점이 된다.

고른 값은 `Roadmap.switchMiddle(middle, major)` 로 흐름 상태에 심는다. CAS 만의 별도
선택을 두면 로드맵과 CAS 가 서로 다른 직무를 목표라고 말하게 된다 — `roadmap.js` 를
만든 이유가 그 상태를 없애는 것이었다.

**덤으로 잡은 '조용히 틀리던 곳'** — 목표 직무의 선배 표본이 0명이면 예전에는 말없이
학과 기준으로 내려갔다. 위쪽 목표 칩은 '○○ 직무군' 이라고 적혀 있는데 점수는 다른
집단에서 나온 셈이라 숫자를 설명할 수 없었다(6-3). 이제는 계산을 접고 왜 접었는지 적고,
셀렉트에서 표본이 있는 직무군으로 바로 옮겨가게 한다.

모집단을 정하는 판단은 `CASHero.resolveContext()` 한 곳으로 모았다 — 스펙업 화면도
같은 함수를 쓴다.

### 19-5. 스펙 채우기 화면 신설 (#specup)

CAS 2단계의 '스펙 채우기' 는 지금까지 **마이페이지 스펙 입력 폼**으로 갔다. 그런데
거기는 *이미 한 것을 적는* 곳이다. "채우면 좋을 항목이 3개 있어요" 를 읽고 누른
학생에게 빈 입력 폼을 주면 흐름이 거기서 끊긴다.

**무엇을 보여줄지 정하는 근거 (사용자 지시: 선배·직무 기준)**

1. **부족한 것 우선** — CAS GAP 판정 그대로. "선배 보유율 40% 이상인데 내게 없는 것"
2. **부족한 게 없으면** 그 직무군 선배 보유율 상위를 보여준다 — '더 할 게 없다' 와
   '보여줄 게 없다' 는 다른 말이다
3. 거기에 **'지금 접수 중'** 을 덧붙인다 (국가자격 시험일정 · 공모전 모집공고)

**판정 기준을 새로 만들지 않았다.** '부족' 의 정의는 `mentoring.js` 가 단일 출처고
(`window.Gap` 으로 내보냈다), 비교 모집단은 `cas-hero.js` 가 정한다. 여기서 따로
계산하면 CAS 에서 3개라던 것이 스펙업에서 5개가 되는 식으로 갈린다.

탭 넷 — 자격증 · 어학 · 공모전·대회 · 대외활동·서포터즈.

> **어학에는 시험 일정을 넣지 않았다.** TOEIC·OPIc·TOEIC Speaking 은 시행기관이 공개
> API 를 열지 않고 국가자격 시험일정에도 없다. 정적 표로 박아 두면 **다음 달에 조용히
> 틀린 날짜**가 된다(6-3 그 부류다). 대신 선배 평균을 목표치로 놓고 차이만 말하고,
> 접수는 공식 페이지로 보낸다.

### 19-6. 외부 API — 찾았고, 붙였고, **열쇠가 없다**

| 무엇 | 소스 | 상태 |
|---|---|---|
| 국가자격 시험일정 | `apis.data.go.kr/B490007/qualExamSchd` (data.go.kr **15074408**) | ✅ **승인 완료 (2026-08-16)** · 실데이터로 배선 끝 |
| 공모전·대외활동 | 온통청년 청년정책 `getPlcy` | ⚠️ **키 없음** — `invalid api key` |

**data.go.kr 키는 API 마다 따로 활용신청해야 한다.** 이미 승인돼 있던
`fetch-qnet-certs.js`(국가자격 **종목 목록**, 15003024)와 시험**일정**(15074408)은
다른 데이터셋이다. 종목 목록이 되니까 일정도 될 거라 넘겨짚으면 헤맨다.

조사해 보고 **버린 대안**(다시 조사하지 말 것)과 필드 이름 확인 절차는
`docs/외부API-연동구조.md` 의 '스펙업 데이터 소스' 장에 있다.

**열쇠가 없어도 화면은 죽지 않는다.** 부족 판정·자격증 목록·어학 목표치는 우리 DB 로
계산하고, 외부 API 자리에는 **무엇을 하면 열리는지**가 뜬다.

점검: `cd backend && node scripts/check-specup-api.js`

### 19-6-1. 명세서만 보고 짠 코드가 세 군데 틀렸다 (승인 직후, 같은 날)

승인이 나자마자 `check-specup-api.js` 를 돌렸더니 **첫 호출에서 세 군데가 드러났다.**
원본 item 을 그대로 찍게 해 둔 것이 값을 했다 — 공정위(`entrprsNm`)·고용24(`coNm`) 에
이어 **명세서를 믿었다가 틀린 세 번째** 사례다.

| 넘겨짚은 것 | 실제 |
|---|---|
| 응답에 종목코드·종목명이 들어온다 | **안 들어온다.** 한 줄은 '자격구분 × 회차' 다 |
| 한 해치를 받아 종목코드로 나눠 쓰면 된다 | 나눌 코드가 없다. **요청에 `jmCd` 를 실어** 종목별로 받는다 |
| `numOfRows` 를 크게 잡으면 페이지가 준다 | **상한 50.** 넘기면 `resultCode 930` 인데 **HTTP 200** 이다 |

세 번째가 이 저장소가 제일 경계하는 부류다. 200 이라 성공으로 읽히고 items 만 비어서,
**페이징 실수가 화면에 '남은 회차가 없어요' 로 나간다.** 이제 `resultCode` 가 00 이
아니면 전부 실패로 올린다.

**실기를 빼고 필기만 고른다** — 실기 원서접수는 필기 합격자만 할 수 있다. 처음엔 상태만
보고 가장 가까운 단계를 골랐더니, 2026-08-16 기준 정보처리기사가 **'실기 9/21 접수'** 로
떴다. 정작 필기(제3회)는 접수가 끝난 상태였다. 아직 필기도 안 본 학생을 헛걸음시키는,
에러가 안 나는 종류의 오류다.

**"국가자격이 아니다" 라고 단정하지 않는다** — 큐넷 종목 목록(613종)에 구멍이 있어
**정보보안기사가 없다**(cert-catalog.js 가 이미 기록한 한계). 못 찾은 것을 "국가자격이
아니에요" 라고 적으면 틀린 말을 자신 있게 하는 것이라, "못 찾았다" 로만 적는다.

실측 확인 (2026-08-16):

```
정보관리기술사  → 필기 2026년도 제140회 [접수중]  2026-08-16~08-17  마감 D-1
한식조리기능사  → 필기 2026년도 제27회  [접수예정] 2026-08-25~08-26  9일 뒤
정보처리기사    → 필기 2026년도 제3회   [접수마감] 시험 2026-08-07
변리사          → 2026~2027년 남은 회차 없음 (2027년은 아직 미공개)
SQLD            → 종목 목록에서 못 찾음
```

### 19-6-2. 정식 서버(MySQL)로 확인하며 나온 화면 문제 셋

스텁 서버로는 안 보이던 것들이다. **선배 스펙 127건과 시험일정 실호출**이 붙으니
드러났다 — 만들어 둔 데이터로만 보면 놓치는 부류다.

| 무엇 | 왜 문제였나 | 어떻게 했나 |
|---|---|---|
| 같은 안내가 6줄 반복 | 상위 자격 6개 중 5개가 민간자격(SQLD·ADsP·CKA…)이라 줄마다 같은 두 문장이 붙어 **화면이 통째로 경고문**이 됐다 | 줄에는 사실만 짧게, 이유는 목록 아래 **각주 하나**로 |
| 절반이 이미 보유한 자격 | '채울 것' 을 찾는 화면인데 상위 6개 중 3개가 내가 가진 것이었다 | 목록에서 빼지 않고(그러면 "선배들이 많이 가진 것" 이라는 뜻이 달라진다) **미보유를 위로 올리는 정렬만** 바꿈 |
| '공모전·대회' 탭에 인턴십·프로젝트 | 해당 유형이 부족 목록에 없으면 전체로 물러서게 해 둔 탓. **탭 제목과 내용이 다르면** 무엇을 근거로 고른 목록인지 알 수 없다 | 이 유형이 부족하지 않으면 그 줄을 안 그린다 |

**확인 결과 (MySQL + 시드 168명, 2026-08-16)**

- 직업 카드 클릭 시 스크롤 774 → 762 (맨 위로 안 튐) · 연봉·같은갈래·바꾸기 없음
- 비교 직무 셀렉트 35개 · optgroup 10개 · 실제 표본이 붙는다
  (경영·행정·사무직 66명 · 정보통신 18명 · 금융·보험 17명 · 법률 8명 …)
- 표본 0명 직무군을 고르면 학과 기준으로 물러서지 않고 이유를 적는다
- 스펙 채우기: 선배 보유율 + 시험일정 실데이터. 온통청년만 503(키 미발급)
- 서버 오류 로그 없음 · 테스트 585개 통과

> **How to apply** — 시드로만 확인하면 '데이터가 고르게 있는' 화면만 보게 된다.
> 이번 셋은 전부 **한쪽으로 쏠린 실제 분포**(민간자격이 다수 · 이미 보유가 절반 ·
> 해당 유형이 0개)에서만 나왔다. 새 화면은 실데이터로 한 번 더 볼 것.

### 19-7. 사용자가 해야 할 일 (A 항목)

| # | 무엇 | 어디서 |
|---|---|---|
| ~~A5~~ | ~~국가자격 시험일정 API 활용신청~~ | ✅ **2026-08-16 완료** — 실데이터로 동작 확인 (19-6-1) |
| **A6** | 온통청년 인증키 발급 → `.env` 의 `YOUTH_API_KEY` | 온통청년 회원가입 → 마이페이지 → OPEN API. **OPEN API 명은 `청년정책API`** 를 고른다(코드가 부르는 `getPlcy` 가 그것이다) |

A6 이 끝나면 `node scripts/check-specup-api.js` 로 확인한다.

---

## 20. '무엇을 하는 회사인가' 를 줄글로 (2026-08-17)

사용자 지시: **"제목과 본문으로 나눠서 설명하도록 해줘. 그리고 키워드로만 설명하지
말고 줄글로 적어줘 — DART 에서 정보를 긁어오거나 다른 API 를 찾아서라도"**

### 20-1. 왜 여태 키워드밖에 없었나

16장에서 "부문별 매출 비중은 오픈API 에 없다" 고 적어 두고, 대신 정형으로 받을 수
있는 두 값(직원현황의 사업부문·타법인 출자현황)을 칩으로 늘어놨다. 그런데 칩을
아무리 읽어도 **"무엇을 만들어 누구에게 팔아 돈을 버는가"** 라는 문장이 안 나온다.
'도료 627명 · 100%' 는 인력 배치지 사업 설명이 아니다.

그 문장은 사업보고서 본문 「II. 사업의 내용」 에만 글로 있다. 이번에는 그 본문을
직접 열기로 했다 — `document.xml`. **오픈API 중 유일하게 JSON 이 아니라 ZIP 을 주는
엔드포인트**라 여태 건드리지 않았던 곳이다.

### 20-2. 원문을 여는 데 두 번 걸렸다

| 걸린 것 | 무엇이었나 |
|---|---|
| ZIP 이 안 풀린다 | DART 는 **스트리밍 ZIP** 이다. 로컬 헤더의 압축 크기가 `0` 이고 진짜 크기는 데이터 뒤 descriptor 에 있다. 로컬 헤더만 훑으면 첫 파일에서 길이 0 을 읽고 멈춘다 → 끝의 **중앙 디렉터리**에서 읽는다 |
| 감사보고서가 나왔다 | 제출 하나에 문서가 여러 개다. 첫 엔트리를 본문으로 알고 읽었더니 「독립된 감사인의 감사보고서」 였고 「사업의 내용」 을 못 찾았다 → `{접수번호}.xml` 을 이름으로 고른다 (`_00760` 꼬리는 첨부) |

외부 zip 패키지는 넣지 않았다. 우리가 여는 ZIP 은 DART 가 만든 이 한 종류뿐이고
deflate 는 `zlib` 에 이미 있다.

### 20-3. 절 제목이 회사마다 다르다

```
강남제비스코 · 삼성전자 · NAVER   → "1. 사업의 개요"
카카오 · 현대자동차               → "1. (제조서비스업)사업의 개요"
```

번호와 말머리로 맞추면 절반이 빠진다. 「II. 사업의 내용」 과 그다음 대제목(III.)
**사이에서** 처음 걸리는 것을 쓴다 — 금융 겸업사의 '(금융업)사업의 개요' 가 뒤에 또
와도 본업이 앞이라 자연히 본업 쪽이 잡힌다.

### 20-4. 진짜 위험은 파싱 실패가 아니라 **부스러기를 회사 설명인 척 보여주는 것**

표는 카드 밖으로 넘쳐서(16장의 재무 표에서 이미 겪었다) 통째로 버리는데, 그러면
그 표에 달려 있던 각주만 남는다. 길이는 문단만 해서 그대로 첫 줄로 올라왔다:

```
현대자동차 → "※ 차량부문은 내부거래조정과 관련된 영업이익을 포함하고 있음."
카카오     → "참고 1: 당기중 ㈜카카오헬스케어와 그 종속기업으로 구성된…"
CJ제일제당 → "(*1) 부문간 내부매출액 제외기준입니다. (연결 기준)"
```

**회사 소개가 각주로 시작한 것이다.** `FOOTNOTE` 정규식으로 걷어내고, 각주만 남는
절은 통째로 뺀다 — 부스러기를 보여 주느니 그 칸이 없는 편이 낫다.
버린 표가 있으면 "표·나머지 내용은 원문에서" 링크를 붙인다. 말없이 지우면 학생은
우리가 보여준 것이 전부라고 믿는다.

같은 문장 중복도 막았다. 강남제비스코는 '사업의 개요' 와 '주요 제품' 의 첫 문장이
거의 같아서 화면에 같은 문단이 연달아 두 번 떴다.

### 20-5. AI 에게 다시 쓰게 하지 않았다

요약을 시키면 **회사가 한 말이 아니라 우리가 지어낸 말**이 되는데 화면에는
'사업보고서에서' 라고 적힌다. 출처를 빌려 쓴 창작이다. 원문 문단을 그대로 옮기고
어느 보고서 어느 절인지 같이 적는다 (6장·9장 원칙 그대로).

### 20-6. 라우트를 따로 뒀다 — `/api/company/business`

원문 ZIP 이 **회사마다 5~14MB** 다(강남제비스코 5.5M · 카카오 13.7M). `/analysis`
에 얹으면 재무도 뉴스도 공고도 그 다운로드를 기다린다. 16장 머리주석의 "두 번
부르게 하면 반쪽만 그려진다" 는 판단은 지금도 맞지만, 이 값은 성격이 다르다 —
**모두를 늦추느니 이 칸만 늦게 채운다.** 화면은 오기 전까지 "사업보고서 원문에서
가져오는 중" 을 보여준다. 푼 결과(수 KB)만 접수번호로 캐시하고 원문은 버린다.

### 20-7. 그러다 **여태 틀리고 있던 직원 수**를 찾았다

줄글로 바꾸면서 삼성전자 문장이 이렇게 나왔다:

> 직원 **257,762명** 가운데 가장 많은 **성별합계**에 128,881명(50%)…

응답을 열어 보니 부문별 줄 **뒤에 합계 줄이 한 번 더** 온다:

```
DX 남 38,119 · DX 여 12,698 · DS 남 56,154 · DS 여 21,910
성별합계 남 94,273 · 성별합계 여 34,608   ← 위 넷의 합
```

전부 더해서 **인원이 정확히 두 배**였고, 사업부문 1위에 '성별합계' 라는 없는 부문이
올라와 있었다. 칩으로 보여줄 때는 그냥 칩 하나가 더 있는 정도라 눈에 안 띄었는데,
문장으로 쓰니 바로 읽혔다. 개요 카드의 직원 수(01 칸 얼굴 숫자)도 같이 틀린 값이었다.

`'전사'·'전체'` 는 합계가 아니다 — 부문을 나누지 않은 회사가 쓰는 유일한 줄이라
빼면 카카오·NAVER 의 인원이 0이 된다. 합계 줄만 온 회사는 그것을 그대로 쓴다.

접기 규칙을 `foldEmployees()` 로 떼어 내보냈다. 틀린 것은 호출이 아니라 이 규칙이라,
고정 응답으로 검증할 수 있어야 같은 실수를 다시 잡는다.

### 20-8. '전사' 를 부문으로 읽으면 화면이 거짓말을 한다

카카오 문장이 이랬다:

> 직원 3,922명이 모두 **전사** 한 부문에 있어요. 사업을 한 갈래로 모아 둔 회사입니다.

**바로 위 문단에서 회사가 플랫폼·콘텐츠 두 부문이라고 적어 뒀다.** 신고 양식의
빈칸을 회사의 사업 구조로 착각한 것이다. `NO_SPLIT` 에 걸리면 "부문으로 나누지 않고
'전사' 한 줄로만 신고했어요 — 이 표로는 알 수 없습니다" 로 바꿔 말한다.

### 20-9. 화면 구성

| 칸 | 내용 | 출처 |
|---|---|---|
| 무엇을 하는 회사인가 | 원문 줄글 | 사업보고서 「사업의 개요」 |
| 무엇을 팔아 버는가 | 원문 줄글 | 사업보고서 「주요 제품 및 서비스」 |
| 사람을 어디에 두고 있나 | 코드가 만든 문장 | 「직원 현황」 — 인력 기준 |
| 밖으로 벌려 둔 사업 | 코드가 만든 문장 | 「타법인 출자현황」 중 경영 참여 |

아래 둘도 칩이 아니라 문장이지만 **숫자 그대로만** 말한다(모델을 거치지 않는다).
매출 비중이 아니라 인력·지분 기준이라는 것은 그대로 적는다.

### 20-10. 확인 (정식 서버 · MySQL)

| 회사 | 결과 |
|---|---|
| 강남제비스코 | 4칸 · 도료 627명 · 관계사 8곳 · 원문 링크 |
| 삼성전자 | **128,881명** (고치기 전 257,762) · DS 61% / DX 39% |
| 카카오 | "부문으로 나누지 않고 '전사' 한 줄로만 신고했어요" |
| NAVER · 현대자동차 · SK하이닉스 · CJ제일제당 | 줄글 정상 |
| 우아한형제들 | "정기보고서가 없어요 — 비상장사는 제출 의무가 없어요" |
| 모바일 375px | 가로 스크롤 없음 |

테스트 694개 통과 (`dart-report.test.js` 22개 신규 · `dart.test.js` 에 접기 8개 추가).

### 20-11. 회사를 한 번 고르면 못 빠져나오던 것 (같은 날, 사용자 지적)

> "회사 한번 선택하면 회사찾기 홈으로는 못 돌아간다. CAS에서 회사찾기로 넘어갈 때도
>  홈 뜨게 바꿔줘야 한다. 한번 회사선택해두면 지금 안 바뀌는 오류 있음."

원인이 셋이었다. 하나씩은 사소한데 겹쳐서 **회사를 바꿀 방법이 사실상 없었다.**

| # | 무엇 | 실측 |
|---|---|---|
| 1 | '다른 회사 검색' 버튼이 **사이드바 맨 아래**, 최근 본 회사·많이 찾는 회사 목록 뒤에 있었다 | 화면 812px 에서 top 955 — 스크롤해야 나온다. 없는 것과 같았다 |
| 2 | `onEnter()` 가 `if (selected) { paint(); return; }` 였다 | CAS 에서 회사찾기를 눌러도 전에 보던 리포트가 그대로 떴다 |
| 3 | 좁은 화면에서 사이드바가 리포트 **위로** 쌓였다 | 375px 에서 회사 이름 top 1233 · 돌아가기 top 1099 — 보러 온 리포트가 1,000px 아래에 |

고친 것:
- **리포트 맨 위, 회사 이름 바로 앞에 '← 회사 찾기'** 를 뒀다. 사이드바 버튼도 남긴다
  (`querySelector` → `querySelectorAll` 로 바꿔야 둘 다 산다 — 하나만 잡으면 사이드바 쪽이 죽는다)
- `onEnter()` 는 **늘 검색 화면부터**. 잃는 것은 없다 — 방금 보던 회사는 '최근 본 회사'
  맨 앞이라 한 번 누르면 돌아가고, 로드맵이 쓰는 회사는 `Roadmap.setCompany` 가 따로 든다.
  자소서 코치가 넘긴 회사(`careerly_company_open`)만 예외로 그대로 연다
- `back()` 이 `reqSeq` 를 올린다. 안 올리면 **늦게 온 응답이 `selected` 가 null 인 채로
  화면을 다시 그린다.** 검색어·자동완성도 비운다 — 남으면 되돌아가지 못한 것처럼 보인다
- 860px 이하에서 `order` 로 **리포트를 사이드바보다 앞에** 둔다. 사이드바는 리포트를
  다 읽은 뒤 '다음에 볼 회사' 자리로 내려간다

확인: 데스크톱 1280px — 뒤로 버튼 top 146(회사 이름 208보다 위) · 2단 유지 ·
CAS→회사찾기 = 홈 · 다른 회사로 교체됨 · 검색/자동완성/최근 본 회사 왕복 정상.
모바일 375px — 뒤로 버튼 top 235 · 회사 이름 369 · 사이드바는 아래로 · 가로 스크롤 없음.

---

## 21. 로드맵 3·4단계를 다시 짰다 (2026-08-18)

사용자 지시: **"2번은 그대로 놔둬도 될 것 같은데 판단해주고, 3번 4번을 다시 3개 정도
생각해서 바꿔줘."** 흐름은 ① 직무 찾기(기업분류까지) → ② CAS → ③ 관련 회사 → ④ 자소서.
①은 팀원, ②~④는 이 작업 담당이다.

### 21-1. 2단계는 손대지 않았다 — 넘겨주는 값만 늘렸다

CAS 화면 자체는 고칠 이유가 없었다. 상대평가 기준이 화면 막대와 같은 함수를 쓰고,
GAP 판정도 선배 데이터 실계산으로 바뀌어 있고(17-5), 2→3 갈림길도 한쪽을 막지 않는다.

비어 있던 것은 **1단계에서 고른 기업분류를 담을 자리**였다. `roadmap.js` 상태에는
직무와 회사뿐이라, 팀원이 1단계에 그 칸을 만들어도 3단계가 받을 곳이 없었다.

### 21-2. 3·4단계의 구멍은 셋이었다 (전부 코드에 있었다)

| # | 무엇 | 근거 |
|---|---|---|
| 1 | 3단계가 '고르기' 를 못 돕는다 | 계열 목록의 회사 데이터가 `{name, stock, ksic}` 뿐이라 화면에 **이름 칩 778개**만 깔렸다. 규모도 업종명도 없다 |
| 2 | 3→4 로 넘어갈 게 대부분 없다 | 담기가 채용공고 칸에만 있는데, 대기업 공채는 자사 사이트로만 올라와 워크넷·잡알리오에 안 잡힌다. 개요·뉴스는 담기 자체가 없었다 |
| 3 | 4단계 입구가 잠겨 있다 | `text.length < 30` 이면 분석을 거부한다. 그런데 공고를 못 구하는 것이 정상이라, **정상 경로로 와도 막혔다** |

### 21-3. A — 3단계 입구: 검색에서 고르기로

- `Roadmap.corpType()` / `setCorpType()` 신설. **직무와 다른 localStorage 키**에 둔다 —
  직무를 바꿔도 "대기업에 가고 싶다" 는 안 바뀌고, `read()` 가 middle 없는 상태를
  null 로 보기 때문에 같은 객체에 넣으면 직무 전에 고른 값이 저장되지 않는다.
  **1단계는 `Roadmap.setCorpType('large')` 한 줄만 부르면 된다.** null 이면 3단계는
  지금까지처럼 전체를 보여준다 — 팀원 작업 전에도 화면이 돌아가야 한다.
- `company-sectors.js` 가 회사마다 `size` 를 붙인다(company-classify 판정 그대로).
  실측 778곳 = **대기업 306 · 중견 469 · 공공 3**, 미분류 0건.
- **공공기관은 계열에 못 들어간다.** 업종코드가 상장사에만 있어서(16-9) 공공기관
  1,667곳 중 계열에 든 것은 4곳뿐이다(한국전력공사·강원랜드·한전KPS·한국가스공사).
  1단계에서 '공공' 을 고른 학생에게 4곳을 내미는 건 목록이 아니라 사고라,
  **기관 유형**(공기업 30 · 준정부 61 · 기타공공 264 · 지방공기업 422 · 지방출자출연 890)
  으로 묶어 `/api/company/public-orgs` 로 따로 낸다.
- 중소기업 칩은 **만들지 않는다.** 이 목록이 상장사 ∩ (공정위 ∪ 고용24) 라 0곳이고,
  눌러도 빈 화면이 나오는 칩은 고장으로 읽힌다. 대신 1단계에서 중소를 고른 사람에게는
  **왜 없는지와 대신 무엇을 할 수 있는지**(검색창으로 바로 열림)를 적는다.

**곁가지로 잡은 진짜 버그** — `classify('SK하이닉스')` 가 **중소기업**이었다.
공정위 명단이 음차(에스케이하이닉스)라 안 걸린 것인데, 음차 규칙을 `suggest` 만 갖고
있었다. 즉 **자동완성은 대기업이라고 하고 그걸 골라 저장하면 중소기업**이 됐다.
LG전자·KT·CJ제일제당·NAVER 도 같았다. 규칙을 `queryKeys()` 하나로 합쳤다.

### 21-4. B — 3→4 다리: 담기를 넓히되 쓰임을 밝힌다

개요(사업 내용 줄글·직원수·급여)·재무(계정별 3년 추이)·최근 이슈(기사)에 담기를 붙였다.

**예전에 같은 것을 했다가 걷어낸 적이 있다** — "담은 것들이 서로 다른 종류라 4단계에서
무엇에 쓰라는 것인지 알 수 없다" 는 이유였다. 그래서 이번엔 담을 때 **종류(kind)** 를
붙이고 종류마다 **어느 문항에 쓰는지(use)** 를 화면이 말한다.

| kind | 라벨 | 쓰는 문항 |
|---|---|---|
| `job` | 채용공고 | 직무역량 |
| `biz` | 사업 내용 | 지원동기 |
| `fact` | 회사 숫자 | 지원동기 |
| `news` | 최근 기사 | 지원동기 |

`use` 값은 `jd-coach.js` 의 `QUESTION_TYPES[].label` 과 **글자까지 같아야 한다** —
한 글자만 달라도 에러 없이 '해당 근거 없음' 으로만 보인다. 테스트가 지킨다.

**저장 자리를 `roadmap.js` 로 옮겼다.** 그전에는 company-cover 와 jd-coach 가 같은
localStorage 키를 각자 파싱했다 — 한쪽에서 모양을 바꾸면 다른 쪽은 조용히 빈 목록을 봤다.
`kind` 없는 옛 항목은 `job` 으로 읽는다(담아 둔 공고가 갱신 한 번에 사라지면, 사용자는
우리가 지운 줄 모르고 자기가 잘못한 줄 안다).

**출처를 반드시 같이 담는다.** 뉴스는 검색 API 가 언론사를 안 줘서 **주소의 호스트**를
쓴다 — 지어낸 값이 아니라 링크에 이미 있는 사실이다.

### 21-5. C — 4단계 입구: 공고 없이도 시작

`GET /api/jd/guide` 신설. 작성 기준(STAR·체크리스트·검사 사전)만 주고 **역량은 주지
않는다.** 역량은 공고에서 나오는 값이라, 없는데 "이 직무는 보통 이런 역량" 을 지어 주면
근거 없는 목록이 된다.

> **처음 제안에서 직무 트렌드(job-trends)로 채우겠다고 했는데, 확인해 보니 못 쓴다.**
> `job-trends.json` 은 워크넷 목록 API 로 만드는데 그 API 가 개인회원에게 막혀 있어
> (10-7·A3) 캐시가 **아예 없다**. 이 경로는 워크넷 키가 풀릴 때까지 비어 있다.

화면은 **역량 칸이 비는 두 경우를 가른다** — "공고를 넣었는데 못 뽑았다"(실패)와
"애초에 공고 없이 시작했다"(정상). 빈 목록 하나로 처리하면 근거를 담아 온 사람이
'실패' 화면을 보게 된다(16-5·17-5 와 같은 원칙).
문항 칸이 비어 있으면 지원동기 문항 하나를 **입력칸에 실제로 써 넣는다** — 화면에만
띄우면 고치려 할 때 어디를 고칠지 모른다.

### 21-6. 지원동기 AI 문단 초안 (사용자 결정)

`POST /api/jd/motive`. `/draft` 와 나눈 이유는 축이 달라서다 — 저기는 역량 하나를 내
경험으로 증명하는 STAR 문단이고, 여기는 "왜 이 회사인가" 다.

**지어내기를 막을 자리가 하나 더 있다.** STAR 초안에서는 '지원자가 겪지 않은 일' 이
위험이었는데, 여기서는 **회사가 하지 않은 일**이 더 위험하다 — 화면에 '사업보고서에서'
라고 적혀 있으니 학생은 맞는 줄 안다. 담아 온 근거를 '유일한 회사 사실' 로 못 박고,
근거가 없으면 **라우트가 400 으로 막는다**(지어낼 재료가 프롬프트 밖에 있어서 규칙으로는
못 막는다). 쓴 근거를 `usedEvidence` 로 돌려주어 초안 옆에 출처를 적는다.

### 21-7. 실측으로 잡은 것 셋

**① AI 초안 기능이 통째로 죽어 있었다** (내 작업 이전부터).
`.env` 의 `GROQ_MODEL` 이 `openai/gpt-oss-120b` 인데, 이 계열은 답 전에 추론 토큰을
먼저 쓰고 그 몫도 `max_tokens` 안에서 나간다. 자소서 초안처럼 프롬프트가 긴 요청에서는
**추론에서 예산을 다 쓰고 본문이 빈 채로 끝난다** → Groq 가 `HTTP 400
json_validate_failed` (failed_generation 이 빈 문자열). 화면에는 "초안을 만들지
못했어요" 로만 보여서, 몇 번을 눌러도 똑같았다.

| 같은 프롬프트·같은 모델 | 결과 |
|---|---|
| `max_completion_tokens: 1100` | 400 (본문 0자) |
| `+ reasoning_effort: 'low'` | **200 (본문 470자)** |

짧은 프롬프트에서는 그냥 되기 때문에 이 기능에서만 죽어 있었다. `ai-provider.js` 가
모델 이름으로 추론형을 가려 `reasoning_effort` 를 붙인다.

**② 종류 이름의 대괄호가 빈칸으로 읽혔다.**
프롬프트에 `- [사업 내용] 도료 및 관련제품을…` 으로 근거를 적었더니, 모델이 그 대괄호를
빈칸 표기로 읽고 초안에 `귀사는 현재 [사업 내용]을 진행하고 있으며` 라고 썼다.
**쓰라고 준 재료가 도리어 빈칸이 된 것이다.** 이 프롬프트에서 대괄호는 "여기를 비워라"
하나만 뜻해야 한다 → `사업 내용 — 도료…` 로 바꿨다.

**③ 빈칸이 0개인 문단이 나왔다.** 대괄호 상한(4개)만 걸고 하한을 안 걸었더니 그대로
제출할 수 있는 완성문이 됐다. 그건 초안이 아니라 대필이고, 이 프로젝트가 하지 않기로
한 것이다(16-2). 하한을 걸었고, 화면도 빈칸 0을 '넣었어요' 가 아니라 **"빈칸 없이
나왔어요 — 본인 사실로 바꿔 쓰세요"** 로 말한다.

### 21-8. 확인 (DB 없이 · 17-11 과 같은 하네스)

Docker 가 안 떠서 스크래치패드에 미리보기 서버를 세웠다. 이번에는 **DART·뉴스·자소서
라우터를 진짜로 마운트**했다 — 셋 다 외부 API 라 DB 가 필요 없다.

| 확인 | 결과 |
|---|---|
| 규모 필터 | 중견 469곳 · 대기업 306곳 · 공공 1,667곳 · 전체 778곳 |
| 직무 좁히기 | '백엔드 개발자' → 4개 계열 148곳 (중견만 보면 94곳) |
| 중소기업 선택 | 목록 대신 "왜 없는지 + 검색창으로 여세요" |
| 담기 | 강남제비스코 개요 2 · 재무 3 · 기사 5 곳에 버튼, 4건 담아 게이지 "사업 내용 1 · 회사 숫자 1 · 최근 기사 2" |
| 4단계 인계 | 종류별 묶음 + "지원동기 문항에 씁니다" + 출처 |
| 공고 없이 실행 | 열림. 왼쪽은 "요구 역량은 공고에서 나옵니다", 문항은 지원동기 기본값 |
| 지원동기 초안 | 담은 기사·매출 −6.9%·가동률 75.4% 를 그대로 인용, 빈칸 4개, 출처 4건 표시 |
| 모바일 375px | 가로 스크롤 없음 (칩 2줄) |

테스트 **756개** 통과 (666 → 756. roadmap +33 · company-sectors +26 · draft-coach +20).

### 21-9. 남은 것

| | |
|---|---|
| **팀원 연동** | 1단계에서 `Roadmap.setCorpType(id)` 호출. id 는 `large`/`mid`/`small`/`public` |
| **A** | `.env` 의 `GROQ_MODEL` — 지금 값(`openai/gpt-oss-120b`)은 `reasoning_effort` 로 살렸지만, 추론 없는 모델로 바꾸면 더 빠르다 |
| B17 | 사람인 키가 나오면 3단계에 '채용 중' 배지 (자리는 그대로 비어 있다) |
| B21 | 워크넷 키가 풀리면 `job-trends` 로 공고 없는 4단계에 "이 직무 공고 N건 중 M%" 를 얹는다 |
| B22 | 중소기업 목록 — 지금은 검색으로만 닿는다. DART 색인은 11만 곳이라 리포트는 나오는데 **둘러볼 목록**이 없다 |

### 21-10. 산업 선택 · 개요 담기 정리 (같은 날, 사용자 지시)

> "개요에서 직원수, 평균 근속연수, 1인평균 급여는 담기 없애줘. 그리고 회사 찾기에서
>  산업 선택이 없는데 이거 추가해 줘야돼 — dart나 임금직업포털이나 채용공고에
>  나오는 걸로 너가 확인해서 해줘."

**개요의 숫자 세 칸에서 담기를 뺐다.** 이건 **회사를 고를 때 보는 값**이지 자소서에
인용할 말이 아니다 — "직원 627명인 회사에 지원합니다" 는 지원동기가 되지 않는다.
안 쓰게 된 `id`·`text` 필드도 같이 지웠다(남겨 두면 담기가 있던 자리로 읽힌다).
담을 만한 숫자는 재무 칸의 매출·영업이익 추이 쪽이고, 담기는 거기 있다.

#### 산업 축을 주는 데이터는 DART 하나뿐이다 (세 군데 다 열어 확인)

| 출처 | 실제로 들어 있는 축 | 산업 분류? |
|---|---|---|
| DART 기업개황 | `induty_code` = KSIC 5자리 | **있음** |
| 임금직업정보시스템 | 직업 10대분류 → 중분류 → 직업 461개 | 없음 (직업 축) |
| 잡알리오 공고 532건 | `ncsCdNmLst` = NCS 직무 · `instNm` = 기관명 | 없음 (직무 축) |

그래서 분류를 새로 만들 필요는 없었다 — 지금 계열이 이미 KSIC 로 만든 것이다.
실측으로 회사 778곳이 **KSIC 중분류 55종**에 걸쳐 있고 계열이 그것을 빠짐없이 덮는다.

#### 검증하다 잡은 것 — 금융·보험의 절반이 지주회사였다

KSIC 는 지주회사를 **금융업(64992)** 으로 분류한다. 통계 분류로는 맞지만, 그대로 두면
'금융·보험' **68곳 중 33곳이 지주회사**다. 금융을 고른 학생에게 농심홀딩스·하림지주·
크라운해태홀딩스(식품) · 노루홀딩스(도료) · 코오롱(화학) · 한진칼(항공) · 롯데지주(유통)가
나온다. 순수 금융사는 29곳뿐이었다.

훑어보는 목록일 때는 칩 몇 개 섞인 정도였는데, **산업이 '고르는' 축이 되는 순간
화면이 거짓말을 한다.** `지주회사` 를 계열로 따로 세웠다(35 + 33 = 68, 총합은 그대로).

**코드로 아는 것과 이름으로 짐작하는 것을 갈랐다.**
- `64992` (29곳) → 코드가 지주회사라고 말한다. 그대로 쓴다.
- `649` 3자리만 온 6곳 → '기타 금융업' 이라 코드로는 알 수 없다. 실제로 그 안에
  미래에셋벤처투자(벤처캐피탈)가 섞여 있다. **이 6곳에 한해서만** 이름이 홀딩스·지주로
  끝나는지 본다(4곳).
- 이름 규칙을 전체에 훑지 않는 것이 요점이다. '효성' 처럼 이름에 흔적이 없는 지주회사는
  금융·보험에 남지만, 그건 우리가 **모르는 것이라 모른 채로 두는 편이 맞다.**

화면에도 적는다 — "지주회사는 업종이 아니라 회사의 형태이고, 실제 사업은 자회사에 있다".

#### 아코디언을 걷어내고 '고르기' 로 바꿨다

예전에는 계열 15개가 아코디언으로 **전부 깔려 있었다.** 그건 훑어보기지 고르기가 아니다 —
지금 어느 산업을 보는 중인지가 화면 어디에도 없었고, 흐름은 "산업 선택 후 회사 고르기"
인데 선택이라는 동작 자체가 없었다.

- 산업 칩 한 줄 → 고른 산업의 회사만 아래에 편다(24곳까지, 나머지는 '더 보기').
- **직무로 좁혀진 계열은 앞으로 당기고 ★ 표시만 붙인다.** 자동으로 골라 주지 않는다 —
  보통 서너 개가 남는데(백엔드 개발자 → 4개) 하나를 우리가 찍으면 나머지를 학생이
  못 본 채로 넘어간다.
- 칩의 곳수는 **규모 필터를 반영한다.** 전체 곳수를 적어 두고 눌렀을 때 절반만 나오면
  숫자를 믿을 수 없게 된다(대기업만 → IT·소프트웨어 42 → 18).
- 규모를 바꾸면 산업 선택을 **푼다.** 안 그러면 "산업은 골랐는데 아무것도 없는" 화면이 된다.
- 공공기관 모드는 산업 축이 없으므로 기관 유형 레인을 그대로 쓴다.
- 최근 본 회사·많이 찾는 회사는 **아래로 내렸다** — 지름길이라 위에 두면 목록을
  고르러 온 사람이 아는 회사 8곳만 보고 나간다.

**확인 (정식 서버 · MySQL):** 산업 칩 16개 · 백엔드 개발자 추천 4개(반도체·IT·전자·통신)
앞에 정렬 · IT·소프트웨어 42곳 → 더 보기 42 · 지주회사 33곳에 안내문 · 대기업 필터에서
곳수 재계산 및 산업 선택 해제 · 공공기관은 기관 유형 5레인 · 개요 숫자칸 담기 0개 ·
재무 3개(fact) · 이슈 5개(news) · 사업 내용 2개(biz) · 375px 가로 스크롤 없음.

테스트 **768개** 통과 (company-sectors +12).

---

## 22. 온통청년 키가 들어왔다 — 그리고 코드가 세 군데 틀려 있었다 (2026-08-18)

19-6 의 A 항목(`YOUTH_API_KEY`)이 승인됐다. 키를 넣고 실호출해 보니 **세 군데가
틀려 있었다.** 19-6-1 과 같은 부류다 — 명세서만 보고 짠 코드는 열쇠가 오기 전까지
틀린 줄 모른다. 이번에는 셋 다 **에러가 아니라 조용히 틀리는** 모양이었다.

### 22-1. 첫 호출은 0건이었다

키는 정상이었다(키워드 없이 부르면 정책 **2,715건**). 그런데 코드가 부르는 방식으로는
공모전 0건 · 대외활동 0건이었고, `resultCode` 는 200 이라 에러도 안 났다.

| # | 무엇이 틀렸나 | 실측 |
|---|---|---|
| 1 | 검색 파라미터가 `plcyKywdNm` 이었다 | `plcyKywdNm` 은 자유검색이 아니라 **정해진 어휘 17종**의 필드다 |
| 2 | 마감 지난 모집을 그대로 실었다 | 2025년에 끝난 공모전이 목록에 남아 있다 |
| 3 | 신청기간 구분코드를 **거꾸로** 읽었다 | `0057001` 을 '상시' 로 적어 뒀는데 정반대다 |

### 22-2. plcyKywdNm 은 어휘가 정해진 필드였다

정책 2,715건을 훑어 실제 어휘를 확인했다 — **17종뿐**이다.

```
교육지원 902 · 보조금 752 · 맞춤형상담서비스 372 · 주거지원 234 · 장기미취업청년 232
인턴 215 · 중소기업 205 · 바우처 143 · 벤처 134 · 해외진출 76 · 대출 58 · 출산 58
금리혜택 56 · 육아 55 · 청년가장 36 · 공공임대주택 25 · 신용회복 13
```

'공모전'·'서포터즈' 는 **아예 없는 말**이다. 정책 **이름**으로 찾는 파라미터가 따로
있었다 — `plcyNm`.

```
plcyNm=서포터즈      → 17건
plcyKywdNm=서포터즈  →  0건
```

> **함정이 하나 더 있다.** 모르는 파라미터는 **무시되고 전체 2,715건이 그대로 온다**
> (`srchWord`·`searchWord`·`pblancNm` 전부 2,715건). 즉 파라미터 이름이 틀리면
> 0건이 아니라 **'전부'** 가 올 수도 있다. 둘 다 화면만 봐서는 못 잡는 모양이라
> 테스트가 소스의 파라미터 이름을 직접 확인한다.

### 22-3. 끝난 모집은 서버에서 뺀다

화면은 지난 것에 '마감 지남' 배지를 붙일 뿐 지우지 않는다. 그대로 내보내면 목록의
절반이 지난 공고가 되는데, 이 화면은 **지금 지원할 수 있는 것**을 보러 오는 곳이다.

**마감일을 모르는 것은 남긴다.** 상시이거나 기간을 안 적은 정책이 있어서
(실측: '청년정책 서포터즈 2기' 는 `aplyYmd` 가 비어 있다), 모른다고 지우면 열려 있는
모집이 사라진다. 아는 것만 판정한다.

### 22-4. 신청기간 구분코드가 정반대였다

정책 1,400건을 훑어 확인했다:

| 코드 | 건수 | 실제 뜻 |
|---|---|---|
| `0057001` | 738 | **전부 `aplyYmd` 가 있다** = 기간이 정해진 모집 |
| `0057002` | 437 | 기간 없음 = **상시** (응시료 지원·면접정장 대여처럼 늘 열린 것) |
| `0057003` | 225 | 기간 없음 = 기타 — 신청방법이 '별도 문의' 인 것이 대부분 |

코드는 `0057001` 을 '상시' 로 적고 있었다. 화면이 `endDate` 를 먼저 보기 때문에
(specup.js actCard) 그 오표기는 가려졌지만, 대신 **상시 배지가 아무 데도 안 떴다** —
진짜 상시인 `0057002` 가 null 로 떨어져 '기간 미상' 으로 나갔다.

`0057003` 은 '상시' 라고 말하지 않는다. 기간을 안 적었을 뿐 늘 열려 있다는 뜻이
아니라서, 아는 만큼만 적고 나머지는 '기간 미상' 으로 둔다.

### 22-5. 데이터가 들어오면서 드러난 화면 결함

표지 태그와 배지가 **같은 배열을 읽어서 첫 키워드가 두 번 찍혔다**
(`장기미취업청년 / 장기미취업청년 / 보조금`). 빈 목록일 때는 보이지 않던 자리다.
표지에 올린 것은 배지에서 뺐다.

> **How to apply** — 키가 없어 늘 비어 있던 화면은 **키가 들어오는 날 처음 렌더링된다.**
> 그날 나오는 것은 API 버그만이 아니라 그동안 아무도 못 본 화면 코드다.

### 22-6. 이 API 로 무엇이 들어오는지 (기대치를 정확히 적어 둔다)

**민간 공모전은 안 들어온다.** 온통청년은 **청년정책** 목록이라, 잡히는 것은
지자체·공공기관이 여는 것들이다(광주 자치경찰 서포터즈, 울산 청년정책 제안 경연대회,
의성군 청년개발자 해커톤…). 삼성·현대 같은 기업 공모전은 여기 없다 —
그런 것만 모아 주는 전국 단위 공개 API 는 없다(조사 결과는 `docs/외부API-연동구조.md`,
다시 조사하지 말 것).

**확인 (정식 서버 · MySQL · 실호출):**

| 탭 | 결과 |
|---|---|
| 공모전·대회 | 5건 — 울산 청년정책 제안 경연대회 D-43 · 청년정책제안 아이디어 공모 D-135 · 의성군 청년개발자 해커톤 |
| 대외활동·서포터즈 | 22건 — 광주자치경찰 서포터즈 D-5 · 청년 생명사랑 서포터즈 D-104 |
| 마감 지난 것 | 목록에서 빠짐 (2025년 건 전부) |
| 키워드 칩 | 중복 사라짐 |

테스트 **774개** 통과 (specup 56 → 62).

### 22-7. 지역을 표지에 올렸다 (사용자 지시) · 이미지는 없다

**이미지 필드는 없다.** 응답 60개 필드를 전부 훑었는데 `img`·`thumb`·`photo`·`file`
류가 하나도 없다. 파일이 있는 정책도 "☞ 자세한 내용은 붙임파일을 확인해주시기
바랍니다"(`sbmsnDcmntCn`)라고 **글로만** 적혀 있고 첨부 URL 은 안 준다.
카드 표지가 이모지 하나인 것은 그래서다 — 넣을 그림이 애초에 없다.

**지역은 `zipCd` 에서 뽑는다.** 후보 셋을 다 재 보고 골랐다:

| 후보 | 문제 |
|---|---|
| `sprvsnInstCdNm` 주관기관 | 늘 차 있지만 **부서명만 오는 일이 잦다** — '복지국'·'지역경제과'·'정보통계담당관' |
| `rgtrHghrkInstCdNm` 최상위등록기관 | 시·도 이름이 잘 오지만 **비는 것이 있고**, 중앙부처('중소벤처기업부')는 지역이 아니다 |
| `zipCd` | **1,300건 전부 차 있다.** 지원 대상 지역을 법정동코드 5자리 목록으로 준다 |

코드↔시도 대응은 **추측하지 않고 데이터에서 뽑았다** — 한 시도만 대상인 정책의
zipCd 앞 2자리와 최상위등록기관을 짝지어 셌다:

```
11 서울(8) · 12 광주시청(124)·전남광주통합특별시(59)·전라남도(20) · 27 대구(1)
28 인천(160) · 30 대전(7) · 31 울산(102) · 36 행정중심복합도시건설청(2)
41 경기(69) · 44 충남(90) · 47 경북(28) · 48 경남(28) · 50 제주(28)
51 강원특별자치도(9) · 52 전북특별자치도(55)
```

법정동코드 체계와 맞는데 **`12` 만 표준에 없다** — '전남광주통합특별시' 라는 통합
광역단체 코드로, 명세서에는 없고 실측으로만 알 수 있었다. 관측 안 된 코드
(부산 26·충북 43 등)는 표준값으로 채우되 **어느 쪽인지 표에 구분해 적어 뒀다.**

**전국이냐 지역이냐로 갈린다.** 걸치는 시도 수가 **1개 1,211건 · 2~8개 0건 ·
9개 이상 89건** 이라 중간이 없다. 9개 이상이면 '전국' 이라고 적는다.

표지 태그를 키워드에서 **지역**으로 바꿨다. 키워드 첫 개('보조금'·'장기미취업청년')는
정책 분류라 공모전 카드에서 읽을 것이 못 됐고, 배지와 같은 배열이라 중복까지 났다.
지역을 모르면 예전처럼 키워드로 물러선다.

**확인:** 대외활동 22건 — 광주·전남 D-5 · 울산 D-104 · 충남 D-135.
공모전 5건 — 울산 · 충남 · **전국**(의성군 청년개발자 해커톤) · 경남.

### 22-8. 남은 것

| | |
|---|---|
| 지역 편중 | 잡히는 정책이 광주·울산·인천에 몰려 있다. 지자체가 온통청년에 얼마나 올리느냐에 달린 것이라 우리가 고칠 수 있는 값이 아니다. 지금은 **표지에 지역을 적어** 학생이 바로 거를 수 있게 해 뒀다 |
| 지역 필터 | 표지에 적기만 했지 **거르지는 못한다.** 내 지역만 보는 필터는 아직 없다 |
| 호출 수 | 키워드 하나당 1회라 탭 한 번에 6~7회 부른다. 1시간 캐시(`SPECUP_YOUTH_TTL_MS`)로 덮고 있지만, 조사 중 연속 호출에서 **HTML 이 돌아오는 것**을 한 번 봤다(레이트 리밋 추정) |
| '대외활동'·'홍보단'·'멘토링단' | 지금은 0건이다. 데이터가 늘면 잡히므로 키워드는 남겨 뒀다 |

---

## 23. 홈의 커리어 인사이트 카드 — 사진과 글을 채웠다 (2026-08-18)

사용자 지시: **"메인페이지 커리어 인사이트 카드칸에 이 사진을 채워줘. 그리고 이 내용
맞게 커리어 인사이트에 내용 적어서 들어갈 수 있게 해줘."** 사진 5장을 받았다.

### 23-1. 예전 카드는 누를 수 없었고, 숫자가 지어낸 값이었다

`careerly.html` 에 제목 네 개가 박혀 있었고 **onclick 이 없었다** — 읽을 글이 애초에
없었기 때문이다. 게다가 제목에 이런 것이 들어 있었다:

```
합격자 71%가 경험한 인턴십, 언제 시작할까
TOEIC 900 vs 850, 실제 합격선은 어디일까
공모전 수상이 CAS 점수에 미치는 영향
```

**출처가 없는 수치다.** 화면을 채우려고 넣은 값인데 학생은 그걸 사실로 읽고 자소서에
쓴다. 새 글에는 그런 숫자를 넣지 않았다 — 대신 **어느 화면에서 자기 숫자를 확인하는지**
를 알려준다. 선배 표본은 직무마다 다르고 계속 바뀌므로, 고정된 글에 박아 두면 그날부터
틀리기 시작한다(6장·9장과 같은 원칙).

### 23-2. 사진은 줄여서 저장소에 넣었다

받은 원본이 0.3~3.7MB, 합계 **10.6MB** 였다. 카드 커버는 250×120px 이라 그대로 쓰면
홈을 여는 사람이 10MB 를 받는다. ffmpeg 으로 가로 800px 로 줄였다 — 합계 **192KB**.

| 파일 | 사진 | 글 |
|---|---|---|
| `neon.jpg` | DO SOMETHING GREAT | 무엇부터 해야 할지 모를 때 — 커리어 로드맵 4단계 |
| `books.jpg` | 쌓인 교재 | 자격증은 몇 개가 아니라 어느 것인가 |
| `trophy.jpg` | 트로피 | 공모전 수상, 자소서에서 실제로 쓰이는 부분 |
| `suit.jpg` | 정장 | 지원 전에 회사를 읽는 순서 |
| `coding.jpg` | 개발 데스크 | 직무 역량은 공고에서 나옵니다 |

### 23-3. 글은 게시판에 진짜로 들어간다 (사용자 결정)

커리어 인사이트(`#insight`)는 사용자 커뮤니티 게시판이다. 편집 글도 거기에 '취업정보'
카테고리로 넣었다 — 카드를 누르면 그 글이 열리고 댓글도 달린다.

| 무엇 | 어디 |
|---|---|
| 글 정의(제목·본문·커버·소요시간) | `src/insight-featured.js` — **단일 출처** |
| 게시판에 넣기 | `scripts/seed-insights.js` (재실행 안전 · `--force` 로 본문 갱신) |
| 홈에 내려보내기 | `GET /api/insights/featured` |
| 그리기 | `home.js paintArticles()` |

**정의를 한 파일에 모은 이유** — 같은 글이 세 군데에서 필요하다. 한쪽에서 제목을
고치면 **홈 카드가 가리키는 글과 실제 글이 어긋나는데, 그것도 에러 없이 조용히** 그렇게 된다.

**글쓴이 계정 `croad`** 를 만든다. 편집 글이라 사람 이름이 아니라 서비스 이름으로 나가야
한다. **비밀번호를 넣지 않는다**(`password_hash NULL`) — 로그인 경로가 비밀번호를 요구하므로
이 계정으로는 로그인할 수 없고, 글쓴이 이름만 빌려주는 자리다.

**제목이 키다.** `insight_posts` 에 '어느 편집 글인가' 를 적을 칸이 없어서(스키마를 늘리지
않았다) 제목으로 찾는다. 제목을 바꾸면 새 글이 하나 더 생기므로 `--force` 로 다시 돌린다.

**목록 API 를 안 쓰는 이유** — `/api/insights` 는 최신순이라 누가 글을 쓰면 편집 글이
밀려난다. 커버 사진이 글마다 짝지어져 있어서 순서가 바뀌면 **사진과 제목이 어긋난다.**

### 23-4. 안 될 때 어떻게 되는지

| 상황 | 화면 |
|---|---|
| 시드를 안 돌린 DB | 카드는 나오되 누르면 **인사이트 목록**으로 간다(`postId: null`). 반응 없는 카드는 고장으로 읽힌다 |
| 서버가 죽었거나 응답 실패 | 카드 칸을 **통째로 감춘다**. 제목만 있고 안 열리는 카드를 보여 주느니 없는 편이 낫다 |

글을 여는 것은 `careerly_insight_open` 키로 넘긴다(회사 찾기의 `careerly_company_open`
과 같은 규약). **키는 한 번 쓰고 지운다** — 남기면 다음에 인사이트를 눌러도 같은 글이
열려서 목록으로 돌아갈 수 없는 것처럼 보인다(20-11 에서 실제로 겪은 문제와 같다).

### 23-5. 확인 — 그리고 이 환경에서 못 본 것

`/api/insights/featured` 5편 전부 `postId` 있음 · 이미지 200 응답 · 카드 5장 ·
커버 250×120 · 이미지 5장 모두 800×534 로 로드 · 4번째 카드 클릭 → '지원 전에 회사를
읽는 순서' 본문 열림 · 핸드오프 키 소멸 · 다시 들어가면 목록 · 375px 가로 스크롤 없음.

> **미리보기 패널에서는 눈으로 못 봤다.** `window.innerWidth` 가 0 이고
> **IntersectionObserver 가 한 번도 fire 하지 않는다**(초기 콜백조차 없다). 그래서
> 홈의 등장 효과(`.rv` → `.rv-in`)가 안 걸려 `home-sec` 두 개가 opacity 0 으로 남는다 —
> **내가 만든 섹션만이 아니라 손대지 않은 섹션도 같다.** 스크린샷이 안 되는 것과 같은
> 원인(패널이 렌더링을 안 한다)이라 코드 문제로 보지 않았고, `reveal-off` 로 모션을
> 끈 상태에서 레이아웃·이미지·링크를 측정해 확인했다.
> **실제 브라우저에서 홈을 한 번 열어 눈으로 봐야 하는 항목이 남아 있다.**

### 23-6. 남은 것

| | |
|---|---|
| 눈으로 확인 | 실제 브라우저에서 홈 → 커리어 인사이트 칸 (등장 효과 포함) |
| 배포 | 새 DB 마다 `node scripts/seed-insights.js` 를 한 번 돌려야 글이 생긴다. 배포 절차에 넣을지 검토 |
| 그리드 | 5장이라 `auto-fit minmax(210px)` 로 바꿨다. 글을 더 늘리면 줄이 어떻게 접히는지 다시 볼 것 |

### 23-7. 본문을 마크다운으로 (사용자 지시, 같은 날)

> "커리어 인사이트 내용은 마크다운으로 적을 수 있게 할 수 있어? 그리고 맞게 고쳐줘"

게시판 본문 전체가 마크다운으로 렌더링된다. 편집 글 5편도 마크다운으로 다시 적었다.

#### 라이브러리를 안 넣었다

프론트에 빌드가 없고 CDN 도 안 쓴다(`careerly.html` 이 스크립트를 직접 나열한다).
marked·markdown-it 을 넣으려면 번들러나 외부 호스트가 필요한데, 우리가 쓰는 문법은
열 가지 남짓이라 값이 안 맞는다. `frontend/js/markdown.js` 에 직접 썼다(약 140줄).

지원: `## 제목` · `### 소제목` · `**굵게**` · `` `코드` `` · `- 목록` · `1. 목록` ·
`> 인용` · `---` · `[글자](https://…)` · 빈 줄 = 문단.
**`#` 하나는 안 받는다** — 글 제목이 이미 h1 이라 h2 를 내주면 문서에 h1·h2 가 둘씩 생긴다.

#### 이 파일의 유일한 위험은 XSS 다

본문은 **사용자가 쓴 글**이다. 마크다운을 HTML 로 바꾸는 일은 곧 "글을 HTML 로
실행한다" 는 뜻이라, 한 군데만 새도 남의 글에 `<script>` 를 넣을 수 있다.
순서를 못 박았다:

1. **통째로 escape** 한다 (`<script>` → `&lt;script&gt;`)
2. **그 다음에** 우리가 아는 문법만 태그로 바꾼다

거꾸로 하면 우리가 만든 태그까지 깨지고, escape 를 건너뛰면 글이 그대로 실행된다
(jd-coach.js 의 `bold()` 가 같은 순서를 쓴다). 그 대가로 **인용은 `>` 가 아니라
`&gt;` 로 찾아야 한다** — 원문 기호로 찾으면 인용이 통째로 안 걸린다.

링크는 **http/https 만** 받는다. `javascript:` · `data:text/html` 은 클릭 한 번으로
실행되는 주소다. 막을 때 **링크만 없애고 글자는 남긴다** — 통째로 지우면 사용자가
쓴 말이 사라진다.

코드(`` ` ``)를 가장 먼저 빼내 자리표시자로 바꾼다. 코드 안의 별표는 굵게가 아니라 글자다.

`test/markdown.test.js` **56개** — 절반이 XSS 다. 실제 화면에서도 DOM 에 넣어
확인했다: script·img 0개 · `javascript:` 링크 안 만들어짐(글자만 남음) ·
`window.__pwned` 오염 없음 · 쿼리스트링 있는 정상 주소는 그대로 통과.

> **테스트가 틀렸던 것 하나** — "여러 줄에 걸친 별표는 굵게가 아니다" 로 적었는데
> 마크다운 규격상 **한 문단 안의 줄바꿈을 넘는 굵게는 정상**이다. 막아야 하는 것은
> 문단 경계(빈 줄)를 넘는 경우다. 기대를 고쳤다 — 코드가 아니라 테스트가 틀렸다.

#### 화면

- `white-space: pre-wrap` 을 **끈다**(`.insight-md`). 마크다운이 줄바꿈을 `<p>`·`<br>`
  로 이미 만들어 두므로, 그대로 두면 태그 사이의 줄바꿈까지 살아나 문단마다 빈 줄이
  하나씩 더 생긴다.
- 글쓰기 폼 아래에 **쓸 수 있는 문법을 적어 뒀다.** 마크다운이 되는지 모르면 아무도
  안 쓰고, 모르고 쓴 별표가 굵게로 바뀌면 그것대로 놀란다.

**확인:** 「지원 전에 회사를 읽는 순서」 본문 — h3 3 · h4 4 · 문단 8 · 목록 4 ·
인용 1 · 구분선 1 · 굵게 8 · **별표 잔여 없음** · 글쓰기 폼 안내문 표시.

테스트 **838개** 통과 (markdown +56).

---

## 24. 회사 찾기를 '업종 단계 고르기' 로 바꿨다 (2026-08-21)

커밋 셋: `fa904bc`(적합도) · `ff43a12`(업종 트리) · `8bc010b`(리포트 업종명).

### 24-1. 계열 16개를 한 판에 늘어놓던 것이 벽이었다

3단계(#company)는 계열 16개를 버튼으로 깔아 두고 고르면 그 아래 회사를 전부 보여줬다.
문제는 **한 칸의 두께**다 — '화학·소재' 하나에 144곳이 들어간다. 학생은 그 목록에서
고르는 게 아니라 그냥 스크롤을 포기한다.

계열을 더 잘게 쪼개는 것으로는 안 된다. 계열은 KSIC 중분류를 묶은 것이라 잘게 쪼갤수록
**통계 분류 냄새**가 진해진다(21-10 에서 이미 겪었다). 축 자체를 바꿔야 했다.

### 24-2. 화면 — 한 번에 한 단계만 연다

```
[기업 형태 필터]  전체 · 대기업 306 · 중견기업 469 · 공공기관 1,667
   1 제조·화학 › 2 반도체·전자부품 › 3 회사
   ✓ 업종 대분류                              제조·화학
   ✓ 업종                                반도체·전자부품
   3 회사   ← 열림: 광전자 중견 · 두산 대기업 · 두산테스나 대기업 …
```

고르면 접히면서 다음 단이 열린다. **접힌 줄에 고른 값이 남아** 되짚으려고 펼칠 필요가
없다. 접힌 헤더를 누르면 그 단계로 돌아간다.

**기업 형태는 단계가 아니다.** 위에 걸린 필터로 뺐다 — 목록 전체를 바꾸는 스위치라
단계에 끼우면 되돌리기가 어렵다(1단계에서 '대기업' 을 골라 놓고 마지막에 마음을 바꾸면
처음부터 다시 해야 한다). 필터를 바꾸면 **남을 수 있는 데까지만** 경로를 되감는다 —
중견만 있는 업종에서 대기업을 누르면 제조·화학은 유지하고 계측·광학기기만 푼다.

**선택지가 하나뿐인 단계는 건너뛴다.** 진행 표시에 점선 + `(자동)` 으로 남긴다 —
없앤 게 아니라 고를 것이 없었다는 뜻이라, 안 보이면 사용자가 단계를 놓쳤다고 여긴다.

**마지막 단은 체크박스가 아니라 회사 칩 한 번 누르기다.** 프로토타입은 복수 선택이었는데
이 화면의 결론은 후보 목록이 아니라 **회사 한 곳의 리포트**다(로드맵 3단계 = 지원할 회사
하나 정하기). 후보 담기를 넣으려면 `roadmap.js` 가 목록을 들어야 해서 이번 범위에서 뺐다.

### 24-3. KSIC 를 버리지 않는다 — 열쇠로 쓰고 이름만 바꾼다

`backend/src/job-industry.js` (새 파일, 279줄). KSIC 는 **통계 분류**라 취업 시장의 말과
다르다:

| 회사 | KSIC 가 부르는 이름 | 학생이 찾는 말 |
|---|---|---|
| 넷마블·크래프톤 | 출판업 (58) | 게임 |
| 아모레퍼시픽 | 기타 화학제품 제조업 (204) | 화장품 |
| 지주회사들 | 기타 금융업 (64992) | 지주회사 |

그렇다고 **회사 이름만 보고 업종을 찍으면 근거가 없다.** 업종코드는 회사가 금융감독원에
신고한 값이라 우리가 가진 유일한 객관적 근거다. 그래서 **코드는 그대로 열쇠로 쓰고 화면에
나가는 이름만 바꾼다.** 대분류 10 · 중분류는 분류표 순서(지원자가 많은 쪽이 앞)를 따른다.

> **덤으로 고쳐진 것** — DART 식료품 코드가 한 칸씩 밀려 있어(104·105·106·108·109)
> KSIC 이름을 그대로 붙이면 농심이 '동물용 사료' 가 됐다. 취업 업종은 그보다 거칠어서
> **밀린 코드도 같은 칸(식품·음료)에 떨어진다.** 틀릴 자리가 사라졌다.

`company-sectors.js` 의 회사 레코드에 **원본 5자리 `code` 를 같이 남겼다.** 취업 업종은
5자리 규칙을 쓴다 — 204(기타 화학제품) 안에서 2042 만 '화장품' 으로 빠지는 식이라,
2자리로 줄인 `ksic` 만으로는 못 가른다.

**분류 실패는 조용히 넘기지 않고 던진다.** 목록에서 사라진 회사는 아무도 못 찾고 왜 없는지도
알 수 없다. 실측 778곳 전부 분류된다.

### 24-4. 깊이는 데이터가 정한다 — 민간 3단, 공공 4단

민간은 대분류 → 업종 → 회사. 공공은 대분류 → 기관 유형 → **소관·지역** → 기관이다.
공공기관 1,667곳을 유형 5개로만 나누면 **지방출자출연기관 한 칸에 890곳**이 들어간다.
중앙은 소관부처가, 지방은 시·도가 지원자가 실제로 쓰는 축이라 그것으로 한 단 더 나눴다.

**화면은 단계를 세지 않는다** — 배열(회사 목록)을 만나면 거기가 끝이다. 단계 이름도
갈래를 따라 바뀐다(업종 ↔ 기관 유형·소관·지역).

공공기관을 계열 목록처럼 따로 두지 않은 이유: 계열에는 업종코드가 없어 4곳밖에 못 들어간다
(18장·`publicOrgs` 주석). 여기서는 **'기관·공공' 이 대분류 하나**로 들어가고 그 아래를
기관 유형으로 나눠서 축이 어긋나지 않는다.

### 24-5. 직무 추천을 새 축으로 옮겼다 — 이름 예외는 근거에서 뺀다

로드맵 1단계에서 직무를 고르고 오면 "이 직무를 주로 뽑는 업종" 에 별표가 붙는다
(건설 직무 → 건설·건축·인테리어·토목·부동산·에너지). `industryFocus()` 가 기존
`sectorFocus()` 의 답(계열/KSIC 대분류)을 새 축으로 옮긴다 — **표를 하나 더 손으로 만들면
계열 표와 어긋난다.** 회사 데이터로 직접 센다.

**이름으로 옮긴 예외 회사는 근거(`codesOf`)에서 뺀다.** 그 회사의 업종코드는 지금 사업과
어긋나서 옮긴 것이라 대표로 세우면 엉뚱한 추천이 붙는다 — 실측: 네패스아크를 반도체로
옮겨 뒀더니 **그 업종코드가 들어간 직무마다 반도체가 딸려왔다.**

목록을 잘라 보내지는 않는다. 좁힌 업종 밖에도 지원할 회사가 있다(21-10 과 같은 판단).

### 24-6. 리포트 머리에 '업종코드 212' 가 그대로 나오고 있었다

새 화면을 확인하다 발견했다. 방금 화면에서 걷어낸 바로 그것이 리포트에는 남아 있었다.
`backend/src/ksic.js` (새 파일) + `scripts/fetch-ksic.js` 로 이름표를 만들어 붙였다.

| 회사 | 전 | 후 |
|---|---|---|
| 유한양행 | 업종코드 212 | 제약 |
| 삼성전자 | 업종코드 264 | 반도체·전자부품 |
| 셀트리온 | 업종코드 21100 | 바이오·진단 |
| 한빛네트(목록 밖) | 업종코드 58221 | 소프트웨어·솔루션 |

순서는 ① 취업 업종 → ② KSIC 공식 명칭 → ③ **null**. 우리 회사 목록은 778곳인데 DART
색인은 11만 곳이라 목록 밖 회사도 리포트는 열린다. 둘 다 없으면 화면이 '—' 로 적는다 —
**지어내지 않는다.**

**이름표는 받아서 쓴다.** 중분류 55개는 손으로 적을 수 있지만 소분류 232개·세분류 495개는
못 적고, 틀리게 적으면 학생은 그걸 공식 업종명으로 믿는다. 출처는 통계청 KSIC 코드집
(FinanceData/KSIC), 받은 뒤 우리 회사 데이터로 교차검증한다.

### 24-7. 이 과정에서 잡은 버그 — `삼성전자(주)` 가 예외표를 빗나갔다

DART 기업개황은 회사명을 `삼성전자(주)` 로 주는데 이름 예외표는 `삼성전자` 로 돼 있었다.
그래서 **리포트에서만** 예외가 조용히 빗나가 삼성전자가 '컴퓨터·통신장비' 로 나왔다.
화면(트리)은 우리 회사 데이터를 쓰니까 멀쩡했다 — **같은 함수가 들어오는 문에 따라 다르게
동작한 것**이다. `company-sectors.js` 의 법인격 표기 정규화(`norm`)를 같이 쓰도록 맞췄다.

### 24-8. 적합도 — 스펙을 바꿔도 옛 점수가 남던 것 (같은 날)

`cas-fit.js` 의 캐시 키가 **직업 코드뿐**이라, 스펙을 새로 넣어도 직업이 그대로면 다시
부르지 않았다. 스펙이 없던 시절의 '아직 근거 없음' 이 계속 떴다 — 저장은 됐는데 화면만
안 바뀌니 사용자에게는 **채점이 고장난 것으로 보인다.** 스펙 지문(`DB.specFingerprint()`)을
키에 넣어 둘 중 하나만 바뀌어도 부른다. 지문에 넣는 값은 **casFit 이 서버로 실제로 보내는
필드 그대로**다 — 보내는 필드를 늘리면 이 함수도 같이 고쳐야 한다.

AI 실패도 무슨 오류든 "잠시 후 다시 시도해 주세요" 한 줄로 나갔다. 실제로 가장 많은 원인은
**Groq 무료 쿼터 초과(429)** 라 그 말이 맞지만(업무특성 35항목 프롬프트에 max_tokens 4000
이라 분당 토큰 한도를 금방 친다), 키 오타나 폐기된 모델(503)은 몇 번을 눌러도 같다.
`ai-provider` 가 원인별로 갈라 둔 문장을 그대로 올리고 상태코드도 같이 준다.
**다시 눌러 되는 실패(429·502·504)에만 버튼을 만든다** — 되지도 않는 버튼을 주면 사용자가
원인을 찾는 대신 계속 누른다. 자세한 것은 `docs/CAS-직무적합도-채점과-AI.md`.

### 24-9. 확인 (정식 서버 · MySQL)

Docker 엔진이 꺼져 있어 컨테이너가 `Exited 255` 였다. **`docker start careerly-mysql`
한 줄이면 된다** — 새로 만들 필요 없다(테이블 16개 그대로).

- `GET /api/company/industry-tree` → 200 · 108KB · 340ms
- 민간 3단 / 공공 4단, 단계 이름이 갈래에 따라 바뀜 · 접힌 헤더로 되돌아가기
- 규모 필터: 중견만 있는 업종에서 대기업 → 남을 수 있는 데까지만 되감김
- 민간→공공 전환 시 경로가 풀리고 '기관·공공' 자동 선택
- 회사 칩 → 기업 리포트 전환 (유한양행·삼성전자, classify·business·analysis 전부 200)
- 규모 필터가 켜지면 칩의 규모 배지가 사라짐(전부 같은 값이라 잡음)
- 콘솔 에러 0 · 테스트 **874개** 통과 (job-industry +36)

### 24-10. 남은 것

- ~~죽은 라우트~~ — **지웠다**(아래).
- **후보 담기** — 마지막 단 복수 선택. `roadmap.js` 가 목록을 들어야 한다.
- `nodemon` 포트 충돌 → 24-11 에서 처리했다.

### 24-12. 죽은 라우트를 지웠다 (같은 날)

새 트리가 대신하면서 `/api/company/sectors` · `/api/company/public-orgs` 를 아무도 안
부르게 됐다. 저장소 전체를 뒤져 호출부가 없는 것을 확인하고(프론트·백엔드·테스트)
라우트와 `db.js` 의 래퍼(`companySectors` · `companyPublicOrgs`)를 지웠다.

**`company-sectors.js` 의 모듈 함수 `sectors()` · `publicOrgs()` 는 그대로 둔다** —
`industryTree()` 가 그 둘을 재료로 트리를 만든다. 지운 것은 **바깥으로 난 문**이지
계산이 아니다.

지운 라우트를 가리키던 주석 세 곳도 같이 고쳤다(트리 라우트 주석이 "계열 목록
(/api/company/sectors)과 무엇이 다른가" 로 없는 것을 가리키고 있었다). 죽은 코드보다
**없는 것을 가리키는 주석**이 더 오래 헷갈리게 한다.

확인: 서버 기동 · `industry-tree` 200(108KB) · 지운 두 라우트 **404** · 테스트 874개 통과.

### 24-11. `npm run dev` 가 저 혼자 죽던 것 (같은 날)

확인 도중 서버가 한 번 죽었다:

```
[nodemon] restarting due to changes...
Error: listen EADDRINUSE :::3000
```

`nodemon` 은 기본으로 **실행 폴더 전체**의 `js,mjs,cjs,json` 을 본다. 그런데 서버는
`backend/data/` 에 캐시를 쓴다(뉴스·잡알리오·업무특성·사업보고서). 그래서 **서버가 제
일을 하는 것만으로 재시작이 걸리고**, 이전 프로세스가 3000 을 놓기 전에 새 프로세스가
뜨면 충돌한다. 이번 변경과 무관한 기존 설정 문제지만 개발 중에 반복된다.

`backend/nodemon.json` 을 만들어 **`src` 만 보게** 했다:

```json
{ "watch": ["src"], "ext": "js,json" }
```

`ignore` 로 `data` 를 빼는 것보다 낫다 — 감시할 곳을 적는 쪽이 캐시 폴더가 새로 생겨도
안 샌다. 확인: `nodemon --dump` → `watching path(s): src\**\*`.

> 대신 **`scripts/` 와 `data/` 를 고쳐도 서버는 안 뜬다.** 수집 스크립트는 따로 돌리는
> 것이고 데이터는 서버가 읽어 두는 값이라, 바꿨으면 직접 재시작한다.

### 24-13. 운영에 올렸는데 "안 바뀐 것 같다" — 원인이 셋이었다 (2026-08-21)

`main` 머지 후 배포 사이트가 그대로라는 지적. **배포는 정상이었다.** 확인 순서:

```
GET /api/company/sectors      → 404   (어제 지운 라우트가 사라짐 = 새 코드 맞음)
GET /api/company/industry-tree → 200   그런데 186 bytes (로컬은 108KB)
```

응답이 스스로 이유를 말했다 — `"reason": "DART 기업 색인이 없습니다…"`.
**화면이 바뀐 게 아니라 목록이 비어 있어서 안 바뀐 것처럼 보인 것이다.** 옛 계열 목록도
같은 `sectors()` 를 썼으니 그 전부터 비어 있었다.

여기서 갈래가 셋으로 나뉘었다.

#### ① 진짜 원인 — 빌드가 60초에 끊겼다

빌드 로그:

```
npm run build   1m 1s
기업 색인을 받지 못했습니다(The operation was aborted due to timeout).
```

키도 있었고 Build Command 도 설정돼 있었다. **요청이 60초에 끊긴 것뿐이다.** 같은 날
로컬에서 받으면 **2.6~4.3초**(2회 실측)다 — 느린 것은 이 요청이 아니라 그때의 경로다.
`--if-possible` 이 조용히 건너뛰고 배포를 성공시키니 초록불로 보였다.

재시도를 넣었다(기본 3회, 5초·10초 쉬고). 제한시간도 120초로 올리고 **둘 다 env 로**
뺐다 — 빌더가 느린 날 코드를 고쳐 배포할 수는 없다. 강제 타임아웃(`DART_FETCH_TIMEOUT_MS=1`)
으로 3회 재시도 후 종료코드 0(배포 계속)을, 실제 실행으로 118,744건 저장을 확인했다.

#### ② 내가 넣은 버그 — 색인이 없으면 500

```
[error] GET /api/company/industry-tree - Cannot convert undefined or null to object
```

빈 트리를 돌려주면서 `codesOf` 를 빠뜨려 `industryFocus` 가 `Object.keys(undefined)` 로
터졌다. **로컬은 색인이 있어 이 가지를 한 번도 안 밟는다** — 어제 "정식 서버로 확인했다"
고 적은 것도 색인이 있는 로컬 서버였다. 결손 상태를 확인하지 않았다.

자식 프로세스에서 `allCorps()` 를 비워 재현하는 회귀 테스트 4건을 넣었다. 고치기 전으로
되돌리면 4건 전부 실패한다. **빈 값과 없는 값은 다르다.**

#### ③ AI 기본 모델이 폐기된 모델이었다

```
[cas-fit] AI 매칭 실패: 503 Groq 모델 "llama-3.3-70b-versatile" 을(를) 쓸 수 없습니다
```

Groq 모델 목록을 실제로 조회하니 그 모델은 없다. 코드 기본값이 바로 그것이었다.
로컬 `.env` 는 `openai/gpt-oss-120b` 로 갈아끼워 뒀으니 멀쩡했고, `GROQ_MODEL` 을 안 넣은
**배포 서버만** 죽은 모델로 떨어졌다. 모델을 env 로 뺀 것은 **갈아끼우기 위해서지 기본값이
틀려도 된다는 뜻이 아니다.**

#### 남는 교훈

셋 다 **로컬에 있는 것이 배포에는 없어서** 생겼다(색인 파일 · `.env` 의 모델 이름).
로컬에서 확인했다는 말은 **결손 상태를 확인했다는 뜻이 아니다.** 없을 때 어떻게 되는지를
같이 봐야 한다 — ②의 테스트가 그것을 자동으로 하게 만든 장치다.

---

## 25. 채용공고 칸 — 자사 채용페이지와 '나가기 전 담기' (2026-08-21)

팀원 의견: **"다른 데로 공고를 보게 하면 우리 걸 안 쓰고 그냥 그 사이트에서 하면 되는
거 아니냐. 굳이 우리 걸로 돌아올 이유가 없다."**

맞는 지적이었다. 공고 칸에는 사람인·잡코리아·워크넷 '더 찾기' 링크만 있었는데, 그 셋은
**우리와 기능이 겹치는 서비스**다(회사 탐색·공고·합격 자소서까지 있다). 우리가 못 하는
일을 부탁하는 게 아니라 **상위 호환 서비스에 사용자를 넘기는 것**에 가깝다.

### 25-1. 무엇을 바꿨고 무엇을 안 바꿨나

| | |
|---|---|
| 바꿈 | 자사 채용페이지 링크를 **먼저·크게** 둔다. 검색 링크는 뒤로 물러 작아진다 |
| 안 바꿈 | 검색 링크를 **없애지 않는다** — 표를 아직 못 채운 회사가 대부분이다 |
| 안 함 | 자사 사이트 공고를 **긁어오지 않는다** |

자사 채용페이지는 공고만 있고 **자소서를 봐주지 않는다.** 돌아올 이유가 남는다.
게다가 대기업 공채는 실제로 자사 사이트에만 올라오는 일이 많아 **학생에게도 그쪽이 더
정확하다** — 우리 이익과 사용자 이익이 같은 방향인 흔치 않은 경우라, 이 근거로 정했다.

### 25-2. 왜 긁어오지 않는가

팀원의 첫 제안은 "자사 사이트 공고를 직접 보여주자" 였다. 접었다:

- **파서가 회사 수만큼 필요하다.** 2,442곳이 전부 다른 사이트고, 개편 한 번에 **조용히**
  깨진다. 그 회사만 빈 칸이 되는데 아무도 모른다
- **절반은 긁히지도 않는다.** 요즘 채용페이지는 ATS 를 iframe 으로 얹거나 JS 로 그려서
  정적 요청에는 빈 HTML 만 온다
- **시의성 부담이 우리 것이 된다.** 공고는 마감이 생명이다. 지금은 API 가 최신을 주지만
  우리가 긁으면 **마감된 공고를 띄우는 책임**을 우리가 진다(18-4 에서 "0건의 이유를
  셋으로 가른다"까지 공들인 것과 정반대다)
- **약관.** 사람인은 정식 오픈API 로 허락받고 쓴다. 자사 페이지 크롤링은 회색지대다

그래서 **링크 하나**만 둔다. 값어치의 대부분을 가져오면서 위 넷을 다 피한다.

### 25-3. 표는 손으로 채운다 — 추측하지 않는다

`backend/data/career-pages.json`. `/recruit`·`/careers` 를 찍어서 만들지 않았다.
맞을 때도 있지만 틀리면 **학생을 404 로 보낸다** — 그건 사람인 링크만도 못하다.

지금 **26곳**이다(전부 실제로 열리는 것을 확인했다). 나머지는 `null` 이고 화면은 검색
링크로 물러난다.

**그룹 통합 채용도 추정하지 않았다.** 삼성·롯데는 계열사를 한 포털에서 뽑는데, "그럴
것이다" 로 붙이면 지원자를 남의 회사 포털로 보내게 된다. 그래서 **포털 페이지를 실제로
받아서 그 계열사 이름이 실려 있는 것만** 넣었다:

| 그룹 | 붙인 계열사 | 안 붙인 것 |
|---|---|---|
| 삼성 (`samsungcareers.com`) | 물산·전기·SDS·증권·카드·화재·바이오로직스·중공업 | — |
| 롯데 (`recruit.lotte.co.kr`) | 케미칼·칠성음료·웰푸드·하이마트·렌탈·이노베이트 | **롯데쇼핑·롯데정밀화학** — 포털에 이름이 없다 |
| 현대차그룹 | — | 대기열 페이지만 내려와 **확인 자체를 못 했다.** 현대자동차 한 곳만 둔다 |

같은 URL 을 여러 회사가 쓰는 것은 그룹 통합일 때만 맞고, 아니면 복사·붙여넣기 실수다.
둘은 눈으로 구분이 안 되므로 통합 포털을 `groupPortals` 에 적게 하고 **거기 없는 중복만**
테스트가 잡는다. 이름은 정규화해서 맞춘다 — DART 는 '삼성전자(주)' 로 주고 표는
'삼성전자' 라, 그대로 두면 리포트에서만 조용히 빗나간다(24-7 과 같은 함정).

```
node scripts/check-career-pages.js              # 표의 링크가 살아 있는지
node scripts/check-career-pages.js --skeleton   # 아직 안 채운 대기업 목록
```

점검 스크립트는 **실패해도 0 으로 끝난다.** 접속이 안 되는 이유가 셋인데(주소 변경 ·
회사가 닫음 · 이 환경에서만 막힘) 셋째가 있어서 자동으로 지우면 안 된다. 사람이 본다.

### 25-4. 진짜 리텐션 장치는 링크가 아니라 '담기'였다

목적지를 바꿔도 학생은 어차피 나간다. **나가기 전에 담게 하는 것**이 돌아올 이유를
만든다 — 담은 공고는 4단계 자소서 코치의 회사·공고 칸을 채운 채로 시작하게 한다.

상태에 따라 말이 다르다. **같은 말을 두 번 하지 않는다:**

| 상황 | 보여주는 것 |
|---|---|
| 공고 있음 · 안 담음 | "나가기 전에 — 담아 두면 자소서 코치가 채운 채로 시작합니다" |
| 담음 | "공고 N건 담김" + **[자소서 코치로 →]** |
| 공고 0건 | "회사만으로도 시작할 수 있어요" + **[자소서 코치로 →]** (21-5) |

공고가 0건일 때 길을 막지 않는 것이 중요하다. 막으면 학생은 "공고를 못 찾았으니 끝"
으로 읽는다. 그리고 이 화면에는 **4단계로 가는 버튼이 여태 없었다** — 스텝바로만 갈 수
있었는데, 공고를 담은 직후가 가장 넘어가기 좋은 순간이라 그 자리에 길을 뒀다.

### 25-5. 확인

DOM 라이브러리가 없어서 `company-cover.js` 를 **디스크에서 고치지 않고** 메모리에서만
내부를 노출시켜(vm 컨텍스트) 실제 렌더 코드가 만드는 HTML 을 봤다. 데이터는 실제 서버
응답이다. 네 상태 전부:

- 삼성전자(자사 링크 있음·공고 0건) → 자사 버튼 + "회사만으로 시작" + 검색 링크 뒤로
- 농심(자사 링크 없음) → 자사 블록 없이 "사람인에서 더 찾기" 로 복귀
- 한국전력공사(잡알리오 공고 6건) → "나가기 전에 담아 두면"
- 공고 담은 뒤 → "공고 1건 담김" + 자소서 코치로

테스트 **890개** 통과 (career-pages +12).

### 25-6. 남은 것

- 표가 **대기업 306곳 중 26곳**이다. `--skeleton` 으로 목록을 뽑아 채워야 한다.
  지원이 몰리는 대기업부터가 값이 크다 — 중견·공공까지 한 번에 하려 들면 안 끝난다
- 그룹 통합 채용(롯데·삼성)에서 계열사를 어디까지 같은 링크로 볼지 미정. 지금은
  **정확히 그 이름인 회사에만** 붙였다

---

## 26. 공고 주소로 본문 가져오기 (2026-08-21)

사용자 지적: **"요즘은 공고가 이미지로 되어 있거나 글을 복사 못 하게 막아둔 곳도 있는데
그걸 해결할 방법이 있을까?"**

두 문제를 갈랐다. **복사 차단과 이미지 공고는 해법이 다르다.**

| | 해법 | 이번에 |
|---|---|---|
| 복사 차단 | 서버가 그 주소를 열어 본문을 가져온다 | **했다** |
| 이미지 공고 | OCR — 외부 API 가 필요하다 | 안 했다(아래) |

복사·우클릭·드래그 차단은 **거의 전부 클라이언트 JS** 다. 서버가 열면 애초에 없는
것과 같다. 주소만 받으면 된다 — 이건 싸고 적중률이 높아서 먼저 했다.

### 26-1. 크롤링이 아니다

사용자가 준 주소 **한 건**을 그 사람 대신 열어 주는 것이다. 목록을 훑지도, 쌓아 두지도,
저장하지도 않는다(25-2 에서 안 하기로 한 것과 구분되는 지점이다). 입력칸을 채우고 끝이다.

### 26-2. SSRF — 이 기능의 진짜 위험

사용자가 준 주소를 서버가 그대로 여는 기능은 **SSRF 의 정석**이다. `127.0.0.1`,
사설망, 클라우드 메타데이터(`169.254.169.254`)를 넣으면 우리 서버가 자기 내부를 대신
읽어다 준다. 막은 방법:

- 주소를 **DNS 로 풀어 실제 IP** 를 보고 판단한다(호스트 이름만 보면 `foo.com → 10.0.0.1`
  을 못 막는다). 여러 IP 중 **하나라도** 내부면 막는다
- **리다이렉트를 매 홉마다 다시 검사**한다(`redirect: 'manual'`). 한 번만 검사하면
  첫 응답이 내부 주소로 넘겨 버린다
- `http`·`https` 만. 시간·크기·홉 수 상한
- **로그인 필수**(`requireAuth`). 열어 두면 우리 서버가 남의 요청을 대신 보내 주는
  익명 프록시가 된다. 사용자당 5분에 15회로도 묶었다

테스트 12건이 이걸 지킨다. IP 를 직접 쓴 주소는 `dns.lookup` 이 네트워크 없이 돌려주므로
네트워크 없이 검증된다.

> **왜 requireAuth 가 server.js 에 있나** — 라우터로 넘기면 순환 참조가 된다. 그리고
> `ah` 가 `const` 라 그 아래에서만 쓸 수 있다. 위쪽(라우터 등록 옆)에 두었더니
> **서버가 뜨다가 죽었다** — 옮겼다.

### 26-3. 성공했는데 내용이 쓰레기인 경우 (실측)

사람인 공고 주소(`relay/view`)를 넣으니 **1,280자를 가져왔는데 전부 메뉴와 개인정보
안내문**이었다. 상세가 iframe 안에 있어서다. 길이만 보면 성공이라 그대로 넘겼을 것이고,
그러면 이런 문장이 역량의 근거로 붙는다:

```
구직자의 개인정보는 채용 활동 외의 다른 목적으로 절대 사용하지 않으며…
```

**18-7 과 똑같은 실패 모드다.** 에러가 안 나고 그럴듯해서 더 나쁘다.

그래서 **공고에만 나오는 말**(담당업무·자격요건·우대사항·모집분야·전형절차…)이 몇 개나
있는지 세고, 2개 미만이면 `weak` 를 세운다. 사이트별 파서는 만들지 않는다(25-2 과 같은
판단). **버리지도 않는다** — 가져온 것은 주되 화면이 다른 색으로 "공고 본문이 아닌 것
같다" 고 말한다. 지우는 것보다 의심하게 하는 편이 낫다.

같은 낱말로 **머리쪽 메뉴도 걷어낸다.** 공고 낱말이 처음 나오는 줄부터 살린다. 앞이
짧으면(120자 미만) 손대지 않는다 — 제목까지 날아간다.

### 26-4. 실측 (진짜 사이트)

| 주소 | 결과 |
|---|---|
| 사람인 공고 `jobs/view` | **OK 4,876자 · weak=false** — `[주요업무]`·`[자격요건]` 그대로 |
| 사람인 `relay/view` | OK 1,280자 · **weak=true** (메뉴·안내문뿐) |
| 농심 채용 메인 | OK 1,947자 · weak=true (공고가 아니라 포털 첫 화면이니 맞다) |
| 카카오 채용 | `empty` — JS 로 그리는 페이지 |
| 한전 상시채용 | `empty` |
| 잡코리아(없는 번호) | `gone` |
| 메타데이터·루프백·file:// | `bad-url` — 전부 막힘 |

**weak 가 항상 참이면 쓸모가 없다**는 것을 확인하려고 진짜 공고를 따로 넣어 봤다.
거기서 false 가 나오는 것까지 봐야 이 판정이 의미가 있다.

### 26-5. 실패를 셋으로 가른다

같은 '안 됨' 이라도 할 일이 다르다(18-4 와 같은 원칙). 화면이 사유마다 다른 안내를 붙인다.

| kind | 뜻 | 안내 |
|---|---|---|
| `blocked` | 401·403 | 로그인해야 보이는 공고 → Ctrl+A 로 복사 |
| `image` | content-type 이 이미지 | 아직 글자를 못 읽는다 |
| `empty` | 열렸는데 본문이 200자 미만 | JS 로 그리는 페이지 → Ctrl+A 로 복사 |
| `gone` | 404·410 | 마감돼 내려갔을 수 있다 |
| `bad-url` | 형식·내부망 | 주소 확인 |

### 26-6. 가져온 글을 바로 분석하지 않는다

입력칸에 **채워 넣기만** 하고 멈춘다. 페이지에는 공고 말고도 회사 소개·푸터가 섞여
들어오므로, 사람이 보고 지운 뒤 누르게 한다. 이미 적어 둔 글이 있으면 덮어쓰기 전에 묻는다.

### 26-7. 덤으로 고친 것 — `db.js` 의 api() 가 사유를 버리고 있었다

`api()` 는 실패를 `new Error(data.error)` 로만 만들어서 **서버가 갈라 준 `kind` 와
상태코드가 사라졌다.** 화면이 사유마다 다른 안내를 붙이려면 그게 필요하다.
`e.status`·`e.kind` 를 살려 두게 고쳤다(cas-fit 의 `aiStatus` 와 같은 이유).

### 26-8. 이미지 공고(OCR)는 아직 안 한다

- 프론트에 라이브러리를 못 넣는다(빌드·CDN 을 안 쓴다 — 23-7). 한국어 OCR 데이터만 15MB 가 넘는다
- **Groq 에 비전 모델이 없다**(2026-08-21 모델 목록 직접 조회 — 13개 중 없음)
- 외부 OCR API(CLOVA·Vision·Upstage)가 필요하고, 키와 비용이 붙는다

무엇보다 **먼저 재야 한다.** 팀원들이 실제로 본 공고 20~30건에서 이미지 비율이 얼마인지.
10% 면 나중 일이고 40% 면 우선순위가 올라간다. 18-7 을 반복하지 않으려면 순서가 그렇다.

붙이더라도 **OCR 결과를 그대로 AI 에 먹이면 안 된다.** 화면에 보여주고 고치게 한 뒤
넘겨야 한다 — 26-3 과 같은 이유이고, OCR 은 그보다 더 깨진다.

### 26-9. 붙여넣은 주소가 거절당했다 (같은 날, 사용자 지적)

사용자가 실제로 넣은 주소:

```
saramin.co.kr/zf_user/jobs/relay/view?view_type=search&rec_idx=54655122&…&searchword=사람인&…
```

**'주소 형식이 아닙니다' 로 거절했다.** `https://` 가 없어서다. 크롬은 주소창에서
스킴을 숨겨 보여주므로 **그대로 복사하면 이 꼴이 된다** — 사용자에게는 멀쩡한 주소다.

점이 있는 호스트꼴일 때만 `https://` 를 붙인다. 아무 글자에나 붙이면 '안녕' 이
`https://안녕` 이 되어 '주소를 찾을 수 없습니다' 로 엉뚱하게 흘러간다. 스킴이 이미
있으면 손대지 않는다 — `file:` 은 그대로 프로토콜 검사에 걸린다. **보정이 SSRF 를
무르지 않는다**는 것을 테스트로 못 박았다(`127.0.0.1/admin` 도 여전히 막힌다).

한글이 든 쿼리(`searchword=사람인`)는 `new URL` 이 퍼센트 인코딩한 것을 쓴다.
원문을 그대로 fetch 에 넘기면 서버에 따라 400 이 온다.

### 26-10. 그런데 열고 보니 또 메뉴뿐이었다 — canonical

주소는 열렸는데 26-3 의 그 문제였다. `relay/view` 는 본문이 iframe 안이라 **1,280자
전부 메뉴와 안내문**이다. 그런데 **그 페이지가 스스로 진짜 주소를 알려 준다:**

```html
<link rel="canonical" href="https://www.saramin.co.kr/zf_user/jobs/view?rec_idx=54655122">
```

`canonical`·`og:url` 은 **웹 표준**이다 — 검색엔진이 중복 페이지를 가릴 때 쓰는 값을
그대로 읽는 것이라, 25-2 에서 안 하기로 한 '사이트마다 선택자를 손으로 짜는 파서' 와
다르다. 얇거나 `weak` 일 때만 **한 번** 따라간다(canonical 이 서로를 가리키면 끝나지
않는다). 따라간 쪽이 **더 나을 때만** 바꾸고, 아니면 원래 것을 준다 — 빈손으로
돌려보내지 않는다.

따라간 주소도 `fetchPosting` 이 처음부터 다시 검사하므로 **SSRF 안전은 그대로다.**

**실측 — 같은 주소, 같은 요청:**

| | 전 | 후 |
|---|---|---|
| 본문 | 1,280자 (메뉴·개인정보 안내문) | **7,507자** |
| weak | true | **false** |
| 최종 주소 | relay/view | `jobs/view?rec_idx=54655122` |
| 내용 | — | `자격요건` · `우대사항` 항목 그대로 |

테스트 **938개** 통과 (posting-fetch 32 → 47).


### 26-11. 가져오긴 했는데 절반이 공고가 아니었다 (같은 날, 사용자 지적)

26-10 으로 진짜 공고 페이지까지 갔다. 그런데 사용자가 결과를 보고 물었다 — **"필요한
정보와 아닌 정보를 분류해서 뽑아올 수 있냐."** 4,844자 중 뒤쪽 절반이 공고가 아니었다.

지저분한 정도가 아니라 **결과를 틀리게 하는 값**이 섞여 있었다:

```
경쟁자들의 역량 키워드 — 유연한 사고 · 데이터 기반 의사결정 · 웹디자인 · 피그마
경쟁자들의 Skill      — 프로토 타이핑 · 그래픽/UI디자인 툴 · Design System 구축
```

**이건 다른 지원자들의 스펙이지 이 공고(PM)가 요구하는 역량이 아니다.** 그대로 넘기면
PM 공고에서 '피그마' 가 요구 역량으로 잡힌다. 기업리뷰에 실린 문장도 그대로 있었다:

```
전화선이 꼬인 사람은 즉시 해고하셨습니다.
```

이게 근거 문장으로 자소서에 인용될 수 있었다. 18-7 의 실패 모드가 더 나쁜 얼굴로
돌아온 것이다 — 그때는 법정 문구였지만 이번엔 **남의 스펙**이다.

#### 어떻게 잘랐나

사이트별 선택자를 짜지 않는다(25-2). 취업 사이트가 공통으로 쓰는 **말**로 자른다.

| 무엇 | 예 |
|---|---|
| 꼬리 | 지원자 통계 · 경쟁력 분석 · 기업리뷰 · 면접후기 · 관련 태그 · 이어보는 |
| 덩어리 | AI 서류 합격률 · AI 이력서 코칭 · 코칭 받을 이력서 (광고·로그인 유도) |
| 줄 | 공유하기 · 페이스북 · 지도보기 · 닫기 · 조회수 N · …상세보기 |

#### 본문을 자르지 않도록 못 박은 것 셋

- **꼬리 표시가 본문보다 앞에 나오는 일이 있다.** 이 페이지도 'AI 서류 합격률'(51줄)이
  '상세요강'(75줄)보다 위에 있다. 그래서 꼬리는 **마지막 공고 낱말 뒤에 나오는 것만** 자른다
- **공고 낱말이 하나도 없으면 아예 안 자른다.** 본문이 어디까지인지 알 수 없는데 자르면
  남는 게 없다. 그때는 `weak` 가 "공고 같지 않다" 고 알려 주는 것으로 충분하다
- **줄 삭제는 정확히 일치할 때만.** 부분일치로 지우면 "지도 보기 편한 자료를 만들었습니다"
  같은 본문이 날아간다. 덩어리 제거에도 최대 줄 수(40)를 둬서 표시를 잘못 잡아도
  본문을 통째로 먹지 못하게 했다

#### 실측 (같은 페이지)

| | 전 | 후 |
|---|---|---|
| 분량 | 4,844자 · 435줄 | **2,193자 · 137줄** |
| 경쟁자 역량·Skill | 있음 | **없음** |
| 기업리뷰·면접후기·관련 태그 | 있음 | **없음** |
| 로그인 폼·공유 버튼·조회수 | 있음 | **없음** |
| 주요업무·자격요건·우대사항·근무조건·채용절차·접수기간·복리후생 | 있음 | **그대로 있음** |

테스트 **948개** 통과 (posting-fetch 47 → 57).


### 26-12. 잡코리아는 요약표만 왔다 — 본문이 딴 주소에 있다 (같은 날)

사용자가 잡코리아 공고로 확인해 보고 결과를 보내왔다. 모집분야·경력·학력·마감일은
있는데 **담당업무와 자격요건 상세가 통째로 없었다.** 그 페이지가 상세요강을 따로
불러오기 때문이다:

```
GI_Read_Comt_Ifrm?...&Gno=49798770   ← 이 안에 [담당업무]·[자격요건 및 우대사항] 이 있다
```

#### canonical 과 성격이 다르다 — 바꾸는 게 아니라 이어 붙인다

`canonical` 은 "이 페이지가 곧 저 페이지" 라 통째로 **바꾸는** 것이고(26-10),
이건 "이 페이지의 **일부**" 라 **이어 붙이는** 것이다. 요약표(경력 2~8년, 학력,
마감일)도 지원 가능 여부를 판단하는 데 쓰이므로 버릴 이유가 없다.

**길이로 재지 않는다.** 잡코리아는 요약표(693자)보다 본문(516자)이 짧은데 담당업무는
거기 있다. 그래서 **담당업무를 말하는 낱말이 있는지**로 판단한다.

사이트별 선택자는 짜지 않았다. **같은 사이트 안**의 주소 중 끼워 넣는 내용처럼 보이는
것(`ifrm`·`iframe`·`embed`·`detail`·`content`)을 후보로 보고, 최대 셋만, **공고 낱말이
든 것만** 채택한다. 남의 사이트로 나가는 주소는 후보가 아니다 — 그건 광고다.
후보 주소도 `fetchPosting` 이 처음부터 다시 검사하므로 SSRF 안전은 그대로다.

#### 이 과정에서 잡은 버그 — `&amp;` 때문에 같은 페이지를 두 번 받았다

`canonical` 이 HTML 속성이라 `&amp;` 로 escape 돼 있다. 안 풀면 `?a=1&b=2` 와
`?a=1&amp;b=2` 를 **다른 주소로 본다.** 그래서 잡코리아에서 canonical 이 자기 자신을
가리키는데도 따라가 버렸고, `!better.weak` 라 거기서 끝나 **정작 본문이 있는
상세요강까지 가지 못했다.** escape 푸는 규칙을 `canonicalOf` 와 `embedUrls` 가 같이
쓰도록 한 곳에 뒀다.

#### 실측

| 주소 | 전 | 후 |
|---|---|---|
| 잡코리아 공고 | 693자 · **담당업무 없음** | **1,066자 · 담당업무·자격요건·우대사항 있음** |
| 사람인 공고 | 2,193자 | 2,152자 (잔노이즈 더 정리) |
| 농심 채용 포털 | weak=true | weak=true (공고가 아니니 맞다) |
| 카카오 채용 | empty | empty (JS 로 그리는 페이지) |

잔노이즈도 더 걷었다 — `인근지하철`·`궁금해요`·`홈페이지 바로가기`·`로그인 하고 …`·
`채용정보에 잘못된 내용이…`. 주소 끝에 달라붙은 `지도보기` 는 **줄째로 지우면 주소까지
잃으므로** 끝의 그 글자만 떼어낸다.

테스트 **955개** 통과 (posting-fetch 57 → 64).


### 26-13. 로컬은 되는데 배포 서버만 못 열었다 (같은 날)

사용자가 배포 사이트에서 같은 잡코리아 주소를 넣으니 **"공고 페이지를 열지 못했습니다"**
가 떴다. 같은 시각 로컬에서는 1,066자가 정상으로 온다 — 코드가 깨진 것이 아니라
**배포 서버에서 그 사이트에 닿지 못하는 것**이다.

문제는 그 한 줄로는 **사용자가 할 수 있는 일이 없다**는 것이다. 우리 잘못인지, 사이트가
막은 것인지, 주소가 틀린 것인지 알 수 없다. 우리도 원인을 추측만 하게 된다.

그래서 실패 원인을 코드로 갈랐다:

| 원인 코드 | kind | 하는 말 |
|---|---|---|
| `ENOTFOUND`·`EAI_AGAIN` | bad-url | 주소를 찾을 수 없다 |
| `ECONNRESET`·`ECONNREFUSED`·`EPROTO`·`CERT_*`·`UND_ERR*` | **blocked** | 이 사이트가 우리 서버에서 오는 접속을 막고 있다 |
| 시간 초과 | error | 제때 응답하지 않았다 |
| 그 밖 | error | 원인 코드를 괄호에 붙여 그대로 보여준다 |

취업 사이트가 데이터센터 IP 나 해외 접속을 막는 일은 흔하다. 그때는 **브라우저로 여는
것은 멀쩡하므로** 복사해 붙여넣으라고 안내하는 편이 맞다 — `blocked` 로 분류한 이유다.

화면 안내도 정리했다. 예전에는 `blocked` 에 "로그인해야 보이는 공고예요" 를 붙였는데,
서버가 이미 사유를 말하므로 **사유를 두 번 말하면 서로 어긋난다.** 화면은 **할 수 있는
일**만 덧붙인다 — "브라우저에서 열고 Ctrl+A → Ctrl+C 로 복사해 붙여넣어 주세요."

> 원인 코드를 화면에 노출하는 것이 불친절해 보일 수 있지만, 이 값이 없으면 다음에
> 같은 일이 나도 **또 추측만 하게 된다.** 18-4 에서 0건의 이유를 셋으로 가른 것과
> 같은 판단이다.


### 26-14. 잡코리아는 우리 서버 IP 를 막고 있다 — 진단과 결론 (같은 날)

26-13 의 원인 구분을 배포하고 사용자가 다시 넣으니 코드가 나왔다:

```
UND_ERR_CONNECT_TIMEOUT
```

**TCP 연결 자체가 안 맺어졌다는 뜻이다.** HTTP 요청을 보내기도 전 단계다. 여기서 두
가지가 확정된다.

**① 우리 서버가 한국에 못 닿는 게 아니다.** 같은 서버가 DART(`opendart.fss.or.kr`)에서
색인 118,744건을 받고, 잡알리오에서 공고를 받고, 사람인 공고도 가져온다. **한국은 잘
닿는다. 잡코리아만 안 된다.**

**② User-Agent 를 바꿔도 소용없다.** 연결이 맺어지기 **전에** 끊긴 것이라 우리가 보낸
헤더는 잡코리아에 도달조차 하지 않았다. 브라우저인 척해도 결과가 같다 — 그래서 그
방법은 검토 대상이 아니다. (연결 단계 실패라는 사실 하나가 이 선택지를 지운다.)

결론: **잡코리아가 네트워크 단에서 데이터센터 IP 를 막고 있다.** 크롤러를 막으려는
흔한 조치이고, 서버 쪽에서 우회할 방법이 없다. 프록시를 두는 길이 남지만 비용과 약관
문제가 생긴다 — 하지 않는다.

#### 그래서 고친 것은 '기다리는 시간' 이다

막혔다는 판정에 **10초**가 걸린다. 공고를 여러 개 확인하는 학생은 그 10초를 매번 다시
기다린다. 연결 단계에서 실패한 호스트를 **10분만 기억**했다가 즉시 안내로 넘긴다.

- **짧게 기억한다.** 일시적 장애를 영구 차단으로 오해하면 멀쩡해진 뒤에도 계속 막는다
- **성공하면 바로 지운다.** 차단 목록은 갱신된다
- **HTTP 응답이 온 실패는 기억하지 않는다.** 401·403·404 는 이미 빠르다 —
  연결은 됐다는 뜻이기 때문이다

#### 지금 어디까지 되나

| 대상 | 결과 |
|---|---|
| 사람인 | 가져와진다 (canonical 따라가기 포함) |
| 자사 채용페이지 · 공공기관 | 대체로 가져와진다 |
| **잡코리아** | **차단 — Ctrl+A 복사 안내** |
| 카카오 등 JS 로 그리는 페이지 | `empty` — Ctrl+A 복사 안내 |
| 이미지 공고 | `image` — 직접 입력 안내 |

테스트 **961개** 통과 (posting-fetch 64 → 70).


---

## 27. STAR 없이도 AI 초안을 만든다 (2026-08-21, 사용자 지시)

### 27-1. 사실은 처음부터 됐다 — 팝업이 막는 것처럼 읽혔다

`/api/jd/draft` 는 `star` 가 없어도 받는다(`starOf` 가 null 을 돌려주고 프롬프트가
그대로 조립된다). 막고 있던 것은 화면의 `confirm` 하나였다:

```
위 STAR 칸이 비어 있어요.
… 확인을 누르면 그대로 만들고, 취소하면 STAR 부터 채웁니다.
```

**확인을 누르면 만들어졌으니 기능상으로는 열려 있었다.** 그런데 팝업이 뜨는 것 자체가
"아직 하면 안 되는 일" 로 읽힌다 — STAR 를 다 채우기 전에는 초안을 못 본다고 여기게 된다.

### 27-2. 순서가 거꾸로였다

STAR 를 먼저 채우게 한 것은 **재료가 있어야 문단이 구체적**이기 때문이고, 그 판단
자체는 지금도 맞다(활동 이름·기간·역할만으로는 "노력했고 잘 마무리했습니다" 가 나온다).

다만 **초안을 한 번 보고 나서야 무엇을 적어야 할지 알게 되는 사람이 더 많다.**
빈 칸 넷을 앞에 두고 "S 에 무엇을 적지" 로 막히는 것보다, 뻔한 문단이라도 하나 보고
"아, 여기에 내 숫자를 넣으면 되는구나" 로 가는 편이 빠르다.

### 27-3. 막지 않되 모르게 하지도 않는다

`confirm` 을 없애고 **바로 만든다.** 재료가 빠졌다는 말은 **결과 옆에** 남긴다:

```
빈칸 4개를 본인 사실로 채우세요 · 위 STAR 를 채우면 더 구체적으로 나와요 [채우러 가기]
```

`[채우러 가기]` 를 누르면 S 칸이 열리고 그리로 스크롤한다 — 예전 `confirm` 의 '취소'
가 하던 일이 그대로 남았다. 다만 **초안을 본 뒤의 선택지**가 됐다.

> 만들기 전에 말하면 경고이고, 만든 뒤에 말하면 다음에 할 일이 된다.
> 21-5 에서 '공고 없이도 시작' 을 열어 둔 것과 같은 판단이다.

### 27-4. 확인

- `POST /api/jd/draft` 를 `star` 없이 호출 → 문단 생성, 빈칸 4개
- 남은 `confirm` 은 둘뿐이고 둘 다 **되돌릴 수 없는 일**이다(초안 삭제 · 공고 칸 덮어쓰기).
  이건 물어보는 게 맞다
- 테스트 923개 통과

### 27-5. 위 STAR 띠를 눌러서 쓴다 (같은 날, 사용자 지시)

`모든 문항의 뼈대 — STAR` 띠는 **설명만 있는 읽기 전용**이었다. 그런데 화면을 열면
가장 먼저 눈에 들어오는 것이 이 띠라, **여기를 누르면 쓸 수 있을 것으로 읽힌다.**
실제로 쓰는 곳은 그 아래 `여기에 내 경험을 채우세요` 아코디언이었다.

칸마다 입력칸을 하나 더 만들지는 **않았다.** 같은 값을 두 곳에서 고치면 저장이 어긋난다
(초안·STAR 모두 localStorage 한 곳에 회사·문항별로 넣는 규약이다). 대신 칸을 누르면
**아래 입력의 그 칸을 열고 그리로 데려간다.** 저장은 여전히 `bindStarInput` 한 곳에서만
일어난다.

겸사겸사 띠가 **지금 뭘 적어 뒀는지**도 보여준다:

```
S 상황                                   61자
배경과 문제 상황
2~3줄로 짧게. 배경 설명이 길어지면 …
────────────────────────────────
기획과 개발이 같은 문서를 다르게 읽고 있었습니다. …

T 과제                                 미작성
…
누르면 여기에 적습니다
```

설명 넷만 떠 있는 것보다 **무엇이 비었는지**가 한눈에 보인다. 비어 있으면 '누르면 여기에
적습니다' 가 흐리게 앉는다 — 빈자리를 그냥 두면 여기가 쓰는 곳이라는 것을 모른다.

**띠와 아코디언은 같은 값을 보여주므로 같이 갱신한다.** 한쪽만 고치면 같은 화면에
'미작성' 과 '61자' 가 동시에 떠서 어느 쪽이 맞는지 알 수 없다.

**확인** — DOM 라이브러리가 없어 `jd-coach.js` 를 디스크에서 고치지 않고 메모리에서만
내부를 열어(vm) 실제 렌더 코드가 만드는 HTML 을 봤다. 화면이 쓰는 저장 함수를 그대로
태워서 키가 같은 것까지 확인했다(`(회사 미지정)::q0`).

### 27-6. 띠와 입력칸을 하나로 합쳤다 (같은 날, 사용자 지시)

27-5 로 띠를 누르면 아래 아코디언으로 데려가게 했더니, 다음 지적이 나왔다 —
**"star를 각각 누르면 글 적는 칸과 저장하는 칸이 나오게 해줘."**

맞는 말이었다. 27-5 는 **같은 네 칸이 화면에 두 번** 나오는 상태였다. 위에는 설명
띠(S·T·A·R), 아래에는 입력 아코디언(S·T·A·R). 누르면 데려가 주긴 하지만, 사용자
입장에서는 "어디에 적는 것인가" 를 한 번 더 찾는 구조다.

**아래 `여기에 내 경험을 채우세요` 섹션을 통째로 없앴다.** 이제 띠가 곧 입력이다 —
칸을 누르면 **바로 그 아래**에 그 칸의 쓰는 자리가 열린다.

#### 없애면 손해인 것은 전부 옮겼다

| 살린 것 | 어디로 |
|---|---|
| 나쁜 예 / 고친 예 | 입력칸 위 그대로 — "구체적으로 쓰세요" 보다 훨씬 빠른 안내다 |
| 저장 표시(`저장됨 · 61자`)·600ms 자동저장 | 그대로 |
| `저장하고 T 쓰기 →` | 그대로. 마지막 R 칸에서는 'AI 초안 넣기를 누르세요' |
| `0/4 칸 작성`·저장 위치 안내 | 띠 머리줄로 |
| AI 되짚기 '더 적을 것' | 띠의 배지 + 입력칸 위 빨간 줄 |

#### 지킨 규칙

- **한 번에 한 칸만 연다.** 네 칸을 다 열면 S 를 대충 쓰고 A 로 건너뛴다. STAR 는
  앞 칸이 뒤 칸의 전제다
- **같은 칸을 다시 누르면 접힌다**(오른쪽 `✕` 도 같은 일). 좁힌 것을 푸는 길이 같은
  자리에 있어야 '닫기' 를 따로 찾지 않는다
- **타이핑 중에는 띠만 다시 그린다.** 입력칸까지 갈면 커서를 잃는다. 반대로 칸을
  바꿀 때는 둘 다 그린다 — 한쪽만 갈면 방금 접은 칸이 열린 채로 보인다
- **아무 칸도 안 열렸을 때도 빈 껍데기(`<div class="jd-starin">`)는 남긴다.** 다시
  그릴 때 붙일 자리가 필요하다
- 기본으로 **S 를 열어 둔다** — 띠만 네 칸 떠 있으면 '어디에 적나' 를 한 번 더 찾는다
- 입력칸 머리에 **어느 칸·어느 문항인지 다시 적는다.** 스크롤로 띠가 화면 밖으로
  나가면 무엇을 쓰는 중인지 알 수 없다

**확인** — 실제 렌더 코드로 네 상태(안 적었을 때 · S/R 을 적은 뒤 · 마지막 R 칸 ·
다 접었을 때). 테스트 970개 통과.


---

## 28. 스펙을 다 넣었는데 "입력하지 않았어요" 가 떴다 (2026-08-21, 사용자 지적)

> "지금 CAS 점수는 안 뜨는데 이유 좀. '아직 스펙을 입력하지 않았습니다' 가 뜨는데
> 학점, JLPT, 정보처리산업기사, 정성스펙 2개 적었는데도 안 떠. 직무역량 레이더도."

### 28-1. 원인 — `dept` 로 "스펙이 있는가" 를 판단하고 있었다

`cas-hero.js` 와 `cas-radar.js` 가 둘 다 이렇게 막고 있었다:

```js
if (!savedSpec || !savedSpec.dept) → '아직 스펙을 입력하지 않았어요'
```

그런데 **`dept` 는 사용자가 넣는 값이 아니다.** 학과명에서 우리가 자동으로 정하는
집계 분류이고(`major-catalog.js deptOf`), 계열이 **8개뿐**이다. 실측:

| 학과 | dept |
|---|---|
| 컴퓨터공학과 · 경영학과 · 심리학과 | cs · business · psych |
| **일어일문학과 · 영어영문학과 · 간호학과 · 기계공학과** | **null** |

`spec-form.js` 는 **일부러** 저장을 막지 않는다("분류가 비면 그 학과 기준 통계에만
안 잡힐 뿐"). 그래서 8개 계열에 안 맞는 학과 사람은 **스펙을 다 채우고 저장까지
성공했는데 화면이 "입력하지 않았어요" 라고 말했다.** 저장은 됐는데 없다고 하니,
사용자는 자기가 뭘 잘못했는지 찾게 된다.

JLPT 를 넣었다는 것에서 어문 계열임을 짐작할 수 있었고, `deptOf('일어일문학과')` 가
`null` 인 것으로 확인됐다.

### 28-2. 더 나쁜 것 — 낼 수 있는 점수도 안 냈다

`resolveContext` 의 구조를 보면 **목표 직무가 있으면 `dept` 없이도 채점된다.**
`roadmapBenchmark()` 는 로드맵이 정한 모집단(`Roadmap.bench()`)만 쓰기 때문이다.

그런데 `dept` 검사가 **그 앞에** 있어서, 목표 직무를 골라 둔 사람조차 계산에 들어가지
못했다. 순서 하나 때문에 낼 수 있는 점수를 안 낸 것이다.

### 28-3. 고친 것

- **판단 기준을 바꿨다.** `CAS.hasAnySpec(spec)` — 학과명·학점·어학·자격증·활동 중
  **본인이 채운 값이 하나라도 있는가**로 본다. `dept` 는 우리가 정하는 값이라 세지 않는다
- **순서를 바꿨다.** 목표 직무 경로를 `dept` 검사보다 **앞**에 뒀다. 이제 계열 통계가
  없는 학과여도 목표 직무만 골라 두면 점수가 나온다
- **말을 갈랐다.** 스펙이 없는 것과 계열 통계가 없는 것은 다른 상황이다

| 상황 | 예전 | 지금 |
|---|---|---|
| 스펙 없음 | 아직 스펙을 입력하지 않았어요 | (그대로) |
| 스펙 있음 + 목표 직무 있음 | **아직 스펙을 입력하지 않았어요** | **점수가 나온다** |
| 스펙 있음 + 계열·직무 둘 다 없음 | 아직 스펙을 입력하지 않았어요 | 이 학과는 아직 계열 통계가 없어요 — **스펙은 저장돼 있어요.** 목표 직무를 고르면 그 기준으로 계산합니다 |

레이더도 같이 고쳤다. 위 CAS 카드가 목표 직무로 모집단을 정했으면(`CASDashboardContext`)
그걸 그대로 쓰므로 `dept` 없이도 그려진다.

> **How to apply** — "값이 있는가" 로 "사용자가 입력했는가" 를 판단할 때는, 그 값이
> **사용자가 넣은 것인지 우리가 채운 것인지** 먼저 본다. 우리가 채우는 값은 우리가
> 못 채울 수도 있고, 그때 사용자에게 "당신이 안 넣었다" 고 말하게 된다.

테스트 **970개** 통과 (cas +9).

---

## 29. '스펙 채우기' → '스펙UP', 상단바에 올렸다 (2026-08-21, 사용자 지시)

### 29-1. 이름

화면에 보이는 곳 세 군데를 바꿨다 — 스펙UP 화면 머리(`커리어 로드맵 2단계 · 스펙UP`),
CAS 2단계 갈림길 버튼, 직무 적합도 안내문의 링크.

**내부 이름(`specup`·`SpecUp`·`page-specup`)은 그대로 둔다.** 라우팅 키와 저장 키를
바꾸면 주소·상태가 끊긴다(브랜드명을 C:road 로 바꿀 때와 같은 판단 — 19-1).

### 29-2. 상단바 — 예전 판단을 뒤집는다

`app.js` 에 이렇게 적혀 있었다:

> 스펙 채우기는 CAS(2단계)에서 갈라지는 곁가지라 CAS 를 강조한다 —
> **별도 메뉴를 만들면 "스펙을 채워야 다음으로 갈 수 있다" 로 읽힌다.**

사용자 지시로 뒤집는다. 자리는 **CAS 바로 뒤** — 로드맵 2단계에서 갈라지는 화면이라
흐름 순서가 맞다. 모바일 더보기 메뉴(≤900px)에도 같이 넣었다.

메뉴 강조도 바꿨다. 예전에는 스펙UP 에 들어가면 **CAS 에 불이 들어왔다**(곁가지니까).
이제는 자기 메뉴를 강조한다 — **눌러서 들어왔는데 다른 메뉴가 켜져 있으면 어디에
있는지 알 수 없다.**

곁가지라는 사실은 화면 머리의 `커리어 로드맵 2단계 · 스펙UP` 과 로드맵 스텝바가
여전히 말해 준다. 예전 우려(순서를 강제하는 것처럼 읽힌다)는 그것으로 막는다고 봤다.

> 뒤집은 판단이므로 **왜 뒤집었는지**를 코드 주석에도 남겼다. 안 적으면 다음 사람이
> "곁가지인데 왜 메뉴에 있지" 로 되돌린다.

---

## 30. 자격증 추천을 근거 있는 것으로 바꿨다 (2026-08-22, 사용자 지시)

### 30-1. "멘토에만 뜬다" 의 진짜 원인

지적은 **"자격증 추천이 멘토에만 뜨고 멘티에는 안 뜬다"** 였다. 코드에는 역할 분기가
없었다. 실측으로 원인을 찾았다:

```
demo_kim  (멘토) dept='cs'   → 추천 6개
mentee_a  (멘티) dept=NULL   → 추천 0개
```

추천이 **학과(dept) 하나만** 보고 있었고, 그 표는 손으로 쓴 **8개 계열**뿐이었다
(`aggregation.js` CERT_CATALOG). 멘토 시드 계정에만 dept 가 채워져 있어서 화면상
역할 차이로 보였을 뿐이다. 간호·기계·전자처럼 그 8개에 없는 학과는 멘토라도 0개다.

**같은 코드에 없는 분기를 찾느라 시간을 쓰지 않으려면 데이터를 먼저 본다.** 이번에도
`SELECT dept FROM user_specs` 한 줄이 원인을 바로 가리켰다.

### 30-2. 학과가 아니라 직무를 본다

자격증은 학과가 아니라 **직무**를 따라간다. 같은 컴퓨터공학과에서도 백엔드와
정보보안이 따는 자격이 다르다. 그래서 기준을 바꿨다:

```
희망 세부직무(KECO 2차) → 없으면 희망 진출분야(1차) → 없으면 학과(대비책)
```

화면에 어느 기준으로 뽑았는지도 적는다(`희망 세부직무 기준 추천 자격증`). 안 적으면
학생은 왜 이 목록이 나왔는지 모른 채 시험에 돈과 시간을 쓴다.

### 30-3. 근거 둘 — 자격 직무분야 + 채용공고 언급

| 근거 | 출처 | 커버리지 |
|---|---|---|
| **자격 직무분야** | 큐넷이 자격마다 붙여 주는 분류(`certs.field`) | 국가기술자격 513 + 민간 30 |
| **채용공고 언급** | 잡알리오 공고의 응시자격·우대사항 원문 | **공공기관 532건뿐** |

공고 집계는 **보조**다. 실측으로 정했다 — 532건에서 언급이 잡힌 자격은 **46종,
최대 9건**이다. 이걸 주 근거로 쓰면 대부분의 직무에서 추천이 0개가 되고, 9건이라는
숫자를 비율로 말할 수도 없다. 그래서 화면 문구를 **"공공기관 채용공고 532건 중
N건에서 요구"** 로 못 박았다. "채용의 N%" 같은 말로 부풀리지 않는다.

### 30-4. 큐넷 분류가 두 자리에서 틀렸다 (실측)

그대로 쓰면 안 됐다.

**① 국가전문자격 100종에는 직무분야가 아예 없다.** 공인노무사·세무사·관세사·
물류관리사·사회복지사처럼 대학생이 실제로 노리는 자격이 전부 여기 있다. 보정하지
않으면 추천에서 통째로 빠진다.

**② '경영.회계.사무' 한 칸이 너무 넓다.** 실측 결과:

```
금융·보험직(03) 추천 → 워드프로세서, 공장관리기술사, 포장기술사, 품질경영기사
```

금융투자분석사·CFA 와 포장기술사가 같은 칸에 들어 있었기 때문이다.

`cert-reco.js` 의 `FIELD_OVERRIDE` 가 둘을 보정한다. 그리고 **누가 정한 분야인지를
응답에 실어 보낸다**(`fieldSource: 'qnet' | 'croad'`) — 화면이 "큐넷 직무분야" 와
"우리 분류" 를 섞어 말하지 않게 하려는 것이다. 6-3(조용히 틀린 값)과 같은 원칙이다.

### 30-5. 등급 순서를 뒤집었다

처음엔 기술사 → 기능장 → 기사 순으로 뒀다. 자격 등급이 그 순서라서다. 실측하니
정보통신 직무 추천이 **정보관리기술사·컴퓨터시스템응용기술사**로 시작했다.

기술사는 **실무 경력 4~9년이 있어야 응시할 수 있다.** 대학생 화면에서 위로 올라오면
추천 목록이 통째로 '지금은 못 따는 것' 이 된다. 그래서
**기사 → 산업기사 → 그 밖 → 기능사 → 기능장 → 기술사** 순으로 바꿨다.

> **How to apply** — 정렬 기준을 '자료의 위계' 로 정하기 전에, **이 화면을 보는
> 사람이 그 위계를 쓸 수 있는지** 본다.

### 30-6. 같은 계열은 하나만

안 묶으면 목록이 한 자격의 변형으로 도배된다(실측):

- 여행·숙박 직무 → 추천 8칸이 전부 `관광통역안내사(독어/러시아어/…)`
- 사회복지 직무 → `1급/2급/3급 청소년상담사` + `1급/2급/3급 청소년지도사`

계열 키는 괄호 이하와 등급 표기를 지운 이름이다. **줄을 세운 뒤에** 솎는다 —
먼저 솎으면 어느 등급을 남길지 정할 기준이 없다.

### 30-7. 발급기관 — 아는 것만 채운다

큐넷 API 원본에는 발급기관 필드가 **아예 없다.** 그래서 예전에는 목록에서 고른
자격증도 늘 빈칸이었고, 학생이 매번 직접 찾아 적었다.

시행기관을 세 갈래로 채운다:

| 갈래 | 값 | 근거 |
|---|---|---|
| 큐넷 목록 613종 | 한국산업인력공단 | 그 목록을 받아 온 API 자체가 산업인력공단 것이다 |
| 예외 | 국가유산청·소방청 등 | `ISSUER_EXCEPTIONS` (접두어 매칭) |
| 민간·해외 30종 | 한국데이터산업진흥원·대한상공회의소·CFA Institute … | `PRIVATE_ISSUERS` |

**모르면 null 이다.** 표에 없으면 빈칸을 내주고 학생이 직접 적는다 — 그럴듯한
기관명을 지어내면 틀린 값이 조용히 저장된다.

시행기관 표는 DB 컬럼으로 만들지 않았다. **자격 이름·구분만 보고 정해지는 값**이라
컬럼으로 두면 표를 고칠 때마다 배포 DB 에 이관을 돌려야 한다.

### 30-8. 저장한 줄은 통째로 잠근다 (사용자 지시)

한 번 저장한 자격증은 **이름도 발급기관도** 글자로 굳고, [수정]을 눌러야 둘 다
열린다. 삭제(x)는 잠금과 무관하게 늘 된다 — 지우는 건 되돌릴 수 있고 고치는 것과
성격이 다르다.

> 처음에는 발급기관만 굳혔다. 그랬더니 **한 줄 안에서 한쪽은 잠겨 있고 한쪽은
> 검색 입력칸인** 이상한 상태가 됐다(사용자 지적). 훑다가 이름을 실수로 덮어쓰기도
> 쉽다. 판단 기준은 '저장됐는가' 하나로 두고 줄 전체에 같게 적용한다.

잠긴 줄은 입력칸을 **readonly 로 두지 않고 아예 안 만든다.** readonly 는 눌러도
아무 일이 없어 고장으로 읽히고, `data-cert-i` 가 남아 있으면 입력 핸들러도 계속 걸린다.

직접 입력분(취득일까지 있는 것)은 **저장 전이라도 늘 모달에서 고친다.** 이름·기관·
취득일이 한 묶음이라, 셋 중 하나만 따로 고칠 수 있게 두면 이름은 바꿨는데 취득일은
옛 자격증 것인 줄이 생긴다.

`certEditing` 은 이름을 키로 쓴다. 그래서 고치는 도중 이름이 바뀌면 **키도 함께
옮긴다** — 안 옮기면 저장돼 있던 다른 이름을 쳤을 때 줄이 도로 잠긴다.

늘 입력칸이면 **저장된 값인지 방금 자동으로 채워진 값인지** 화면에서 구분이 안 된다.
그래서 `certMetaSaved`(서버에서 받아 온 것)와 `certMeta`(지금 편집 중인 것)를 따로 든다.

같이 고친 것 — 배지가 `certMeta` 유무로 '직접 입력' 을 판단하고 있었다. 발급기관이
자동으로 채워지기 시작하면 **목록에서 고른 자격까지 '직접 입력' 배지**가 붙는다.
판단 기준을 **취득일이 있는지**로 바꿨다(직접입력 모달만 취득일을 받는다).

테스트 **1,026개** 통과 (`test/cert-reco.test.js` +46, `alio-jobs` +10).

---

## 31. 멘토 찾기 — 가짜 멘토 102명을 걷어냈다 (2026-08-22, 사용자 지시)

### 31-1. 무엇이 있었나

`mentoring.js` 에 손으로 쓴 멘토 6명이 있었고, 그걸 NCS 24분야로 불려 **96명을 더
만들고** 있었다. 별점·후기·상담 횟수·예약 가능 일정까지 멘토 id 로 난수를 돌려 채웠다.

두 가지가 동시에 틀려 있었다.

**① 멘토가 멘토 페이지를 채워도 목록에 안 떴다.** 화면이 서버를 보지 않았다.
실제 회원과 화면이 아예 이어져 있지 않았던 것이다(사용자 지적 그대로).

**② 후배가 그 별점과 후기를 진짜로 읽었다.** 4.8점·후기 12건은 화면을 채우려고
만든 숫자인데, 멘토를 고르는 근거로 쓰인다. 8장 CEO 공개 선행조건과 같은 성격이다.

### 31-2. 서버가 원본이 됐다

`GET /api/mentors` → `repo.mentors.list()`. **프로필을 채운 멘토만** 내려간다
(소개글이나 전문분야 중 하나라도 있어야 한다). 소개글도 전문분야도 없는 카드를
후배가 눌러 봐야 아무것도 없기 때문이다.

로그인을 요구하지 않는다 — 가입 전에 "어떤 선배가 있는지" 를 보고 가입을 정한다.
대신 **내보내는 칸을 이름으로 못 박았다.** `SELECT *` 를 쓰면 연락처·생년월일·
주소가 그 순간 공개된다.

id 는 **username** 이다. 내부 id 를 노출하면 목록만 봐도 남의 레코드를 지목할 수 있다.

### 31-3. 없는 값은 만들지 않는다 (사용자 결정)

별점·후기 수·상담 인원·학번·상담 주제는 채울 데이터가 없다(멘토링 신청 테이블 0건).
0으로 채우거나 '아직 없음' 을 늘어놓는 대신 **칸 자체를 없앴다.**

카드에 남은 것: 이름 · 직무 · 회사 · 경력 연차 · 전문분야 · 소개글 한 토막.
정렬에서도 평점순·멘토링순을 뺐다. 남은 것은 경력 연차와 **프로필을 얼마나 채웠나**
순이다 — 후배에게는 읽을 게 많은 카드가 먼저 보이고, 멘토에게는 채울수록 노출된다.

### 31-4. 분류를 KECO 로 통일했다 (사용자 결정)

멘토 찾기만 NCS 24분야 칩이었고, 실제 멘토 스펙에 저장되는 값은 로드맵·스펙입력과
같은 **KECO 1차 10개**였다. 둘을 잇는 번역표가 필요했는데, 분류를 하나로 모으면
그 자리가 사라진다.

같이 고친 것 — 멘토링 방식 필터가 `온라인/오프라인` 이었다. 멘토가 실제로 고르는
값은 `video30/onsite60/text` 라 **한 글자도 안 맞아 고르면 항상 0명**이었다.

### 31-5. 신청도 서버가 확인한다

가짜 멘토 시절에는 `mentorId` 를 검증할 방법이 없어서 그냥 저장했다. 이제
**목록에 있는 멘토에게만** 신청이 간다. 멘토 이름도 본문에서 받지 않고 서버가
채운다 — 화면이 보낸 이름을 그대로 저장하면 신청 목록에 아무 이름이나 남길 수 있다.

### 31-6. 연동 직후엔 목록이 빈다

실측: 멘토 계정 127개 중 **프로필을 채운 사람이 0명**이었다. 그래서 데모 시드
(`demo-seed.js`)에 멘토 프로필 5명을 넣었다 — 소개글·전문분야·타임라인·가능 형식·
예약 가능 일정.

가짜 멘토와 다른 점은 **관리자가 명시적으로 넣는 데이터**라는 것이다.
`/api/admin/seed` 는 운영에서 권한으로 막혀 있고, 앱 코드에는 남지 않는다.

예약 가능 일정은 날짜를 박지 않고 **'오늘로부터 며칠 뒤'** 로 적어 넣는 순간에
날짜로 바꾼다. 박아 두면 며칠 뒤부터 전부 지난 날짜가 되어 달력이 빈다.

시드 라우트도 고쳤다. 예전에는 계정이 이미 있으면 **그냥 건너뛰었다.** 그러면
데모 계정을 이미 만들어 둔 DB 에는 새로 생긴 멘토 프로필이 영영 안 들어간다.
지금은 **빈 칸만 채운다** — 덮어쓰지는 않는다(직접 적어 본 소개글을 지우면
시연 준비가 통째로 날아간다).

---

## 32. CAS — 두 점수를 나란히, 그리고 지금 뽑는 곳 (2026-08-22, 사용자 지시)

### 32-1. 왜 "둘 다 보여 달라" 가 나왔나

대시보드에는 이미 **직무 적합도 카드**와 **CAS 카드**가 위아래로 둘 다 있었다.
그런데 적합도는 로드맵에서 **직업**까지 골라야 계산되는 값이라, 보통은 그 자리가
안내문 한 칸이다. 그래서 스크롤하기 전에는 **점수가 하나뿐인 화면**으로 읽힌다.

맨 위에 두 점수를 나란히 두는 띠(`#cas-two`)를 만들었다. 같은 크기·같은 모양이다 —
한쪽이 크면 그쪽이 '진짜 점수' 로 읽히는데, 둘은 대등한 값이다.

**숫자를 여기서 다시 계산하지 않는다.** 적합도는 `CASFit.summary()`, CAS 는
`cas-hero.js` 의 `scoreOf()` 결과를 받아 적기만 한다. 띠가 따로 계산하면 위아래
숫자가 갈리고, 학생은 어느 쪽이 맞는지 알 수 없다.

한 줄에 나란히 두면 **합계처럼 읽힐 수 있어서**, 칸마다 무엇에 답하는 점수인지를
한 줄로 적었다 — `이 일에 내가 맞나` / `선배들 사이에서 내가 어디쯤인가`.

### 32-2. 지금 모집 중인 공고

> **2026-08-22 걷어냈다 — 36장을 볼 것.** 아래는 왜 넣었는지의 기록으로 남긴다.

점수만 보여주고 끝내지 않고, **그 직무로 지금 뽑는 곳**을 옆에 붙였다
(`GET /api/job-postings`). 링크는 공고 원문으로 바로 간다.

직무↔공고를 잇는 표는 **`cert-reco.js` 하나**를 그대로 쓴다. 여기에 표를 하나 더
두면 같은 직무가 화면마다 다른 분야로 번역된다.

**공공기관 공고뿐이다.** 사람인 키(`SARAMIN_ACCESS_KEY`)가 비어 있고 워크넷은
개인회원이 막혀 있다(10-7). 출처를 안 적으면 "이 직무는 5곳만 뽑는구나" 로 읽히므로,
`공공기관 채용정보(잡알리오) 532건 중 · 2026-08-15 기준` 을 함께 적는다.
0건일 때도 사유를 갈라서 말한다(분야에 공고 없음 / 전부 마감 / 신입 조건에 걸림).

기본은 **신입 공고만**이다. 이 화면을 보는 사람은 대학생이고, 경력 공고가 섞이면
'지원할 수 있는 곳' 이 실제보다 많아 보인다.

---

## 33. 커리어 인사이트가 배포에만 안 보였다 (2026-08-22)

코드는 전부 있었다 — 게시판·홈 카드·시드 스크립트(`seed-insights.js`)까지.
로컬 DB 에도 글 5편이 들어 있었다. **배포 DB 에만 시드를 안 돌린 것**이다.

증상이 헷갈리는 이유는, 카드를 못 그리면 **칸을 통째로 숨기기** 때문이다
(제목만 있고 안 열리는 카드보다 낫다는 판단 — 23장). 그래서 "인사이트가 안 보인다"
가 아니라 "홈에 그런 칸이 없다" 로 보인다.

`docs/배포.md` 4장·8장에 시드 단계를 넣고, 확인 방법을 적었다:

```bash
curl -s https://<배포주소>/api/insights/featured
# articles 의 postId 가 전부 null 이면 시드를 안 돌린 것이다
```

> **How to apply** — "없어도 화면이 멀쩡하게" 를 위해 칸을 숨기는 설계는 옳지만,
> **숨긴 사실이 어디에도 안 남으면** 운영에서 원인을 못 찾는다. 숨기는 조건은
> 배포 문서의 점검 항목으로 같이 적는다.

---

## 34. 채용공고 — 자동 갱신을 붙이고, 다른 소스를 다시 훑었다 (2026-08-22, 사용자 지시)

### 34-1. "자동으로 갱신되나?" — 아니었다

`alio-jobs.json` 은 **사람이 스크립트를 돌릴 때만** 갱신되는 스냅샷이었다. 코드에
스케줄러가 하나도 없고(`setInterval`·cron 없음), 서버는 파일 mtime 이 바뀌면 다시
**읽을** 뿐 다시 **받지는** 않는다.

더 나쁜 것은 그 파일이 `.gitignore` 에 있고 **빌드에도 없었다**는 것이다.

```
루트 package.json  build: fetch-dart-corps.js --if-possible   ← DART 만 있었다
```

`dart-corps.json` 은 같은 이유로 gitignore 인데 build 에 넣어 뒀고, alio 만 빠져
있었다. **배포하면 CAS 의 공고 칸이 통째로 비는 상태**였다. 16-9(그 .gitignore 가
업종코드를 같이 데려갔다)와 성격이 같다 — 회사 리포트의 곁가지일 때는 비어도 티가
안 났는데, CAS 메인에 올리면서 눈에 띄는 자리가 됐다.

**A안 적용**(사용자 결정): 루트 `build` 에 `fetch-alio-jobs.js --if-possible` 을 넣었다.
DART 와 같은 방식이라 새로 만드는 장치가 없고, **배포 주기가 곧 갱신 주기**가 된다.
스크립트에 `--if-possible`(SOFT) 를 새로 넣어, 키가 없거나 잡알리오가 죽어 있어도
빌드가 0 으로 끝나게 했다 — 공고 칸 하나 때문에 배포 전체가 막히면 손해가 훨씬 크다.

실행 확인: 472건 · 기관 124곳 · 신입 435건 (2026-08-22).

> `ongoingYn=Y` 없이 부르면 `totalCount` 가 **113,093** 이다. 그건 과거 이력까지
> 포함한 전체이고, 접수 중은 472건이 맞다. 숫자만 보고 "우리가 덜 받고 있다" 고
> 판단하지 말 것.

### 34-2. 다른 소스 재탐색 — 실호출로 확인한 것

추정하지 않고 전부 실제로 불렀다(3-1 의 교훈).

| 소스 | 응답 | 판정 |
|---|---|---|
| **잡알리오** (지금 쓰는 것) | 200 · 접수중 472건 | ✅ 유일하게 도는 것 |
| **고용24 채용정보 210L01·210D01** | `개인회원은 사용할 수 없는 OPEN-API입니다` | ❌ **회원유형 차단.** 신청해도 안 된다 |
| 고용24 다른 오퍼레이션 211~220L01 | `신청하신 OpenApi 서비스가 존재하지 않습니다` | 미신청이거나 없는 번호 |
| 워크넷 구 게이트웨이 `wantedApi` | `유효하지 않은 인증키` | ❌ 2026 발급 키를 구 게이트웨이가 모른다 |
| data.go.kr 3038225 (워크넷 채용정보) | API 유형이 **LINK** | ❌ data.go.kr 키가 아니라 워크넷 자체 인증을 태운다 = 위와 같은 벽 |
| **온통청년 `getPlcy`** | 200 · '일자리' 1,080건 | ⚠️ 채용공고가 아니라 **청년 정책·프로그램 모집**이다(창업특강·지원사업). 이미 스펙업이 쓰고 있다 |
| 그리팅(Greeting) ATS | 문서상 **비즈니스 플랜 고객 전용 API 키** | ❌ 우리가 발급받을 수 없다 |
| **서울 열린데이터광장 `GetJobInfo`** | 200 · **24,897건** | △ 아래 참고 |

**10-7 은 여전히 유효하다.** 고용24 채용정보는 코드로 풀 수 있는 문제가 아니다.

### 34-3. 서울 일자리포털(GetJobInfo) — 되긴 되는데 성격이 다르다

`http://openapi.seoul.go.kr:8088/{KEY}/json/GetJobInfo/{시작}/{끝}/`
인증키는 data.seoul.go.kr 에서 **무료·자동발급**(심사 없음)이고, 키 자리에 `sample`
을 넣으면 5건까지 시험 호출이 된다 — 그렇게 확인했다.

건수는 24,897건으로 잡알리오(472)의 50배지만 **그대로 붙이면 안 된다**:

- 샘플 5건이 전부 `학력 관계없음` · 시간제였다 (재가 간병인 3 · 건물 청소원 · CNC 선반).
  `MNGR_INSTT_NM` 이 **고용노동부**인 것으로 보아 고용센터 구인 등록 피드다 —
  대졸 공채가 아니라 생계형·중소·서비스직이 주류일 가능성이 크다.
  **샘플 키로는 5건 이상 못 봐서 구성을 끝까지 확인하지 못했다.** 붙이기 전에
  실제 키로 표본을 봐야 한다.
- **공고 원문 URL 필드가 없다.** 담당자 이름·전화번호(`MNGR_PHON_NO`)뿐이다.
  우리 화면은 "눌러서 공고로 간다" 가 전제라 이 소스는 그 규약을 못 지킨다.

> 결론: 가능성은 있지만 **"건수가 많다" 는 이유로 붙이면 화면이 나빠진다.**
> 대학생이 볼 공고가 아닌 것이 대부분이고 링크도 못 준다.
> 붙인다면 직종코드(`RCRIT_JSSFC_CMMN_CODE_SE`)로 거르고, 링크 대신
> `job.seoul.go.kr` 검색으로 보내는 설계가 먼저 필요하다.

### 34-4. 민간 공고는 결국 사람인이다 (사용자 조치 필요)

코드는 이미 사람인을 **1순위**로 부르게 되어 있다(`companyAnalysis.js` SOURCES).
`SARAMIN_ACCESS_KEY` 만 채우면 그날부터 민간 공고가 들어온다.

신청: <https://oapi.saramin.co.kr/join> — 확인한 것:

- 요구 항목: 이메일·이름·전화·**회사명/학교명**·부서명/전공명·**API 사용할 곳 URL**·
  이용목적(50자 이상)
- 심사: "이용신청 결과 회신 및 목적 심사" (자동승인 아님)
- **호출 한도 일 500건**, 상업적 재판매 금지
- 학교명 칸이 따로 있어 **학생 프로젝트로도 신청 가능해 보인다**

> **34-A** | 사람인 API 이용신청 (학교명 + 배포 URL 로) | **사용자가 해야 함**

일 500건은 우리 쓰임에 넉넉하다 — 회사별 조회는 캐시가 아니라 그때그때 부르지만
(alio-jobs.js 머리주석), 하루 500회면 실사용 규모에서 남는다.

---

## 35. 자격증 API 재탐색 — 그리고 내가 넣은 예외표가 틀렸다 (2026-08-22)

### 35-1. 실호출로 확인한 것

| API | 결과 | 판정 |
|---|---|---|
| 큐넷 종목 목록 (사용 중) | 200 · 613종 | ✅ 대조군 |
| **큐넷 결격사유 `getDisqList`** | 200 · `공인노무사 / 미성년자` 등 | ✅ **지금 키로 바로 된다.** 아직 안 쓰는 정보 |
| 큐넷 `getSimilarJmList` · `InquiryTestInformationSVC` | 404 | ❌ 그런 오퍼레이션 없음 |
| B490007 시험일정 (사용 중) | 200 | ✅ 대조군 |
| B490007 공개문제 | `해당 오픈API 서비스가 없거나 폐기됨` | ❌ 경로 추정 실패 |
| **공인민간자격등록현황** 15069557 | 파일데이터 **100건** · 소관부처·자격종목·등급·**자격관리자**·공인유효기간 | △ 활용신청 필요. **가장 쓸모 있다** |
| 민간자격등록정보 15075600 | 3만 건 전체 | ❌ 그대로는 못 쓴다(카탈로그가 망가진다 — cert-catalog.js 머리주석과 같은 판단) |
| odcloud 호출(uddi 없이) | `등록되지 않은 서비스` | 활용신청 후 uddi 를 받아야 부를 수 있다 |

### 35-2. 시행기관을 주는 API 는 없다 — 대신 구조를 알아냈다

국가기술자격 **시행기관은 9곳**이다: 한국산업인력공단 · 대한상공회의소 ·
한국원자력안전기술원 · 영화진흥위원회 · 한국콘텐츠진흥원 · 한국방송통신전파진흥원 ·
한국광해광업공단 · 한국데이터산업진흥원 · 한국디자인진흥원.

**큐넷 API 는 그중 한국산업인력공단 시행분만 준다.** 그래서 컴퓨터활용능력·
워드프로세서·전산회계운용사(대한상공회의소)와 정보보안기사(전파진흥원)가
목록에 없는 것이다 — 실측으로 전부 **0건**임을 확인했다. cert-catalog.js 머리주석이
"실측으로 확인한 구멍" 이라고 적어 둔 것의 **이유**가 이것이다.

### 35-3. 그러다 내가 넣은 예외표가 틀린 걸 발견했다

30-7 에서 시행기관 매핑표를 만들며 예외를 다섯 개 넣었다.

```
국가유산수리기술자 → 국가유산청
소방시설관리사     → 한국소방안전원
소방안전교육사     → 소방청
경매사             → 농림축산식품부
```

**전부 틀렸다.** '시행기관' 을 물었는데 **'주관 부처'** 를 적었다.

근거는 두 개이고 서로 맞물린다.

1. 큐넷 목록에 다른 기관 시행분이 0건이다 → 이 목록은 산업인력공단 시행분만 담는다
2. 그런데 소방시설관리사(9658) · 국가유산수리기술자(9667) · 경매사(9697) 는
   **산업인력공단 시험일정 API 가 회차를 내준다**
   (`B490007/qualExamSchd`, `qualgbCd=S`, 실측 2026-08-22)

```
소방시설관리사        → 전문자격 (2026년도 26회 2차)
국가유산수리기술자(단청) → 전문자격 (2026년도 44회 면접)
경매사(수산)          → 전문자격 (2026년도 24회 2차)
```

즉 **큐넷 목록에 있다 = 산업인력공단이 시행한다** 가 맞고, 예외를 만드는 순간
기본값의 근거를 스스로 깨는 셈이 된다. 예외표를 **비웠다.**

> `qualgbCd` 값도 실측으로 갈랐다. 국가기술자격은 `T`, 전문자격은 **`S`** 다.
> `P` 로 부르면 0건이 오는데 **HTTP 200 이라 '일정 없음' 으로 조용히 둔갑한다** —
> specup.js 머리주석이 `numOfRows` 상한에서 겪은 것과 같은 부류다.

예외표는 지우지 않고 **빈 배열로 남기고 왜 비웠는지를 적었다.** 지워 버리면 다음
사람이 같은 착각으로 다시 넣는다. 테스트에도 회귀 케이스를 넣었다
(`주관 부처가 따로여도 시행기관은 산업인력공단`).

> **How to apply** — 표를 만들 때 **기본값의 근거와 예외의 근거가 같은 자료에서
> 나오는지** 본다. 기본값을 "이 목록에 있으니까 A" 로 정했다면, 그 목록 안의 항목을
> "사실 B" 라고 예외 처리하는 순간 기본값이 무너진다. 둘 중 하나는 틀린 것이다.

### 35-4. 남은 것

| # | 할 일 | 상태 |
|---|---|---|
| ~~35-A~~ | 공인민간자격등록현황 활용신청 | ✅ 사용자가 승인받음 → 35-5 참고 |
| ~~35-B~~ | 그 100건으로 PRIVATE_ISSUERS 보강 + '국가공인' 배지 | ❌ **폐기.** 데이터가 8년 묵었다(35-5) |
| 35-C | 큐넷 결격사유(`getDisqList`)를 스펙UP 에 붙인다 — "미성년자는 응시 불가" 처럼 **따려다 못 따는 자격**을 미리 걸러 준다 | 지금 키로 바로 됨 |

### 35-5. 승인받아 불러 봤더니 — 8년 묵은 데이터였다 (2026-08-22)

사용자가 15069557 을 활용신청해 승인받았다. **호출은 됐다.** uddi 는 활용신청 없이도
Swagger 문서에서 공개로 볼 수 있었다:

```
https://infuser.odcloud.kr/oas/docs?namespace=15069557/v1
  → /15069557/v1/uddi:92abeef2-6b56-4c98-89d3-ea434ac82a1e
```

98건을 받았다. 필드는 기대한 대로 `자격종목·등급·자격관리자·소관 부처·공인유효기간`.
**그런데 쓸 수 없다.**

| 확인한 것 | 값 |
|---|---|
| 공인유효기간이 아직 살아 있는 것 | **0건 / 98건** |
| 만료 연도 분포 | 2014:11 · 2015:33 · 2016:24 · 2017:24 · 2018:5 |
| 소관 부처 이름 | **미래창조과학부 20건 · 행정자치부 3건** ← 2017년 정부조직 개편 **전** 이름 |

파일명은 `20250829` 지만 그건 게시일이고 **내용은 2017~2018년 스냅샷**이다.
이걸로 '국가공인' 배지를 붙이면 지금 공인인지 아닌지 모르는 채 붙이는 것이고,
그게 바로 6-3(조용히 틀린 값을 보여주는 곳) 이다. **안 쓴다.**

> 갱신본으로 보이는 `민간자격등록정보`(15075600, 20251231)는 별도 활용신청이
> 필요하고 **3만 건 전체**라 그대로는 카탈로그를 망가뜨린다(cert-catalog.js
> 머리주석과 같은 판단). 추가로 파지 않기로 했다 — 아래 35-6.

**소득이 없지는 않았다.** 대조군으로 쓰니 우리 손 표가 맞다는 게 확인됐다:

| 자격 | 공공데이터의 자격관리자 | 우리 PRIVATE_ISSUERS |
|---|---|---|
| 재경관리사 | 삼일회계법인 | 삼일회계법인 ✅ |
| 전산세무회계 | 한국세무사회 | 한국세무사회 ✅ |
| SQL | (재)한국데이터베이스진흥원 | 한국데이터산업진흥원 ✅ **우리 쪽이 최신** (기관 개명) |

### 35-6. 없는 자격증은 직접 입력으로 둔다 (사용자 결정)

시행기관이 9곳으로 흩어져 있어 통합 API 가 존재하지 않고(35-2), 민간자격 공공데이터는
묵었거나 너무 크다(35-5). **더 파지 않기로 했다.**

카탈로그에 없는 자격증은 지금처럼 **직접 입력**으로 받는다. 그 경로가 이제
대비책이 아니라 **정식 경로**이므로 동작을 다시 확인했다(2026-08-22 실측):

```
'정보보안기사' 입력  → "목록에 없는 자격증이에요" + [정보보안기사 직접 입력]
  → 모달에서 이름·발급기관·취득일을 받는다
  → 저장하면 잠긴 줄 + [수정] (직접입력분이라 [수정]은 모달을 연다)
  → 서버에 { date, issuer } 로 정확히 저장된다
```

> **How to apply** — 외부 데이터를 붙일지 정할 때 **건수와 필드만 보지 말고 값의
> 나이를 본다.** 이번엔 '소관 부처' 에 사라진 부처 이름이 남아 있는 것이 결정적이었다.
> 날짜 필드가 없는 자료라도, 안에 든 고유명사가 자료의 나이를 말해 준다.

테스트 **1,027개** 통과.

---

## 36. CAS 에서 채용공고를 걷어냈다 (2026-08-22, 사용자 지시)

CAS 화면 위쪽에 있던 **`지금 모집 중인 공고`** 띠(32-2)를 지웠다. 자리는 비워 뒀다.

### 36-1. 왜 지웠나 — 화면이 답하는 질문이 두 개였다

로드맵은 단계마다 질문이 하나씩이다.

| 단계 | 화면 | 이 화면이 답하는 질문 |
| --- | --- | --- |
| 2 | CAS | **내가 지금 어디쯤인가** |
| 3 | 회사 찾기 | **그래서 어디에 지원하는가** |

공고 띠는 2단계 화면에서 3단계의 질문에 답하고 있었다. 게다가 **더 얕게** 답했다 —
회사 찾기의 `채용공고` 칸은 회사별로 지원자격·우대사항까지 담고 `[담기]` 로 4단계
자소서 코치까지 이어지는데(21장), 이 띠는 공공기관 공고 5건을 링크로만 뿌리고 끝이었다.
같은 학생이 두 곳에서 공고를 보면 **어느 쪽이 진짜 목록인지** 알 수 없다.

### 36-2. 자리를 무엇으로도 메우지 않았다

메울 후보를 두 개 따져 봤고 둘 다 접었다.

- **CAS 점수 추이** — 점수 이력을 저장하지 않는다(`schema.sql` 에 그런 표가 없다).
  넣으려면 스키마부터 만들어야 하고, 만든 직후에는 점 하나뿐이라 **그래프가 거짓말을 한다.**
- **다음 할 일 안내** — 이미 아래에 GAP 카드와 `#cas-next` 가 있다. 위에 또 두면
  같은 말을 두 번 한다.

이 화면은 이미 일곱 칸(두 점수 요약 · 적합도 · CAS · 레이더 · 스펙 비교 · GAP · 갈림길)
이고 칸마다 다른 질문에 답한다. **답할 질문이 없는데 칸을 채우면 화면만 길어진다.**

### 36-3. 지운 것

| 파일 | 내용 |
| --- | --- |
| `frontend/careerly.html` | `#cas-jobs` 자리 (지운 이유를 주석으로 남김) |
| `frontend/js/cas-hero.js` | `renderPostings()` · `_jobsSeq` · `render()` 안의 호출 |
| `frontend/css/mentoring.css` | `.cas-jobs*` · `.cj-*` |
| `frontend/js/db.js` | `DB.jobPostings()` — 부르는 화면이 없어졌다 |

**서버는 그대로 둔다.** `GET /api/job-postings` 와 `alio-jobs.js` 의 `jobPostings()`
는 테스트가 붙어 있고(`test/alio-jobs.test.js`), 신입 필터·마감 판정 같은 판단이 그 안에
있다. 나중에 회사 찾기 쪽에서 직무 단위 목록이 필요해지면 그대로 쓴다.

> **How to apply** — 화면에 칸을 더할지 말지는 **그 칸이 답하는 질문이 이 화면의
> 질문인가**로 정한다. "여기 있으면 편하겠다" 는 이유로 다른 단계의 답을 끌어오면,
> 원래 그 답을 맡은 화면과 **어느 쪽이 진짜인지 겨루게 된다.**

테스트 **1,027개** 통과. 브라우저 실측 — 직무를 골라도 띠가 뜨지 않고 콘솔 오류 없음.

