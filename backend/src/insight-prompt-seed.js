/* ════════════════════════════════════════════════════════════
   AI 프롬프트 공유 게시판에 올리는 편집 프롬프트 — **정의의 단일 출처**

   ── 왜 필요한가 (사용자 지시 2026-09-07) ────────────────────
   "AI프롬프트에 우리 기본 프롬프트를 올려놓아줘. 그리고 2개 정도 더 만들어서."

   게시판이 비어 있으면 학생은 **무엇을 올리는 곳인지** 모른다. 안내문(promptIntro)
   이 설명해도, 실제 글이 하나도 없으면 '아직 아무도 안 쓰는 칸' 으로 읽힌다.
   그래서 우리가 먼저 세 개를 올려 본보기를 만든다.

   ── 커스텀 프롬프트는 규칙을 **통째로 대체**한다 ────────────
   draft-coach.js buildPrompt: `const rules = customRules || restriction` 이다.
   담아 간 프롬프트가 있으면 기본 Restriction 이 **통째로 빠진다.** 그래서 여기
   올리는 글은 **자기 완결적**이어야 한다 — "이것도 추가로 지켜라" 식의 조각을
   올리면, 담는 순간 지어내기 금지·한국어 전용 같은 안전장치까지 사라진다.

   세 개 모두 아래를 스스로 담고 있다:
     · 주어진 STAR 밖의 사실을 지어내지 않는다 (없으면 대괄호로 비운다)
     · 빈칸이 하나도 없는 답은 대필이다 — 최소 한 곳은 비운다
     · 회사 사실을 아는 척하지 않는다
     · 한국어로만 쓴다
     · 분량 상한을 넘기지 않는다 (넘으면 뒤가 잘려 문단이 끝나지 않는다)

   ── 기본 프롬프트는 여기 베껴 두지 않는다 ───────────────────
   `draft-coach.defaultRules()` 가 만들어 주는 값을 그대로 싣는다. 베껴 두면
   엔진의 규칙을 고칠 때마다 게시판 글이 조용히 옛것이 된다 — 담아 간 사람은
   **옛 규칙으로 초안을 쓰게 되고**, 화면 어디에도 그 사실이 안 나온다.

   ── 제목이 키다 ──
   insight_posts 에는 '이 글이 어느 편집 글인가' 를 적을 칸이 없다(seed-insights.js
   와 같은 규약). 제목을 바꾸면 새 글이 하나 더 생기므로 제목은 여기서만 고치고
   `--force` 로 다시 돌린다.
   ════════════════════════════════════════════════════════════ */
const DRAFT = require('./draft-coach');
const { PROMPT_CATEGORY } = require('./insight-prompt');

/* 공통 안전장치. 세 프롬프트가 문구를 조금씩 다르게 쓰면 어느 것이 진짜인지
   읽는 사람이 판단해야 한다 — 같은 뜻은 같은 문장으로 둔다. */
const SAFETY = `- **주어진 STAR 에 없는 사건·행동·수치를 새로 만들지 마라.** 더 구체적인 값이 필요한 자리는 [문의 건수], [팀원 수] 처럼 대괄호로 비우고 넘어간다. 지어내기와 비워두기 중에서는 **언제나 비워두기가 맞다.**
- **빈칸이 하나도 없는 답은 틀린 답이다.** 지원자만 아는 값 최소 한 곳은 반드시 대괄호로 비운다 — 빈칸이 없으면 그건 초안이 아니라 대필이다.
- **회사에 대한 사실을 아는 것처럼 쓰지 마라.** 이 프롬프트는 회사 정보를 주지 않았다. 회사 이야기가 필요한 자리는 [확인할 회사 사실] 로 한 번만 비운다.
- 대괄호 안까지 포함해 **한국어로만** 쓴다(고유명사·널리 쓰는 약어는 예외).
- 존댓말 서술체(~했습니다)로 쓴다.`;

/* ── 1. 짧은 문항용 ────────────────────────────────────────
   실제로 자주 막히는 자리다. 500자 문항에 1,000자용 규칙을 그대로 쓰면 모델이
   길게 쓰고 뒤가 잘린다. 기본 규칙과 **다른 것은 분량 배분과 문장 길이**이지
   안전장치가 아니다. */
const SHORT_FORM = `${SAFETY}
- 전체 **500~600자**로 쓴다. **600자를 절대 넘기지 마라** — 넘으면 뒤가 잘려 문단이 끝나지 않는다. 모자라게 끝나는 편이 넘치는 것보다 낫다.
- **결론을 맨 앞 한 문장으로 먼저 쓴다.** "저는 ~를 배웠습니다 / ~를 중요하게 여깁니다" 로 열고, 뒤는 그 결론의 근거만 쓴다. 결론을 끝에 다시 반복하지 마라.
- 분량 배분: 결론 1문장 · 상황과 내 몫 2문장 · **행동 3문장 이상** · 결과 1~2문장.
  **행동이 절반을 넘어야 한다.** 배경 설명이 길면 짧은 문항에서는 답이 사라진다.
- 한 문장은 **60자 안쪽**으로 끊는다. 접속사로 이어 붙이지 마라.
- 수식어를 쓰지 마라: 매우, 정말, 굉장히, 다양한, 많은, 적극적으로, 열심히.
- 대괄호는 문단 전체에 **최대 2개**까지만 쓴다. 짧은 글에서 빈칸이 셋이면 읽을 것이 남지 않는다.
- 아래 표현은 쓰지 마라(변별력이 없다): 귀사에 기여, 시너지, 역량을 함양, 최선을 다하겠습니다, 열정을 가지고, 성장하는 기업, 비전에 공감, 원만한 대인관계.
- "노력했습니다", "소통했습니다", "잘 마무리했습니다", "많은 것을 배웠습니다" 는 행동도 결과도 아니다. 무엇을 어떻게 했는지로 바꿔 쓴다.
- 결과는 성공이 아니어도 된다. 목표에 못 미쳤으면 못 미친 대로 적고, 그때 건진 것을 한 줄 붙인다.`;

/* ── 2. 기술 직무용 ────────────────────────────────────────
   개발·데이터·연구 직무에서 자주 나오는 실패는 반대 방향이다 — 감상은 많은데
   **무엇을 어떤 도구로 어떻게 했는지**가 없다. 그래서 도구·지표를 살리게 한다.
   다만 **STAR 에 없는 스택을 지어내는 것**이 더 큰 위험이라, 그 자리를 못 박는다. */
const TECH_FORM = `${SAFETY}
- 전체 **900~1000자**로 쓴다. **1000자를 절대 넘기지 마라** — 넘으면 뒤가 잘려 문단이 끝나지 않는다.
- **행동(A)이 문단의 절반 이상**이어야 한다. 상황·과제가 행동보다 길면 배경 설명으로 끝난 답이다.
- 행동은 **무엇을·어떤 도구로·어떤 기준으로·어떤 순서로** 했는지 단계로 쓴다. 방법 이름만 대지 마라.
  · 이렇게 쓰면 탈락: "데이터를 분석해 인사이트를 도출했습니다."
  · 이렇게 쓴다: "[도구]로 [기간] 로그를 뽑아 [기준]으로 [대상]을 추렸고, 그중 …"
- **STAR 에 적힌 도구·기술·지표 이름은 그대로 살려라.** 요약하느라 '개발 도구', '통계 기법' 처럼 뭉뚱그리지 마라 — 그 이름이 이 문단에서 유일하게 확인 가능한 사실이다.
- **STAR 에 없는 기술 스택·라이브러리·버전을 새로 쓰지 마라.** 필요하면 [사용한 도구] 로 비운다. 면접에서 가장 먼저 파고드는 자리다.
- **왜 그 방법을 골랐는지 한 문장을 반드시 넣는다.** 대안이 있었다면 무엇을 왜 버렸는지까지. 선택의 이유가 없으면 시킨 일을 한 것으로 읽힌다.
- 결과는 **숫자나 확인 가능한 사실**로 쓴다. 숫자가 STAR 에 없으면 [개선 폭] 으로 비우고, 무엇을 재야 하는지를 coach 에 적어라.
- 팀이 한 일과 **내가 한 일**을 섞지 마라. 내가 맡은 경계를 분명히 밝힌다.
- 아래 표현은 쓰지 마라: 최신 기술, 다양한 기술 스택, 효율적으로 개선, 최적화를 진행, 성능을 향상시켰습니다(무엇이 얼마나인지 없이).`;

/* 게시판에 올릴 글 세 편. body 는 '이 규칙이 무엇을 하는가' 를 설명하는 글이고,
   promptText 가 담아 가는 원문이다 — 둘을 섞지 않는다(insight-prompt.js 머리주석). */
const PROMPTS = [
  {
    title: 'C:road 기본 프롬프트 (자소서 코치가 쓰는 그대로)',
    body: `자소서 코치가 AI 초안을 만들 때 실제로 쓰는 규칙입니다. 아무것도 담지 않았을 때 적용되는 바로 그것이라, **무엇을 고칠지 정하기 전에 먼저 읽어 보시라고 올려 둡니다.**

담아 가서 한두 줄만 고쳐 쓰는 것을 권합니다. 처음부터 새로 쓰면 지어내기 금지·빈칸 남기기 같은 안전장치를 빠뜨리기 쉽습니다.

이 글의 원문은 **엔진에서 그대로 가져옵니다.** 규칙이 바뀌면 이 글도 같이 바뀝니다.`,
    /* 베끼지 않고 엔진에서 읽어 온다 — 위 머리주석 참고. */
    promptText: () => DRAFT.defaultRules({ limit: 1000, questionType: 'competency' }),
  },
  {
    title: '500자 문항용 — 짧게, 결론부터',
    body: `**글자 수가 빡빡한 문항**(500~600자)에서 쓰는 규칙입니다.

기본 규칙은 900~1000자를 전제로 합니다. 그대로 두고 500자 문항에 쓰면 모델이 길게 쓰다가 **뒤가 잘려 문단이 끝나지 않습니다.**

바꾼 것은 분량 배분과 문장 길이입니다.
- 결론 한 문장으로 열고, 뒤는 근거만
- 한 문장 60자 안쪽 · 수식어 금지
- 빈칸은 최대 2개 (짧은 글에 빈칸이 셋이면 읽을 것이 없습니다)

**안전장치는 그대로 뒀습니다** — 지어내기 금지, 최소 한 곳 비우기, 한국어 전용.`,
    promptText: () => SHORT_FORM,
  },
  {
    title: '개발·데이터 직무용 — 도구와 지표를 살린다',
    body: `개발·데이터·연구 직무 자소서에서 자주 나오는 실패는 **감상은 많은데 무엇을 어떻게 했는지가 없는 것**입니다. "데이터를 분석해 인사이트를 도출했습니다" 같은 문장이요.

그래서 이 규칙은 세 가지를 못 박습니다.
- STAR 에 적은 **도구·기술·지표 이름을 그대로 살릴 것** (뭉뚱그리지 말 것)
- **왜 그 방법을 골랐는지** 한 문장 (없으면 시킨 일을 한 것으로 읽힙니다)
- 팀이 한 일과 **내가 한 일**을 가를 것

반대로 **STAR 에 없는 스택·버전을 새로 쓰는 것**은 막았습니다. 면접에서 가장 먼저 파고드는 자리입니다.`,
    promptText: () => TECH_FORM,
  },
];

module.exports = { PROMPTS, CATEGORY: PROMPT_CATEGORY, SAFETY, SHORT_FORM, TECH_FORM };
