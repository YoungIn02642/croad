# 화면 구조 지도 — 무엇을 어디서 고치나

C:road 프론트엔드는 **빌드 없는 단일 문서 SPA** 다. HTML 파일이 하나뿐이라
"이 화면 HTML은 어디 있지?"를 찾는 데 시간을 쓰기 쉽다. 그 지도를 여기 둔다.

> **행 번호는 참고용이다.** 코드를 고치면 밀린다. 정확히 찾으려면 아래
> [찾는 요령](#찾는-요령)의 `grep` 을 쓰고, 행 번호는 "대충 이 근처" 정도로만 본다.

---

## 1. 큰 그림

```
frontend/careerly.html      ← 화면 HTML 전부 (페이지 16개가 이 안에)
frontend/css/*.css          ← 4개 파일
frontend/js/*.js            ← 21개 모듈
backend/src/server.js       ← API + 정적 파일 서빙
```

**동작 방식** — `careerly.html` 안에 `<div class="page" id="page-XXX">` 가 16개 들어 있고,
`js/app.js` 의 `showPage()` 가 그중 하나에만 `.active` 를 붙인다. 나머지는 CSS 로 숨는다.
주소는 해시 라우팅(`#career`, `#jd` …). **페이지를 이동해도 문서는 다시 로드되지 않는다.**

---

## 2. 페이지 지도

| 주소 | 화면 이름 | HTML 위치 | 내용을 만드는 곳 |
|---|---|---|---|
| `#main` | 홈 | `careerly.html` **112행** | HTML + `js/home.js` (일부 섹션) |
| `#login` | 로그인 | **335행** | HTML |
| `#signup` | 회원가입 | **363행** | HTML |
| `#mypage` | 마이페이지 — 탭 3개 | **438행** | ⚠️ 계정 관리 `js/account.js` · 스펙 관리 `js/spec-form.js` · **멘토 페이지 `js/mentor-profile.js`(멘토만)**<br>탭은 해시 뒤에 붙는다(`#mypage/account` · `/spec` · `/mentor`) |
| `#backoffice` | 백오피스 | **452행** | ⚠️ **`js/backoffice.js`** — **관리자(`users.is_admin`)만.** 네비 버튼도 숨고, 서버가 404 로 막는다 |
| `#dashboard` | 멘토링 대시보드 | **458행** | HTML + `js/mentoring.js` |
| `#search` | 멘토 찾기 | **554행** | HTML + `js/mentoring.js` (카드) — **목록은 `GET /api/mentors` 다.** 프로필을 채운 실제 멘토만 뜬다 |
| `#profile` | 멘토 상세 | **611행** | ⚠️ **`js/mentoring.js`** |
| `#mentoring` | 내 멘토링 | **616행** | HTML + `js/mentoring.js` (탭 내용) |
| `#specup` | 스펙 채우기 | (`#page-specup`) | ⚠️ **`js/specup.js`** — HTML 은 `<div id="specup-wrap">` 하나뿐 |
| `#jd` | 자소서 코치 | **709행** | HTML(입력부) + `js/jd-coach.js`(결과) |
| `#career` | 커리어 로드맵 | **793행** | HTML(사이드바) + `js/career.js`(본문) |
| ~~`#mentor-profile`~~ | (옛 주소) | — | **`#mypage/mentor` 탭으로 옮겼다.** 이 주소로 들어오면 그쪽으로 보낸다 |

### 🧭 커리어 로드맵은 화면 하나가 아니다

`#career` 는 로드맵의 **1단계**일 뿐이고, 흐름은 네 화면을 가로지른다.

```
① #career 직무 찾기 → ② #dashboard 지금 내 위치(CAS)
   → ③ #company 지원할 회사 → ④ #jd 자소서 코치
                ↑
       ②에서 부족하면 #specup 스펙 채우기 로 갔다가 ②로 돌아온다
```

- 곁가지 `#specup` 은 예전에 `#mypage/spec`(스펙 입력 폼)이었다. 거기는 *이미 한 것을
  적는* 곳이라, 부족한 항목을 보고 넘어온 학생에게 빈 폼만 주고 흐름이 끊겼다.
  지금은 부족 항목마다 **지금 접수 중인 시험·모집 공고**를 붙여 보여준다

- 고른 직무·회사는 **`js/roadmap.js` 한 곳**에 있다. 화면끼리 직접 주고받지 않는다
- 네 화면 맨 위의 스텝바도 거기서 나온다 — HTML 에는 빈 껍데기
  `<div class="rm-bar-host" id="rm-bar-*">` 만 있고 `Roadmap.mount()` 가 채운다
- **옛 '회사 검색 / 자소서 코치' 서브네비(`.cover-subnav`)는 없앴다.** 그 둘이
  3·4단계 그 자체라 같은 이동이 두 줄이 됐다
- 자세한 판단 근거는 `CROAD-작업정리.md` **17장**

### ⚠️ HTML만 보면 안 보이는 화면

**아래 세 곳은 HTML이 빈 껍데기다.** 여기서 제일 많이 헤맨다.

| 화면 | HTML에 있는 것 | 실제 내용 |
|---|---|---|
| 스펙 입력 | `<div id="mypage-form-container"></div>` | `spec-form.js` → `render()` |
| 멘토 프로필 | `<div id="mentor-profile-container"></div>` | `mentor-profile.js` → `render()` |
| 멘토 상세 | `<div class="wrap" id="profile-body"></div>` | `mentoring.js` → `openProfile()` |
| 백오피스 | `<div class="bo-wrap"></div>` | `backoffice.js` → `render()` |
| 스펙 채우기 | `<div class="wrap" id="specup-wrap"></div>` | `specup.js` → `render()` |

**커리어 로드맵과 자소서 코치도 절반은 JS다.**

| 화면 | HTML에 있는 것 | JS가 채우는 것 |
|---|---|---|
| 커리어 로드맵 | 왼쪽 사이드바 껍데기 `#job-major-list` | `#career-main` — 1차/2차 분류·직업 카드·연봉·스펙 통계 (`career.js`) |
| 자소서 코치 | 입력 4분할(회사/공고/JD/문항) | `#jd-result`·`#jd-questions-result`·`#jd-news-result` (`jd-coach.js`) |

---

## 3. JS 모듈 — 무엇을 담당하나

로드 순서는 `careerly.html` 821~863행. **의존 순서가 있으니 임의로 바꾸지 말 것**
(`db.js` → 카탈로그 → `cas.js` → 화면 모듈 → `app.js`).

### 화면 모듈

| 파일 | 줄 | 담당 | 주로 건드리는 DOM |
|---|---|---|---|
| `app.js` | 341 | 라우터·로그인·회원가입·네비 | `page-*`, `nav-*`, `su-*` |
| `home.js` | 165 | 홈 화면 섹션 | `page-main`, `job-rail`, `insights-sec` |
| `spec-form.js` | 878 | **스펙 입력 폼 전체** | `sf-*` (어학·자격증·활동 편집기) |
| `career.js` | 575 | **커리어 로드맵** | `career-main`, `job-major-list` |
| `jd-coach.js` | 670 | **자소서 코치** | `jd-*` |
| `mentoring.js` | 1341 | 멘토 찾기·상세·내 멘토링·대시보드 | `mentor-grid`, `profile-body`, `mentoring-body` |
| `backoffice.js` | 273 | 백오피스 | `bo-*` |
| `specup.js` | 380 | **스펙 채우기** — 부족 항목 + 시험일정·공모전 | `specup-wrap`, `sup-*` |
| `company-cover.js` | 1409 | **회사 찾기(로드맵 3단계)** — 업종 단계 고르기 + 기업 리포트 | `company-*`, `.ind-*` |

### 데이터·계산 모듈 (화면 없음)

| 파일 | 줄 | 역할 |
|---|---|---|
| `db.js` | 475 | **서버 API 호출 전부.** 새 API를 쓰려면 여기에 함수부터 추가 |
| `roadmap.js` | 250 | **로드맵 흐름 상태의 단일 출처** — 고른 직무·회사, 집계 기준(`bench()`), 네 화면 공통 스텝바 |
| `cas.js` | 462 | **CAS 점수 엔진 · 모든 배점 상수의 단일 출처** (백엔드도 이걸 읽는다) |
| `aggregation.js` | 195 | 선배 스펙 집계 (평균·보유율) |
| `keco.js` | 80 | 직업 분류 카탈로그 (임금직업정보). 로드맵이 쓴다 |
| `ncs.js` | 1189 | NCS 분류 — **자동 생성 파일, 손으로 고치지 말 것.** 지금은 `job-trends` 집계만 쓴다 |
| `cas-radar.js` / `cas-hero.js` / `cas-compare.js` | 525 | CAS 점수 시각화 조각 |

---

## 4. CSS — 어느 파일에 있나

| 파일 | 줄 | 범위 |
|---|---|---|
| `main.css` | 1544 | 공통(네비·버튼·폼) + **홈 일부 + 스펙 입력 + 커리어 로드맵** |
| `mentoring.css` | 781 | 멘토 찾기·상세·내 멘토링·대시보드 |
| `jd-coach.css` | 308 | 자소서 코치 |
| `home.css` | 251 | 홈 |

로드 순서는 `main.css` → `home.css` → `mentoring.css` → `jd-coach.css`.
**뒤에 오는 파일이 이긴다.** 같은 클래스명을 두 파일에 쓰면 조용히 덮인다.

---

## 5. 백엔드 — API 지도

`backend/src/server.js` 가 진입점. 라우터가 붙는 곳은 다음 5개다.

| 경로 | 파일 | 역할 |
|---|---|---|
| `/api/career-data` | `routes/careerData.js` | 동문 데이터 |
| `/api/recommendations` | `routes/recommendations.js` | 추천 |
| `/api/cas` | `routes/casAnalyze.js` | 스펙 텍스트 AI 분석 |
| `/api/jd` | `routes/jdCoach.js` | 자소서 코치 역량 추출 |
| `/api/news` | `routes/news.js` | 회사 뉴스 |

`server.js` 안에 직접 있는 주요 라우트:

| 메서드 · 경로 | 하는 일 |
|---|---|
| `POST /api/auth/signup` · `login` · `logout`, `GET /api/auth/me` | 인증(세션 쿠키) |
| `GET/PUT /api/specs/me` | 내 스펙 |
| `GET /api/company/classify` | 회사명 → 기업 규모 |
| `GET /api/company/suggest` | 회사명 자동완성 |
| `GET /api/company/industry-tree` | **취업 업종 트리** — 회사 찾기가 쓰는 목록(민간 3단·공공 4단). `?middle=&job=` 을 주면 추천 업종이 `focus.minors` 로 붙는다 |
| `GET /api/certs` | 자격증 카탈로그(국가자격+민간) |
| `GET /api/jobs` | **직업 분류 + 연봉** (커리어 로드맵) |
| `POST /api/jd/posting` | **공고 주소 → 본문** (로그인 필수·SSRF 차단·5분 15회) |
| `POST /api/admin/*` | 데모 시드·초기화 (**운영에선 404**) |

### 외부 데이터 캐시

화면에서 외부 API를 직접 부르지 않는다. **수집 스크립트가 JSON으로 받아 두고 서버는 그걸 읽는다.**

| 캐시 파일 | 만드는 스크립트 |
|---|---|
| `wage-jobs.json` | `scripts/fetch-wage-jobs.js` (직업 분류·연봉) |
| `qnet-certs.json` | `scripts/fetch-qnet-certs.js` (국가자격 613종) |
| `ftc-large-groups.json` | `scripts/fetch-ftc-groups.js` (대기업 판별) |
| `public-orgs.json` · `local-public-orgs.json` | `scripts/fetch-public-orgs.js` · `fetch-local-public-orgs.js` |
| `work24-companies.json` | `scripts/fetch-worknet-jobs.js` |
| `ncs-taxonomy.json` | `scripts/fetch-ncs-taxonomy.js` |

자세한 규격·함정은 `docs/외부API-연동구조.md` 참고.

---

## 6. 찾는 요령

### 화면에서 본 문구로 찾기 (제일 빠름)

```bash
grep -rn "찾는 문구" frontend/careerly.html frontend/js/
```

HTML에서 안 나오면 JS가 만든 것이다. 반대도 마찬가지.

### 클래스명·id 로 찾기

```bash
grep -rn "jc-wage" frontend/          # CSS·JS 어디에 있든 다 나온다
```

### 페이지 컨테이너 위치를 다시 뽑기 (행 번호가 밀렸을 때)

```bash
grep -n 'id="page-[a-z]*"' frontend/careerly.html
```

### 어떤 JS가 어떤 DOM을 채우는지

```bash
grep -n "getElementById\|querySelector" frontend/js/career.js
```

---

## 7. 고칠 때 알아둘 것

- **테스트는 저장소 루트에서**: `for f in test/*.test.js; do node "$f" | tail -1; done`
- **새 라우트·새 파일을 추가하면 서버를 반드시 재시작한다** (`npm run dev`)
- **프론트만 고쳤으면 새로고침으로 충분하다** (빌드 없음)
- **`file://` 로 열면 동작하지 않는다** — 데이터가 전부 서버 API 에 있다. `http://localhost:3000` 으로 접속
- **`js/ncs.js` 는 자동 생성 파일이다.** 고치려면 `backend/scripts/gen-ncs-js.js` 를 고치고 다시 생성
- **배점 상수는 `js/cas.js` 한 곳에만 둔다.** 백엔드도 여기서 읽으므로 다른 곳에 복사하지 말 것

### 자주 하는 작업 → 고칠 파일

| 하려는 것 | 고칠 곳 |
|---|---|
| 스펙 입력 항목 추가·수정 | `js/spec-form.js` + 저장 형식은 `handleSave()` |
| CAS 배점·가중치 변경 | `js/cas.js` (+ `test/cas.test.js`) |
| 커리어 로드맵 화면 | `js/career.js` + `css/main.css` |
| 로드맵 **흐름**(단계 순서·스텝바·목표 직무) | `js/roadmap.js` + `css/main.css` 의 `.rm-*` |
| 회사 찾기 화면(단계 고르기) | `js/company-cover.js` + `css/jd-coach.css` 의 `.ind-*` |
| 자사 채용페이지 링크 추가 | `backend/data/career-pages.json` — **추측 금지.** `scripts/check-career-pages.js` 로 확인하고 넣는다 |
| 업종 이름·분류 규칙 | `backend/src/job-industry.js` 의 `TAXONOMY` — **업종코드가 열쇠, 이름만 우리 말** |
| 직무 → 계열(옛 축) | `backend/src/company-sectors.js` 의 `SECTORS_BY_MIDDLE` — **프론트에 복사하지 말 것** |
| 자소서 코치 화면 | `js/jd-coach.js` + `css/jd-coach.css` |
| 공고 주소 가져오기 | `backend/src/posting-fetch.js` — **본문 추출·SSRF 차단이 여기 다 있다** |
| 멘토링 관련 전부 | `js/mentoring.js` + `css/mentoring.css` |
| 멘토 목록에 나가는 값 | `backend/src/repo.js` 의 `mentors.list()` — **필요한 칸만 이름으로 적는다**(로그인 없이 볼 수 있는 API 라 `SELECT *` 금지) |
| 추천 자격증·시행기관 | `backend/src/cert-reco.js` — 직무↔자격 분야 매핑표·발급기관표가 전부 여기 (+ `test/cert-reco.test.js`) |
| 직무별 채용공고 | `backend/src/alio-jobs.js` 의 `jobPostings()` — **공공기관 공고만** 있다는 사실을 사유에 적는다 |
| 새 API 추가 | `backend/src/server.js` (라우트) → `frontend/js/db.js` (호출 함수) |
| 회원가입 항목 추가 | `careerly.html`(입력칸) + `js/app.js`(검증·전송) + `backend/src/server.js`(저장) |
